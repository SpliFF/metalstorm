# Lane 8 — skills / agents / claude-config review (2026-09-10)

## STATUS

complete (wrapped early) — coordinator wrap-up directive received after commit 1. Existing-skill audit + fixes + splits landed; new skills, agents and claude-config NOT done (see `## Not done`).

Branch: `worktree-agent-a49f17a50f181a73a` (cut from main `88d257bce2`, merged main tip on start).

## Findings (ranked)

| # | Sev | Where | What | Status |
|---|-----|-------|------|--------|
| 1 | high | `.claude/skills/spring-test/SKILL.md` (default loop, "Getting a test game", recipe 1) | Contradicted spring-debug/run skills: told the reader `wait:'ticking'` "can never complete browserless" and to *navigate a browser by hand* to `browserUrl`; the canonical path is `launch_scenario({openBrowser:true})` (server.js has `openBrowser`/`browserHeadless`). | FIXED |
| 2 | high | `.claude/skills/game-browser-test/SKILL.md` frontmatter + "Network verification" flow | Named **WebRTC** and `POST /api/rtc/offer` — WebRTC was fully removed (GW7); the route does not exist in `docs/api.md` or `rts/lobby_main.cpp`. Also claimed `/api/rooms` polls every 2 s — the client uses the `/api/rooms/stream` SSE (`lobby-ui.ts:650`). | FIXED |
| 3 | med | `.claude/skills/run-springrts-web/SKILL.md` Gotchas | Claimed `data/games/*` is gitignored — `data/games/metalstorm` is **tracked** (1216 files); only `content/*` and `data/maps/*` are ignored (map assets are force-added). | FIXED |
| 4 | med | `.claude/skills/run-springrts-web/example-cuspbr-corcom.jpg` | A BAR-unit screenshot (`cuspbr`/`corcom`) shipped as "example of a working drive" — BAR/ZK archived 2026-08-02. | FIXED (deleted) |
| 5 | med | `.claude/skills/spring-debug/SKILL.md` tools table | `launch_scenario` row omitted `modoptions`, `skipBriefing`, `waitTimeoutMs`; `generate_scenario` omitted `works, harbour, shanty, coverage, player`; `restart_game` omitted `roomId`; def/cheat verbs (`set_los {enable}`, `get_unit_def {gameId,name|defId}`, …) were an "etc." with no args. | FIXED |
| 6 | med | `.claude/skills/forge/SKILL.md` | "Do NOT create venvs / npm ci" contradicts `tools/forge/README.md` (per-checkout `venv` + `npm ci` are gitignored and required once); `dist/batch-01..04` described as present but it is local-only/gitignored (absent in every worktree). | FIXED |
| 7 | low | `.claude/skills/run-springrts-web/smoke.sh` header | Pointed at a SKILL.md section "Run (agent path)" that no longer exists. | FIXED |
| 8 | low | skills at 306–401 lines each | Reference material (capture/camera, perf traps, lobby flow, window.test table) inflated four skills past the point of being read; split into sibling files with pointers. | FIXED |
| 9 | info | `~/.claude/skills/{task,work}` | Listed as owned but live in the USER dir, not the repo; not editable from this lane (outside the worktree). Reviewed — proposals below. | PROPOSED |
| 10 | info | `.gitignore:100` | `.claude/*` is ignored with only `!.claude/skills/` re-included, so `.claude/agents/` must be force-added. Reason recorded in the ignore comment: "share skills via repo, keep everything else untracked". | PROPOSED (`!.claude/agents/`) |

## Changes

- Split reference material out of the four big skills (line counts 327/401/268/306 → 180/226/225/261): `spring-debug/capture-and-camera.md`, `game-browser-test/{perf-measurement-traps,lobby-flow}.md`, `spring-test/window-test-reference.md`, `run-springrts-web/{hand-rolled-framing,lobby-flow-verify.js}.md`.
- spring-debug: def/cheat verb arg table, 62-tool census line + `check-skills.sh` pointer, `PENDING lane 7` placeholder, "Traps that outlive any one tool" (schema-hash/release binary, 120 s wakeups, `pgrep -f`, headless never checkpoints, `state>=4`, SQLite THREADSAFE=2).
- spring-test: canonical loop is `openBrowser:true`; recipe 1 uses `capture_subject`.

## Proposed C++ patches (UNCOMPILED)

None.

## Out-of-lane findings

- `data/games/metalstorm/LuaRules/Gadgets/tests/guidance_wire_spec.lua:212` — standing RED on main (`guidance.fund applies the rate cap and charges the one-shot amount`: expected 250, got 0). Reproduces standalone from `Gadgets/` (`busted tests/guidance_wire_spec.lua` → 56/1/0). Lane 5 (ai-actuation / guidance gadget).
- `LuaRules/Gadgets/tests/game_scenario_ai_spec.lua` @296/330/359/375 — 3 failures + 1 error from the game root: the **retired** `scenarios/meridian_basin.lua` names defs that no longer exist (`ms_scout_buggy`, `ms_supply_truck`, `fable_airship`, `ms_technical`, `ms_grain_silo`). Either retire the spec's meridian cases or re-point them at `crossing_standoff`. Lane 12 (battle-flow / scenarios).
- `tools/debug-mcp` `node --test` needs `data/games/metalstorm/cache/defs/` (baked) and `data/maps/green_flat_x34_v3` on disk — 7 failures in a fresh worktree (210/217), 217/217 with both symlinked from the main checkout. Lane 7 should either fixture the def cache or skip with a named reason.
- `data/games/metalstorm/ui/vitest.config.js` header advertises the relative `--config ../…` form that dies with `UNRESOLVED_ENTRY`; absolute paths are required. Lane 9.

## Assumptions / decisions

- `task`/`work` skills are user-level (`~/.claude/skills`), not repo files — not copied into the repo (a repo copy would shadow the user's and drift). Proposals only.
- `.claude/agents/*.md` force-added (`git add -f`) rather than editing `.gitignore` (not my lane).

## Not done (wrapped early — all verified inputs are recorded here so the next session can write straight away)

1. **New skills** (`.claude/skills/<name>/SKILL.md`): `world-layer`, `ai-player`, `metalstorm-design-review`, `lua-gadget-test`, `client-gate`. Verified facts to build them from:
   - world-layer: 19 `/api/world/*` routes (`docs/api.md` §World layer; `lobby_main.cpp:4711-5628`); GETs public + `?world=`, POSTs act on the primary world; `SeedDefaultWorld` runs at lobby boot (no CLI flag); a fresh account is credited `startingAuthority=100` ≥ `foundFactionAuthority=100`, founding spends 50, `claimPoiCost=25`, claims expire 30 world-days; staging window = transit-priced, default 12 world-h (=30 real min at 24:1), clamp 1–72 world-h, tunable only via `worlds.config_json` (`stagingWindow*WorldMs`) — no admin route, so stop the lobby before an `sqlite3 UPDATE` (THREADSAFE=2); materialisation is the lobby loop `DueStagings → demandSeedWar → MarkMaterialised → WorldEscrow::MarkEngaged` (`lobby_main.cpp:9082-9125`), log line `world staging N at POI 'x' materialised as room R`, notification kind `materialised` on the lobby chat SSE only; observe with `query_db`: `world_staging(state, room_id, attempts, last_error, ends_at_world_ms)`, `wars(room_id, state, scenario)`; `/api/world/seasons/latest` is NOT a route (digits only); 25 `world_*`/`war*` tables, none of `world_*` keyed by `room_id` (the `room_id` columns in `WorldStaging/WorldEscrow` are labels).
   - ai-player: strategos module map (`ai/strategos/README.md`); discovery = `<game>/ai/<plugin>/ai.config.lua`; seat via `launch_scenario {ai:'strategos'}`, `launch_direct` `aiSlots[{aiId,team,startPos,profile}]`, `POST /api/rooms/ai/add {ai_id,name?,team?}` + `/api/rooms/ai/profile {slot_index, profile}`, headless `aiSlots[]` (`tools/headless-batch/fixtures/metalstorm-determinism.json`), scenario `ai={team,profile,slate,stipend}` (`slate.kinds ⊆ {garrison,raid,toll}`), caretaker via `ai_caretaker` modoption → `Spring.SpawnAIPlayer`; profile read from team rulesParam `ai_profile[_<playerID>]`; guidance verbs `guidance.{stance,paint,lock,delegate,fund,roe,veto}` are LuaRulesMsgs (`parley/wire.lua`) sent by a seated team member — from tooling use `client/wire/run-wire-client.mjs --wire-command guidance.veto --wire-field goalId=…` (the `make test-ai-veto-loop` arm); read back `Spring.GetDirectives(team)`, `guidance_<t>_intent_count`/`intent_<i>_goal_id`, `authority_player_<id>`, `get_logs {section:'ai'}`; traps: `health` is a 0-1 ratio (D68), a human on the AI's team flips it to `co_commander` with pool 0 (fund it first), `--player` + wait for `firing GameStart`, `stopAt.luaCondition` polled once per game-second. `docs/ai-players.md` / `ai/lib/**` / `tools/ai-eval` are lane 6's (not present on main yet).
   - metalstorm-design-review census commands (all verified 2026-09-10): buildoptions live only in `units/{buildings_sites,engineers,buildings_military,buildings_support}.lua` (`grep -rl buildoptions data/games/metalstorm/units`); reclaim/resurrect/capture vetoed in `Gadgets/squad.lua:32-34`; `frame % PERIOD == 0` census → exactly one live hit `game_gameover.lua:453` (`FOOTHOLD_PERIOD`) — a candidate violation of the ARCHITECTURE rule (lane 11); `actuators.lua` has no `moveSquad|attackTarget|issueCommand`; 12 `FIDELITY-STANDIN` files under `rts/`; unit creators are only `game_start`, `game_scenario`, `game_transports`, `civilians/{convoy,spawn}`; `metalstorm.ui.json` `widgets[]` keys are `id, mount, entry, subscribes, _chrome` (`_chrome` is a string, not an object — the drill-down "collapsed" state is not declarative there).
   - lua-gadget-test (baselines 2026-09-10, worktree of main `88d257bce2`+21): `cd LuaRules/Gadgets/authority && busted .` 50/0/0; `cd LuaRules/Gadgets/{objectives,regions,civilians,parley} && busted tests/` 139 / 70 / 72 / 17 all green; `cd ai/strategos && busted tests/` 131/0/0; `cd LuaRules/Gadgets && busted --exclude-pattern=scenario tests/` 479/1/11 (the 11 errors are `game_landmarks_spec` wanting the game root — it is 11/11 from there; the 1 failure is the standing `guidance_wire_spec:212` red); `cd data/games/metalstorm && busted --pattern=scenario LuaRules/Gadgets/tests/` 112/3/1 (all four = retired `meridian_basin` defs). Whole-tree runs are red from every cwd by construction.
   - client-gate: `cd client && npx tsc --noEmit > f 2>&1; echo EXIT=$?`; `npx vitest run > f 2>&1` then `grep -E 'Test Files|Tests '`; UI suite with ABSOLUTE `--config`/`--root`; bench arms `npx vitest bench --run src/core/<x>.bench.ts` (3 exist; outside gate and tsc); `tools/debug-mcp && node --test` = 217/217 only with `data/games/metalstorm/cache/defs` + `data/maps/green_flat_x34_v3` present (symlink from the main checkout in a worktree; 210/217 without); `tools/headless-batch && npm test`; mapgen = enumerating `.venv/bin/python -m unittest tests.test_*` form with `MS_MAPS_DIR`; never pipe a gate; on a loaded box (`sysctl vm.swapusage`) treat background completions as wake-ups and grep a sentinel yourself.
2. **`.claude/agents/`** definitions (gameplay-reviewer, world-layer-engineer, ai-author, hud-designer, map-forger, model-forger, lane-lander) — `.gitignore:100` ignores `.claude/*` except `skills/`, so they need `git add -f` (or a `!.claude/agents/` line, proposed). lane-lander must confirm merged work in the RESULT tree by grep (`git show <merge>:<path> | grep -c <symbol>` vs `^2`), never by `merge-base --is-ancestor`; land by `git merge --no-ff` + verify ON MAIN after the merge (user preference); `tasks_ack` is FF-only.
3. **tools/claude-config**: README/setup.sh still describe a 7-tool MCP (there are 62) and say `.claude/` is gitignored (skills are tracked); `settings.local.json` template duplicates `.mcp.json` (which also declares `chrome-devtools --isolated`); `taskherd` MCP is user-level (`~/.claude.json`: `node /Users/shannon/WarriorHut/Projects/taskherder/bin/mcp.mjs`); `.tasks/config.json` = `isolation: inplace`, `land: manual-gate`, `parallel.max 4`, bootstrap copies `build/ data/ node_modules/ PLAN*.md CLAUDE.md`. `check-skills.sh` not written — the drift census it should automate is: tool names in `.claude/**/*.md` vs `grep -o "name: '[a-z_]*'" tools/debug-mcp/server.js`, `/api/...` paths vs `docs/api.md` + `rts/lobby_main.cpp`, `test.<method>` vs `client/src/core/test-harness.ts`, and every relative file path mentioned must exist. A scratch schema dumper exists at the session scratchpad (`schema-dump.js`, brace-matching parser of each tool's `properties`).
4. **`~/.claude/skills/{task,work}` proposals**: `work` is subsumed by `task` (its own description says so) — retire it; `task` §1 should say PLAN files are unreadable by `Read` (page with `awk 'NR>=A&&NR<=B{print substr($0,1,400)}'`), that a `/task` fire must `export PATH="/opt/homebrew/bin:…"` first (stripped PATH = silent exec failure), and that "done (exit 0)" is a record of intent (check `git status --porcelain` + `merge-base --is-ancestor`).

## Next milestones

- Write the five skills and seven agents from the facts above (one session, no re-verification needed).
- `check-skills.sh` + wire it into `make test-debug-mcp` or a pre-commit hook.
- Fix `guidance_wire_spec:212` (lane 5) and retire the meridian cases in `game_scenario_ai_spec` (lane 12) so `lua-gadget-test` can quote an all-green partition.
- Fixture the baked def cache for `tools/debug-mcp` tests (lane 7) so a fresh worktree is 217/217.
- Fill the `PENDING lane 7` placeholder in `spring-debug/SKILL.md` once the world_*/ai_*/nl_command tools land.
