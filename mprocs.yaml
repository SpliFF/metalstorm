procs:
  lobby:
    shell: "./build/debug/spring-lobby --port 8011 --game content/games/papertanks --maps content/maps --db data/spring-server.db"
    stop: SIGINT

  client:
    shell: "cd client && GAME_SERVER_PORT=8011 npx vite dev --port 8012"
