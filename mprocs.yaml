procs:
  server:
    shell: "./build/debug/spring-server --port 8011 --game content/games/papertanks --map content/maps/scorched_crossing_v2.4 --db data/spring-server.db"
    stop: SIGINT

  client:
    shell: "cd client && GAME_SERVER_PORT=8011 npx vite dev --port 8012"
