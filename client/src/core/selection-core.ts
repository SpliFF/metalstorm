/** Shared selection/pick/order constants — single source of truth for
 *  the main-thread InputManager (legacy) and worker WorkerSelection. */
export const SELECT_PIXEL_RADIUS = 32;  // screen-space pick radius (px)
export const SELECT_RADIUS = 32;        // world-space pick radius (elmos)
export const DRAG_THRESHOLD_PX = 6;     // drag-box activation threshold
