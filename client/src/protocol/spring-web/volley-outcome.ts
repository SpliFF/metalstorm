// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { CombatResult } from '../spring-web/combat-result.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * Metalstorm statistical combat (Model 1) per-volley outcome
 * (PLAN-metalstorm-combat-resolution.md §2.3 / PLAN-macro-combat.md §3).
 * One per squad-volley, NOT per round — the client invents `rounds`
 * cosmetic tracers/impacts from a single event. Visibility-filtered
 * per session in StateStreamer::BroadcastCombatEvents:
 *   - viewer sees the attacker  -> full outcome (Hit/Miss, damage, attacker_id)
 *   - viewer owns the target but can't see the attacker -> result Unknown,
 *     damage 0, attacker_id 0, plus a counterbattery radar-blip reveal
 *     (reveal_attacker + reveal_pos, Q-D-c ANSWERED 2026-07-19)
 *   - viewer sees only the target (neither owner nor attacker-LOS) -> Unknown,
 *     no reveal
 *   - viewer sees neither -> not sent
 */
export class VolleyOutcome implements flatbuffers.IUnpackableObject<VolleyOutcomeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):VolleyOutcome {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsVolleyOutcome(bb:flatbuffers.ByteBuffer, obj?:VolleyOutcome):VolleyOutcome {
  return (obj || new VolleyOutcome()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsVolleyOutcome(bb:flatbuffers.ByteBuffer, obj?:VolleyOutcome):VolleyOutcome {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new VolleyOutcome()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

attackerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

weaponDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

targetId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

targetPos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

resolveFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

result():CombatResult {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : CombatResult.Hit;
}

damage():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

rounds():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 1;
}

team():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 255;
}

/**
 * Counterbattery reveal (Q-D-c). True only for the victim's team when
 * they cannot see the attacker: the client drops a radar blip at
 * `reveal_pos` (the attacker's firing position) so statistical artillery
 * is counterable. Never set for viewers who can already see the attacker.
 */
revealAttacker():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

revealPos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

/**
 * Attacker morale posture, a derived proxy (morale = clamp(hp% - 10, 0, 100)):
 * 0 = normal, 1 = retreating-while-firing (morale < 10), 2 = panicking
 * (morale == 0). Lets the client show a retreat/panic indicator. Only
 * meaningful when attacker_id != 0 (attacker visible).
 */
attackerPosture():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startVolleyOutcome(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addAttackerId(builder:flatbuffers.Builder, attackerId:number) {
  builder.addFieldInt32(0, attackerId, 0);
}

static addWeaponDefId(builder:flatbuffers.Builder, weaponDefId:number) {
  builder.addFieldInt16(1, weaponDefId, 0);
}

static addTargetId(builder:flatbuffers.Builder, targetId:number) {
  builder.addFieldInt32(2, targetId, 0);
}

static addTargetPos(builder:flatbuffers.Builder, targetPosOffset:flatbuffers.Offset) {
  builder.addFieldStruct(3, targetPosOffset, 0);
}

static addResolveFrame(builder:flatbuffers.Builder, resolveFrame:number) {
  builder.addFieldInt32(4, resolveFrame, 0);
}

static addResult(builder:flatbuffers.Builder, result:CombatResult) {
  builder.addFieldInt8(5, result, CombatResult.Hit);
}

static addDamage(builder:flatbuffers.Builder, damage:number) {
  builder.addFieldInt16(6, damage, 0);
}

static addRounds(builder:flatbuffers.Builder, rounds:number) {
  builder.addFieldInt8(7, rounds, 1);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(8, team, 255);
}

static addRevealAttacker(builder:flatbuffers.Builder, revealAttacker:boolean) {
  builder.addFieldInt8(9, +revealAttacker, +false);
}

static addRevealPos(builder:flatbuffers.Builder, revealPosOffset:flatbuffers.Offset) {
  builder.addFieldStruct(10, revealPosOffset, 0);
}

static addAttackerPosture(builder:flatbuffers.Builder, attackerPosture:number) {
  builder.addFieldInt8(11, attackerPosture, 0);
}

static endVolleyOutcome(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): VolleyOutcomeT {
  return new VolleyOutcomeT(
    this.attackerId(),
    this.weaponDefId(),
    this.targetId(),
    (this.targetPos() !== null ? this.targetPos()!.unpack() : null),
    this.resolveFrame(),
    this.result(),
    this.damage(),
    this.rounds(),
    this.team(),
    this.revealAttacker(),
    (this.revealPos() !== null ? this.revealPos()!.unpack() : null),
    this.attackerPosture()
  );
}


unpackTo(_o: VolleyOutcomeT): void {
  _o.attackerId = this.attackerId();
  _o.weaponDefId = this.weaponDefId();
  _o.targetId = this.targetId();
  _o.targetPos = (this.targetPos() !== null ? this.targetPos()!.unpack() : null);
  _o.resolveFrame = this.resolveFrame();
  _o.result = this.result();
  _o.damage = this.damage();
  _o.rounds = this.rounds();
  _o.team = this.team();
  _o.revealAttacker = this.revealAttacker();
  _o.revealPos = (this.revealPos() !== null ? this.revealPos()!.unpack() : null);
  _o.attackerPosture = this.attackerPosture();
}
}

export class VolleyOutcomeT implements flatbuffers.IGeneratedObject {
constructor(
  public attackerId: number = 0,
  public weaponDefId: number = 0,
  public targetId: number = 0,
  public targetPos: Vec3T|null = null,
  public resolveFrame: number = 0,
  public result: CombatResult = CombatResult.Hit,
  public damage: number = 0,
  public rounds: number = 1,
  public team: number = 255,
  public revealAttacker: boolean = false,
  public revealPos: Vec3T|null = null,
  public attackerPosture: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  VolleyOutcome.startVolleyOutcome(builder);
  VolleyOutcome.addAttackerId(builder, this.attackerId);
  VolleyOutcome.addWeaponDefId(builder, this.weaponDefId);
  VolleyOutcome.addTargetId(builder, this.targetId);
  VolleyOutcome.addTargetPos(builder, (this.targetPos !== null ? this.targetPos!.pack(builder) : 0));
  VolleyOutcome.addResolveFrame(builder, this.resolveFrame);
  VolleyOutcome.addResult(builder, this.result);
  VolleyOutcome.addDamage(builder, this.damage);
  VolleyOutcome.addRounds(builder, this.rounds);
  VolleyOutcome.addTeam(builder, this.team);
  VolleyOutcome.addRevealAttacker(builder, this.revealAttacker);
  VolleyOutcome.addRevealPos(builder, (this.revealPos !== null ? this.revealPos!.pack(builder) : 0));
  VolleyOutcome.addAttackerPosture(builder, this.attackerPosture);

  return VolleyOutcome.endVolleyOutcome(builder);
}
}
