// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ProjectileImpactKind } from '../spring-web/projectile-impact-kind.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * PLAN-latency L3 — a Tier-S projectile's resolved outcome, stamped with
 * the frame it resolves on.
 *
 * ProjectileImpactEvent carries no frame, so the client can only play the
 * explosion when the packet arrives — which is `D` frames after the
 * presentation cursor shows the projectile reaching the target. This event
 * carries `outcome_frame`, so the client schedules the flash/burst on the
 * L1 timeline and it fires exactly when the rendered projectile arrives.
 *
 * Emitted the moment the sim resolves the shot: a collision, a shield
 * absorption, or an interceptor kill. Disjoint from FireOutcomeEvent,
 * which is the Tier-C (never-simulated) equivalent resolved at FIRE time.
 */
export class OutcomeKnownEvent implements flatbuffers.IUnpackableObject<OutcomeKnownEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OutcomeKnownEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOutcomeKnownEvent(bb:flatbuffers.ByteBuffer, obj?:OutcomeKnownEvent):OutcomeKnownEvent {
  return (obj || new OutcomeKnownEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOutcomeKnownEvent(bb:flatbuffers.ByteBuffer, obj?:OutcomeKnownEvent):OutcomeKnownEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OutcomeKnownEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Reuses ProjectileImpactKind — this IS a discovered collision, unlike
 * FireOutcomeEvent's fire-time prediction, so the same vocabulary fits.
 * Shield and Intercepted are reported accurately here; the legacy
 * ProjectileImpactEvent reports both as Terrain (both paths funnel
 * through the no-argument CWeaponProjectile::Collision overload).
 */
outcome():ProjectileImpactKind {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ProjectileImpactKind.Terrain;
}

/**
 * Sim frame the outcome resolves on — schedule the FX here.
 */
outcomeFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Where the visual terminates. The final keyframe (kind=Terminal) for
 * this proj_id carries the same position, so the spline provably ends
 * on the explosion.
 */
outcomePos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

/**
 * Hit unit / feature / shield-host (0 for terrain).
 */
targetId():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Owner-team id — see ProjectileImpactEvent.team.
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Weapon def driving the explosion, so the client can resolve the CEG
 * without a live projectile entry to look up.
 */
weaponDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

static startOutcomeKnownEvent(builder:flatbuffers.Builder) {
  builder.startObject(7);
}

static addProjId(builder:flatbuffers.Builder, projId:number) {
  builder.addFieldInt32(0, projId, 0);
}

static addOutcome(builder:flatbuffers.Builder, outcome:ProjectileImpactKind) {
  builder.addFieldInt8(1, outcome, ProjectileImpactKind.Terrain);
}

static addOutcomeFrame(builder:flatbuffers.Builder, outcomeFrame:number) {
  builder.addFieldInt32(2, outcomeFrame, 0);
}

static addOutcomePos(builder:flatbuffers.Builder, outcomePosOffset:flatbuffers.Offset) {
  builder.addFieldStruct(3, outcomePosOffset, 0);
}

static addTargetId(builder:flatbuffers.Builder, targetId:number) {
  builder.addFieldInt32(4, targetId, 0);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(5, team, 0);
}

static addWeaponDefId(builder:flatbuffers.Builder, weaponDefId:number) {
  builder.addFieldInt16(6, weaponDefId, 0);
}

static endOutcomeKnownEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): OutcomeKnownEventT {
  return new OutcomeKnownEventT(
    this.projId(),
    this.outcome(),
    this.outcomeFrame(),
    (this.outcomePos() !== null ? this.outcomePos()!.unpack() : null),
    this.targetId(),
    this.team(),
    this.weaponDefId()
  );
}


unpackTo(_o: OutcomeKnownEventT): void {
  _o.projId = this.projId();
  _o.outcome = this.outcome();
  _o.outcomeFrame = this.outcomeFrame();
  _o.outcomePos = (this.outcomePos() !== null ? this.outcomePos()!.unpack() : null);
  _o.targetId = this.targetId();
  _o.team = this.team();
  _o.weaponDefId = this.weaponDefId();
}
}

export class OutcomeKnownEventT implements flatbuffers.IGeneratedObject {
constructor(
  public projId: number = 0,
  public outcome: ProjectileImpactKind = ProjectileImpactKind.Terrain,
  public outcomeFrame: number = 0,
  public outcomePos: Vec3T|null = null,
  public targetId: number = 0,
  public team: number = 0,
  public weaponDefId: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  OutcomeKnownEvent.startOutcomeKnownEvent(builder);
  OutcomeKnownEvent.addProjId(builder, this.projId);
  OutcomeKnownEvent.addOutcome(builder, this.outcome);
  OutcomeKnownEvent.addOutcomeFrame(builder, this.outcomeFrame);
  OutcomeKnownEvent.addOutcomePos(builder, (this.outcomePos !== null ? this.outcomePos!.pack(builder) : 0));
  OutcomeKnownEvent.addTargetId(builder, this.targetId);
  OutcomeKnownEvent.addTeam(builder, this.team);
  OutcomeKnownEvent.addWeaponDefId(builder, this.weaponDefId);

  return OutcomeKnownEvent.endOutcomeKnownEvent(builder);
}
}
