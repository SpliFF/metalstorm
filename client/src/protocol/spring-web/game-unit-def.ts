// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Definition of one unit type. Sent once at game start so the client
 * knows what model/texture to load for each def_id it sees in entity
 * state snapshots. Parallel to MapFeatureDef but for dynamic entities.
 *
 * `model_url` and `texture_url` are HTTP paths relative to the game
 * server root, pointing at preprocessed `.glb` and `.png` files under
 * `data/games/<gameId>/models/`. Either may be empty if the unit type
 * has no model or preprocessing failed — the client falls back to a
 * coloured procedural shape.
 */
export class GameUnitDef implements flatbuffers.IUnpackableObject<GameUnitDefT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameUnitDef {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameUnitDef(bb:flatbuffers.ByteBuffer, obj?:GameUnitDef):GameUnitDef {
  return (obj || new GameUnitDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameUnitDef(bb:flatbuffers.ByteBuffer, obj?:GameUnitDef):GameUnitDef {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameUnitDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

defId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

modelUrl():string|null
modelUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
modelUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

textureUrl():string|null
textureUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
textureUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startGameUnitDef(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addDefId(builder:flatbuffers.Builder, defId:number) {
  builder.addFieldInt16(0, defId, 0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addModelUrl(builder:flatbuffers.Builder, modelUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, modelUrlOffset, 0);
}

static addTextureUrl(builder:flatbuffers.Builder, textureUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, textureUrlOffset, 0);
}

static endGameUnitDef(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameUnitDef(builder:flatbuffers.Builder, defId:number, nameOffset:flatbuffers.Offset, modelUrlOffset:flatbuffers.Offset, textureUrlOffset:flatbuffers.Offset):flatbuffers.Offset {
  GameUnitDef.startGameUnitDef(builder);
  GameUnitDef.addDefId(builder, defId);
  GameUnitDef.addName(builder, nameOffset);
  GameUnitDef.addModelUrl(builder, modelUrlOffset);
  GameUnitDef.addTextureUrl(builder, textureUrlOffset);
  return GameUnitDef.endGameUnitDef(builder);
}

unpack(): GameUnitDefT {
  return new GameUnitDefT(
    this.defId(),
    this.name(),
    this.modelUrl(),
    this.textureUrl()
  );
}


unpackTo(_o: GameUnitDefT): void {
  _o.defId = this.defId();
  _o.name = this.name();
  _o.modelUrl = this.modelUrl();
  _o.textureUrl = this.textureUrl();
}
}

export class GameUnitDefT implements flatbuffers.IGeneratedObject {
constructor(
  public defId: number = 0,
  public name: string|Uint8Array|null = null,
  public modelUrl: string|Uint8Array|null = null,
  public textureUrl: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const name = (this.name !== null ? builder.createString(this.name!) : 0);
  const modelUrl = (this.modelUrl !== null ? builder.createString(this.modelUrl!) : 0);
  const textureUrl = (this.textureUrl !== null ? builder.createString(this.textureUrl!) : 0);

  return GameUnitDef.createGameUnitDef(builder,
    this.defId,
    name,
    modelUrl,
    textureUrl
  );
}
}
