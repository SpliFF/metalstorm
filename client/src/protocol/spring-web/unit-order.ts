// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One queued order for a unit. Mirrors Spring's Command struct.
 * `cmd_id` follows Spring conventions: positive values are CMD_*
 * constants (CMD_MOVE=10, CMD_ATTACK=20, …), negative values are
 * build orders where `-cmd_id` is the unit-def id to construct.
 */
export class UnitOrder implements flatbuffers.IUnpackableObject<UnitOrderT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitOrder {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitOrder(bb:flatbuffers.ByteBuffer, obj?:UnitOrder):UnitOrder {
  return (obj || new UnitOrder()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitOrder(bb:flatbuffers.ByteBuffer, obj?:UnitOrder):UnitOrder {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitOrder()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

cmdId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

/**
 * Variable-length param vector. Common shapes:
 *   move/attack:   [x, y, z]
 *   target unit:   [unitId]
 *   build:         [x, y, z, facing]
 *   patrol/area:   [x, y, z, radius]
 */
params(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

paramsArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

/**
 * Bitfield: shift / alt / ctrl / right-mouse / internal-order.
 */
options():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * Unique tag assigned by the queue; used to address specific
 * orders for editing / removal.
 */
tag():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Frame this order auto-cancels at (INT_MAX = no timeout). The
 * server emits the absolute frame to keep the client wire-format
 * stateless across sim resyncs.
 */
timeout():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 2147483647;
}

static startUnitOrder(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addCmdId(builder:flatbuffers.Builder, cmdId:number) {
  builder.addFieldInt32(0, cmdId, 0);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOptions(builder:flatbuffers.Builder, options:number) {
  builder.addFieldInt8(2, options, 0);
}

static addTag(builder:flatbuffers.Builder, tag:number) {
  builder.addFieldInt32(3, tag, 0);
}

static addTimeout(builder:flatbuffers.Builder, timeout:number) {
  builder.addFieldInt32(4, timeout, 2147483647);
}

static endUnitOrder(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitOrder(builder:flatbuffers.Builder, cmdId:number, paramsOffset:flatbuffers.Offset, options:number, tag:number, timeout:number):flatbuffers.Offset {
  UnitOrder.startUnitOrder(builder);
  UnitOrder.addCmdId(builder, cmdId);
  UnitOrder.addParams(builder, paramsOffset);
  UnitOrder.addOptions(builder, options);
  UnitOrder.addTag(builder, tag);
  UnitOrder.addTimeout(builder, timeout);
  return UnitOrder.endUnitOrder(builder);
}

unpack(): UnitOrderT {
  return new UnitOrderT(
    this.cmdId(),
    this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength()),
    this.options(),
    this.tag(),
    this.timeout()
  );
}


unpackTo(_o: UnitOrderT): void {
  _o.cmdId = this.cmdId();
  _o.params = this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength());
  _o.options = this.options();
  _o.tag = this.tag();
  _o.timeout = this.timeout();
}
}

export class UnitOrderT implements flatbuffers.IGeneratedObject {
constructor(
  public cmdId: number = 0,
  public params: (number)[] = [],
  public options: number = 0,
  public tag: number = 0,
  public timeout: number = 2147483647
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const params = UnitOrder.createParamsVector(builder, this.params);

  return UnitOrder.createUnitOrder(builder,
    this.cmdId,
    params,
    this.options,
    this.tag,
    this.timeout
  );
}
}
