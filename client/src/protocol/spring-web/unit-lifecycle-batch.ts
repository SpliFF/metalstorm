// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitLifecycleEvent, UnitLifecycleEventT } from '../spring-web/unit-lifecycle-event.js';


/**
 * Per-tick batch of lifecycle events. Typically empty on quiet ticks;
 * populated on factory completion, AllowResourceTransfer, etc.
 * FromFactory / Taken / Given are broadcast unfiltered (transfers are
 * public). Created is filtered per-session to the viewer's ally team
 * (enemy UnitCreated is synthesised client-side from first-visibility).
 */
export class UnitLifecycleBatch implements flatbuffers.IUnpackableObject<UnitLifecycleBatchT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitLifecycleBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitLifecycleBatch(bb:flatbuffers.ByteBuffer, obj?:UnitLifecycleBatch):UnitLifecycleBatch {
  return (obj || new UnitLifecycleBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitLifecycleBatch(bb:flatbuffers.ByteBuffer, obj?:UnitLifecycleBatch):UnitLifecycleBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitLifecycleBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

events(index: number, obj?:UnitLifecycleEvent):UnitLifecycleEvent|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new UnitLifecycleEvent()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

eventsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUnitLifecycleBatch(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addEvents(builder:flatbuffers.Builder, eventsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, eventsOffset, 0);
}

static createEventsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEventsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitLifecycleBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitLifecycleBatch(builder:flatbuffers.Builder, eventsOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitLifecycleBatch.startUnitLifecycleBatch(builder);
  UnitLifecycleBatch.addEvents(builder, eventsOffset);
  return UnitLifecycleBatch.endUnitLifecycleBatch(builder);
}

unpack(): UnitLifecycleBatchT {
  return new UnitLifecycleBatchT(
    this.bb!.createObjList<UnitLifecycleEvent, UnitLifecycleEventT>(this.events.bind(this), this.eventsLength())
  );
}


unpackTo(_o: UnitLifecycleBatchT): void {
  _o.events = this.bb!.createObjList<UnitLifecycleEvent, UnitLifecycleEventT>(this.events.bind(this), this.eventsLength());
}
}

export class UnitLifecycleBatchT implements flatbuffers.IGeneratedObject {
constructor(
  public events: (UnitLifecycleEventT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const events = UnitLifecycleBatch.createEventsVector(builder, builder.createObjectOffsetList(this.events));

  return UnitLifecycleBatch.createUnitLifecycleBatch(builder,
    events
  );
}
}
