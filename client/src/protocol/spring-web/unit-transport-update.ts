// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitTransportInfo, UnitTransportInfoT } from '../spring-web/unit-transport-info.js';


/**
 * Per-session broadcast: every transport relationship visible to the
 * session. Sent every ~1 Hz alongside UnitCommandQueuesUpdate.
 * The client treats each snapshot as a complete replacement of its
 * transport cache: any transporter not listed has empty cargo, any
 * cargo not listed has no transporter.
 *
 * Visibility matches the UnitCommandQueuesUpdate filter (own-team +
 * allied teams). The transport message is intentionally a full
 * snapshot rather than per-event delta — at 1 Hz the bandwidth cost
 * is negligible (most teams have 0..few transports), and snapshots
 * are self-healing across packet loss / reconnect.
 */
export class UnitTransportUpdate implements flatbuffers.IUnpackableObject<UnitTransportUpdateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitTransportUpdate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitTransportUpdate(bb:flatbuffers.ByteBuffer, obj?:UnitTransportUpdate):UnitTransportUpdate {
  return (obj || new UnitTransportUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitTransportUpdate(bb:flatbuffers.ByteBuffer, obj?:UnitTransportUpdate):UnitTransportUpdate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitTransportUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

transports(index: number, obj?:UnitTransportInfo):UnitTransportInfo|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new UnitTransportInfo()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

transportsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUnitTransportUpdate(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addTransports(builder:flatbuffers.Builder, transportsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, transportsOffset, 0);
}

static createTransportsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTransportsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitTransportUpdate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitTransportUpdate(builder:flatbuffers.Builder, transportsOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitTransportUpdate.startUnitTransportUpdate(builder);
  UnitTransportUpdate.addTransports(builder, transportsOffset);
  return UnitTransportUpdate.endUnitTransportUpdate(builder);
}

unpack(): UnitTransportUpdateT {
  return new UnitTransportUpdateT(
    this.bb!.createObjList<UnitTransportInfo, UnitTransportInfoT>(this.transports.bind(this), this.transportsLength())
  );
}


unpackTo(_o: UnitTransportUpdateT): void {
  _o.transports = this.bb!.createObjList<UnitTransportInfo, UnitTransportInfoT>(this.transports.bind(this), this.transportsLength());
}
}

export class UnitTransportUpdateT implements flatbuffers.IGeneratedObject {
constructor(
  public transports: (UnitTransportInfoT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const transports = UnitTransportUpdate.createTransportsVector(builder, builder.createObjectOffsetList(this.transports));

  return UnitTransportUpdate.createUnitTransportUpdate(builder,
    transports
  );
}
}
