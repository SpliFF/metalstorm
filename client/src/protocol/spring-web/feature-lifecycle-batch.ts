// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { FeatureSpawn, FeatureSpawnT } from '../spring-web/feature-spawn.js';


/**
 * Per-tick batch of feature lifecycle events. Empty on quiet ticks;
 * populated when units die (wreck spawn), buildings collapse, debris
 * drops, or gadget code calls `Spring.CreateFeature` / `DestroyFeature`.
 */
export class FeatureLifecycleBatch implements flatbuffers.IUnpackableObject<FeatureLifecycleBatchT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FeatureLifecycleBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFeatureLifecycleBatch(bb:flatbuffers.ByteBuffer, obj?:FeatureLifecycleBatch):FeatureLifecycleBatch {
  return (obj || new FeatureLifecycleBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFeatureLifecycleBatch(bb:flatbuffers.ByteBuffer, obj?:FeatureLifecycleBatch):FeatureLifecycleBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FeatureLifecycleBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

spawns(index: number, obj?:FeatureSpawn):FeatureSpawn|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new FeatureSpawn()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

spawnsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Feature IDs removed this tick (reclaimed, destroyed, despawned).
 * Parallel to `spawns` order — the client just plays back both.
 */
removed(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

removedLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

removedArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startFeatureLifecycleBatch(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addSpawns(builder:flatbuffers.Builder, spawnsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, spawnsOffset, 0);
}

static createSpawnsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSpawnsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addRemoved(builder:flatbuffers.Builder, removedOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, removedOffset, 0);
}

static createRemovedVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createRemovedVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createRemovedVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startRemovedVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endFeatureLifecycleBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFeatureLifecycleBatch(builder:flatbuffers.Builder, spawnsOffset:flatbuffers.Offset, removedOffset:flatbuffers.Offset):flatbuffers.Offset {
  FeatureLifecycleBatch.startFeatureLifecycleBatch(builder);
  FeatureLifecycleBatch.addSpawns(builder, spawnsOffset);
  FeatureLifecycleBatch.addRemoved(builder, removedOffset);
  return FeatureLifecycleBatch.endFeatureLifecycleBatch(builder);
}

unpack(): FeatureLifecycleBatchT {
  return new FeatureLifecycleBatchT(
    this.bb!.createObjList<FeatureSpawn, FeatureSpawnT>(this.spawns.bind(this), this.spawnsLength()),
    this.bb!.createScalarList<number>(this.removed.bind(this), this.removedLength())
  );
}


unpackTo(_o: FeatureLifecycleBatchT): void {
  _o.spawns = this.bb!.createObjList<FeatureSpawn, FeatureSpawnT>(this.spawns.bind(this), this.spawnsLength());
  _o.removed = this.bb!.createScalarList<number>(this.removed.bind(this), this.removedLength());
}
}

export class FeatureLifecycleBatchT implements flatbuffers.IGeneratedObject {
constructor(
  public spawns: (FeatureSpawnT)[] = [],
  public removed: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const spawns = FeatureLifecycleBatch.createSpawnsVector(builder, builder.createObjectOffsetList(this.spawns));
  const removed = FeatureLifecycleBatch.createRemovedVector(builder, this.removed);

  return FeatureLifecycleBatch.createFeatureLifecycleBatch(builder,
    spawns,
    removed
  );
}
}
