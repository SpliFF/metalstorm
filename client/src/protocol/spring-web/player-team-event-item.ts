// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One player- or team-status change, mirroring the matching Recoil
 * eventHandler callins. `kind` selects which LuaUI callin the widget worker
 * fires; `id` is a playerID (kinds 0-2) or teamID (kind 3); `reason` carries
 * the PlayerRemoved reason (0 otherwise). Streamed as a per-tick batch so a
 * team death (which demotes every member player to spectator) coalesces its
 * PlayerChanged + TeamDied events into one message.
 */
export class PlayerTeamEventItem implements flatbuffers.IUnpackableObject<PlayerTeamEventItemT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PlayerTeamEventItem {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

/**
 * 0 = PlayerChanged, 1 = PlayerAdded, 2 = PlayerRemoved, 3 = TeamDied.
 */
kind():number {
  return this.bb!.readUint8(this.bb_pos);
}

/**
 * PlayerRemoved reason (0=quit, 1=kicked, 2=timeout, 3=detach — parked,
 * may reconnect); 0 for other kinds.
 */
reason():number {
  return this.bb!.readUint8(this.bb_pos + 1);
}

/**
 * playerID for kinds 0-2, teamID for kind 3.
 */
id():number {
  return this.bb!.readUint32(this.bb_pos + 4);
}

static sizeOf():number {
  return 8;
}

static createPlayerTeamEventItem(builder:flatbuffers.Builder, kind: number, reason: number, id: number):flatbuffers.Offset {
  builder.prep(4, 8);
  builder.writeInt32(id);
  builder.pad(2);
  builder.writeInt8(reason);
  builder.writeInt8(kind);
  return builder.offset();
}


unpack(): PlayerTeamEventItemT {
  return new PlayerTeamEventItemT(
    this.kind(),
    this.reason(),
    this.id()
  );
}


unpackTo(_o: PlayerTeamEventItemT): void {
  _o.kind = this.kind();
  _o.reason = this.reason();
  _o.id = this.id();
}
}

export class PlayerTeamEventItemT implements flatbuffers.IGeneratedObject {
constructor(
  public kind: number = 0,
  public reason: number = 0,
  public id: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return PlayerTeamEventItem.createPlayerTeamEventItem(builder,
    this.kind,
    this.reason,
    this.id
  );
}
}
