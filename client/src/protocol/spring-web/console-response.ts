// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Response to a ConsoleCommand.
 */
export class ConsoleResponse implements flatbuffers.IUnpackableObject<ConsoleResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ConsoleResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsConsoleResponse(bb:flatbuffers.ByteBuffer, obj?:ConsoleResponse):ConsoleResponse {
  return (obj || new ConsoleResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsConsoleResponse(bb:flatbuffers.ByteBuffer, obj?:ConsoleResponse):ConsoleResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ConsoleResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

scope():string|null
scope(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
scope(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

success():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

output():string|null
output(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
output(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

level():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startConsoleResponse(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addRequestId(builder:flatbuffers.Builder, requestId:number) {
  builder.addFieldInt32(0, requestId, 0);
}

static addScope(builder:flatbuffers.Builder, scopeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, scopeOffset, 0);
}

static addSuccess(builder:flatbuffers.Builder, success:boolean) {
  builder.addFieldInt8(2, +success, +false);
}

static addOutput(builder:flatbuffers.Builder, outputOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, outputOffset, 0);
}

static addLevel(builder:flatbuffers.Builder, level:number) {
  builder.addFieldInt8(4, level, 0);
}

static endConsoleResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createConsoleResponse(builder:flatbuffers.Builder, requestId:number, scopeOffset:flatbuffers.Offset, success:boolean, outputOffset:flatbuffers.Offset, level:number):flatbuffers.Offset {
  ConsoleResponse.startConsoleResponse(builder);
  ConsoleResponse.addRequestId(builder, requestId);
  ConsoleResponse.addScope(builder, scopeOffset);
  ConsoleResponse.addSuccess(builder, success);
  ConsoleResponse.addOutput(builder, outputOffset);
  ConsoleResponse.addLevel(builder, level);
  return ConsoleResponse.endConsoleResponse(builder);
}

unpack(): ConsoleResponseT {
  return new ConsoleResponseT(
    this.requestId(),
    this.scope(),
    this.success(),
    this.output(),
    this.level()
  );
}


unpackTo(_o: ConsoleResponseT): void {
  _o.requestId = this.requestId();
  _o.scope = this.scope();
  _o.success = this.success();
  _o.output = this.output();
  _o.level = this.level();
}
}

export class ConsoleResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public requestId: number = 0,
  public scope: string|Uint8Array|null = null,
  public success: boolean = false,
  public output: string|Uint8Array|null = null,
  public level: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const scope = (this.scope !== null ? builder.createString(this.scope!) : 0);
  const output = (this.output !== null ? builder.createString(this.output!) : 0);

  return ConsoleResponse.createConsoleResponse(builder,
    this.requestId,
    scope,
    this.success,
    output,
    this.level
  );
}
}
