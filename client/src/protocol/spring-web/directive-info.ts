// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { DirectiveType } from '../spring-web/directive-type.js';
import { OrderShape } from '../spring-web/order-shape.js';
import { StandingOrderConditions, StandingOrderConditionsT } from '../spring-web/standing-order-conditions.js';


/**
 * One macro directive, incl. fulfillment (macro-orders §4.3 demand model):
 * `assigned_strength / requested_strength` reports understrength/fulfilled.
 */
export class DirectiveInfo implements flatbuffers.IUnpackableObject<DirectiveInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DirectiveInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDirectiveInfo(bb:flatbuffers.ByteBuffer, obj?:DirectiveInfo):DirectiveInfo {
  return (obj || new DirectiveInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDirectiveInfo(bb:flatbuffers.ByteBuffer, obj?:DirectiveInfo):DirectiveInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DirectiveInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

directiveId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

ownerTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

groupId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

type():DirectiveType {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : DirectiveType.DefendArea;
}

priority():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

shape():OrderShape {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : OrderShape.Point;
}

params(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

paramsArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

conditions(obj?:StandingOrderConditions):StandingOrderConditions|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? (obj || new StandingOrderConditions()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

requestedStrength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

assignedStrength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

assignedSquadCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

phasesJson():string|null
phasesJson(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
phasesJson(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

active():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

expiresAtFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

createdAtFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startDirectiveInfo(builder:flatbuffers.Builder) {
  builder.startObject(15);
}

static addDirectiveId(builder:flatbuffers.Builder, directiveId:number) {
  builder.addFieldInt32(0, directiveId, 0);
}

static addOwnerTeam(builder:flatbuffers.Builder, ownerTeam:number) {
  builder.addFieldInt8(1, ownerTeam, 0);
}

static addGroupId(builder:flatbuffers.Builder, groupId:number) {
  builder.addFieldInt32(2, groupId, 0);
}

static addType(builder:flatbuffers.Builder, type:DirectiveType) {
  builder.addFieldInt8(3, type, DirectiveType.DefendArea);
}

static addPriority(builder:flatbuffers.Builder, priority:number) {
  builder.addFieldInt8(4, priority, 0);
}

static addShape(builder:flatbuffers.Builder, shape:OrderShape) {
  builder.addFieldInt8(5, shape, OrderShape.Point);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addConditions(builder:flatbuffers.Builder, conditionsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, conditionsOffset, 0);
}

static addRequestedStrength(builder:flatbuffers.Builder, requestedStrength:number) {
  builder.addFieldInt32(8, requestedStrength, 0);
}

static addAssignedStrength(builder:flatbuffers.Builder, assignedStrength:number) {
  builder.addFieldInt32(9, assignedStrength, 0);
}

static addAssignedSquadCount(builder:flatbuffers.Builder, assignedSquadCount:number) {
  builder.addFieldInt16(10, assignedSquadCount, 0);
}

static addPhasesJson(builder:flatbuffers.Builder, phasesJsonOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, phasesJsonOffset, 0);
}

static addActive(builder:flatbuffers.Builder, active:boolean) {
  builder.addFieldInt8(12, +active, +true);
}

static addExpiresAtFrame(builder:flatbuffers.Builder, expiresAtFrame:number) {
  builder.addFieldInt32(13, expiresAtFrame, 0);
}

static addCreatedAtFrame(builder:flatbuffers.Builder, createdAtFrame:number) {
  builder.addFieldInt32(14, createdAtFrame, 0);
}

static endDirectiveInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): DirectiveInfoT {
  return new DirectiveInfoT(
    this.directiveId(),
    this.ownerTeam(),
    this.groupId(),
    this.type(),
    this.priority(),
    this.shape(),
    this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength()),
    (this.conditions() !== null ? this.conditions()!.unpack() : null),
    this.requestedStrength(),
    this.assignedStrength(),
    this.assignedSquadCount(),
    this.phasesJson(),
    this.active(),
    this.expiresAtFrame(),
    this.createdAtFrame()
  );
}


unpackTo(_o: DirectiveInfoT): void {
  _o.directiveId = this.directiveId();
  _o.ownerTeam = this.ownerTeam();
  _o.groupId = this.groupId();
  _o.type = this.type();
  _o.priority = this.priority();
  _o.shape = this.shape();
  _o.params = this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength());
  _o.conditions = (this.conditions() !== null ? this.conditions()!.unpack() : null);
  _o.requestedStrength = this.requestedStrength();
  _o.assignedStrength = this.assignedStrength();
  _o.assignedSquadCount = this.assignedSquadCount();
  _o.phasesJson = this.phasesJson();
  _o.active = this.active();
  _o.expiresAtFrame = this.expiresAtFrame();
  _o.createdAtFrame = this.createdAtFrame();
}
}

export class DirectiveInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public directiveId: number = 0,
  public ownerTeam: number = 0,
  public groupId: number = 0,
  public type: DirectiveType = DirectiveType.DefendArea,
  public priority: number = 0,
  public shape: OrderShape = OrderShape.Point,
  public params: (number)[] = [],
  public conditions: StandingOrderConditionsT|null = null,
  public requestedStrength: number = 0,
  public assignedStrength: number = 0,
  public assignedSquadCount: number = 0,
  public phasesJson: string|Uint8Array|null = null,
  public active: boolean = true,
  public expiresAtFrame: number = 0,
  public createdAtFrame: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const params = DirectiveInfo.createParamsVector(builder, this.params);
  const conditions = (this.conditions !== null ? this.conditions!.pack(builder) : 0);
  const phasesJson = (this.phasesJson !== null ? builder.createString(this.phasesJson!) : 0);

  DirectiveInfo.startDirectiveInfo(builder);
  DirectiveInfo.addDirectiveId(builder, this.directiveId);
  DirectiveInfo.addOwnerTeam(builder, this.ownerTeam);
  DirectiveInfo.addGroupId(builder, this.groupId);
  DirectiveInfo.addType(builder, this.type);
  DirectiveInfo.addPriority(builder, this.priority);
  DirectiveInfo.addShape(builder, this.shape);
  DirectiveInfo.addParams(builder, params);
  DirectiveInfo.addConditions(builder, conditions);
  DirectiveInfo.addRequestedStrength(builder, this.requestedStrength);
  DirectiveInfo.addAssignedStrength(builder, this.assignedStrength);
  DirectiveInfo.addAssignedSquadCount(builder, this.assignedSquadCount);
  DirectiveInfo.addPhasesJson(builder, phasesJson);
  DirectiveInfo.addActive(builder, this.active);
  DirectiveInfo.addExpiresAtFrame(builder, this.expiresAtFrame);
  DirectiveInfo.addCreatedAtFrame(builder, this.createdAtFrame);

  return DirectiveInfo.endDirectiveInfo(builder);
}
}
