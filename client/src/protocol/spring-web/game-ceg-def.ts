// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameCegSpawn, GameCegSpawnT } from '../spring-web/game-ceg-spawn.js';


/**
 * Definition of one CEG (Custom Explosion Generator) — referenced by
 * `cegTag` / `explosionGenerator` strings on weapon defs. Authored as
 * Lua tables under `gamedata/explosions.lua` (which itself loads
 * `effects/*.lua` in ZK / `gamedata/particles/*.lua` in classic Spring).
 * The server parses all CEGs reachable from any weapon def (transitively
 * via `CExpGenSpawner.expgen` references) and ships them as a single
 * table at game start.
 */
export class GameCegDef implements flatbuffers.IUnpackableObject<GameCegDefT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameCegDef {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameCegDef(bb:flatbuffers.ByteBuffer, obj?:GameCegDef):GameCegDef {
  return (obj || new GameCegDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameCegDef(bb:flatbuffers.ByteBuffer, obj?:GameCegDef):GameCegDef {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameCegDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Bare tag (no `custom:` prefix). Lookup is case-insensitive on
 * the client; the server lowercases on serialisation.
 */
tag():string|null
tag(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
tag(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Sub-emitters. Each fires `count` particles at the spawn point
 * when its visibility flags match the impact context.
 */
spawns(index: number, obj?:GameCegSpawn):GameCegSpawn|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new GameCegSpawn()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

spawnsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * True if the original CEG had `useDefaultExplosions = true`
 * (Spring fires its built-in standard explosion in addition).
 */
useDefaultExplosions():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startGameCegDef(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addTag(builder:flatbuffers.Builder, tagOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, tagOffset, 0);
}

static addSpawns(builder:flatbuffers.Builder, spawnsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, spawnsOffset, 0);
}

static createSpawnsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startSpawnsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addUseDefaultExplosions(builder:flatbuffers.Builder, useDefaultExplosions:boolean) {
  builder.addFieldInt8(2, +useDefaultExplosions, +false);
}

static endGameCegDef(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameCegDef(builder:flatbuffers.Builder, tagOffset:flatbuffers.Offset, spawnsOffset:flatbuffers.Offset, useDefaultExplosions:boolean):flatbuffers.Offset {
  GameCegDef.startGameCegDef(builder);
  GameCegDef.addTag(builder, tagOffset);
  GameCegDef.addSpawns(builder, spawnsOffset);
  GameCegDef.addUseDefaultExplosions(builder, useDefaultExplosions);
  return GameCegDef.endGameCegDef(builder);
}

unpack(): GameCegDefT {
  return new GameCegDefT(
    this.tag(),
    this.bb!.createObjList<GameCegSpawn, GameCegSpawnT>(this.spawns.bind(this), this.spawnsLength()),
    this.useDefaultExplosions()
  );
}


unpackTo(_o: GameCegDefT): void {
  _o.tag = this.tag();
  _o.spawns = this.bb!.createObjList<GameCegSpawn, GameCegSpawnT>(this.spawns.bind(this), this.spawnsLength());
  _o.useDefaultExplosions = this.useDefaultExplosions();
}
}

export class GameCegDefT implements flatbuffers.IGeneratedObject {
constructor(
  public tag: string|Uint8Array|null = null,
  public spawns: (GameCegSpawnT)[] = [],
  public useDefaultExplosions: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const tag = (this.tag !== null ? builder.createString(this.tag!) : 0);
  const spawns = GameCegDef.createSpawnsVector(builder, builder.createObjectOffsetList(this.spawns));

  return GameCegDef.createGameCegDef(builder,
    tag,
    spawns,
    this.useDefaultExplosions
  );
}
}
