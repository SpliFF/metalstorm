// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One key/value pair inside a CEG spawn's `properties` table.
 * Keys are the lowercased Lua field names (e.g. `colormap`, `gravity`,
 * `numparticles`, `texture`). Values are the raw Lua-source strings
 * (numbers stringified, lists serialised as comma-separated). The
 * client parser interprets each property based on the spawn class.
 */
export class CegProperty implements flatbuffers.IUnpackableObject<CegPropertyT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CegProperty {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCegProperty(bb:flatbuffers.ByteBuffer, obj?:CegProperty):CegProperty {
  return (obj || new CegProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCegProperty(bb:flatbuffers.ByteBuffer, obj?:CegProperty):CegProperty {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CegProperty()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

key():string|null
key(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
key(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

value():string|null
value(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
value(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startCegProperty(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addKey(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, keyOffset, 0);
}

static addValue(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, valueOffset, 0);
}

static endCegProperty(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createCegProperty(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset, valueOffset:flatbuffers.Offset):flatbuffers.Offset {
  CegProperty.startCegProperty(builder);
  CegProperty.addKey(builder, keyOffset);
  CegProperty.addValue(builder, valueOffset);
  return CegProperty.endCegProperty(builder);
}

unpack(): CegPropertyT {
  return new CegPropertyT(
    this.key(),
    this.value()
  );
}


unpackTo(_o: CegPropertyT): void {
  _o.key = this.key();
  _o.value = this.value();
}
}

export class CegPropertyT implements flatbuffers.IGeneratedObject {
constructor(
  public key: string|Uint8Array|null = null,
  public value: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const key = (this.key !== null ? builder.createString(this.key!) : 0);
  const value = (this.value !== null ? builder.createString(this.value!) : 0);

  return CegProperty.createCegProperty(builder,
    key,
    value
  );
}
}
