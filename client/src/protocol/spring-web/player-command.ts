// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class PlayerCommand implements flatbuffers.IUnpackableObject<PlayerCommandT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PlayerCommand {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPlayerCommand(bb:flatbuffers.ByteBuffer, obj?:PlayerCommand):PlayerCommand {
  return (obj || new PlayerCommand()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPlayerCommand(bb:flatbuffers.ByteBuffer, obj?:PlayerCommand):PlayerCommand {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PlayerCommand()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

sequence():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

commandId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : 0;
}

squadIds(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

squadIdsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

squadIdsArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

params(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

paramsArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

options():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

timeoutFrames():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startPlayerCommand(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addSequence(builder:flatbuffers.Builder, sequence:number) {
  builder.addFieldInt32(0, sequence, 0);
}

static addCommandId(builder:flatbuffers.Builder, commandId:number) {
  builder.addFieldInt16(1, commandId, 0);
}

static addSquadIds(builder:flatbuffers.Builder, squadIdsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, squadIdsOffset, 0);
}

static createSquadIdsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createSquadIdsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createSquadIdsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startSquadIdsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOptions(builder:flatbuffers.Builder, options:number) {
  builder.addFieldInt8(4, options, 0);
}

static addTimeoutFrames(builder:flatbuffers.Builder, timeoutFrames:number) {
  builder.addFieldInt32(5, timeoutFrames, 0);
}

static endPlayerCommand(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPlayerCommand(builder:flatbuffers.Builder, sequence:number, commandId:number, squadIdsOffset:flatbuffers.Offset, paramsOffset:flatbuffers.Offset, options:number, timeoutFrames:number):flatbuffers.Offset {
  PlayerCommand.startPlayerCommand(builder);
  PlayerCommand.addSequence(builder, sequence);
  PlayerCommand.addCommandId(builder, commandId);
  PlayerCommand.addSquadIds(builder, squadIdsOffset);
  PlayerCommand.addParams(builder, paramsOffset);
  PlayerCommand.addOptions(builder, options);
  PlayerCommand.addTimeoutFrames(builder, timeoutFrames);
  return PlayerCommand.endPlayerCommand(builder);
}

unpack(): PlayerCommandT {
  return new PlayerCommandT(
    this.sequence(),
    this.commandId(),
    this.bb!.createScalarList<number>(this.squadIds.bind(this), this.squadIdsLength()),
    this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength()),
    this.options(),
    this.timeoutFrames()
  );
}


unpackTo(_o: PlayerCommandT): void {
  _o.sequence = this.sequence();
  _o.commandId = this.commandId();
  _o.squadIds = this.bb!.createScalarList<number>(this.squadIds.bind(this), this.squadIdsLength());
  _o.params = this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength());
  _o.options = this.options();
  _o.timeoutFrames = this.timeoutFrames();
}
}

export class PlayerCommandT implements flatbuffers.IGeneratedObject {
constructor(
  public sequence: number = 0,
  public commandId: number = 0,
  public squadIds: (number)[] = [],
  public params: (number)[] = [],
  public options: number = 0,
  public timeoutFrames: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const squadIds = PlayerCommand.createSquadIdsVector(builder, this.squadIds);
  const params = PlayerCommand.createParamsVector(builder, this.params);

  return PlayerCommand.createPlayerCommand(builder,
    this.sequence,
    this.commandId,
    squadIds,
    params,
    this.options,
    this.timeoutFrames
  );
}
}
