// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One key/value pair from a UnitDef's or WeaponDef's customParams.
 * Spring games use customParams as the extension point for game-
 * specific data: ZK reads dozens of keys (level, commtype, dynamic_
 * comm, child_of_factory, planetwars_structure, thrower_gather,
 * nuke_coverage, etc.) at widget file-scope. Streaming them as a
 * list of pairs keeps the message format simple and lets the client
 * reconstruct a Lua-shaped customParams table directly.
 */
export class CustomParam implements flatbuffers.IUnpackableObject<CustomParamT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CustomParam {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCustomParam(bb:flatbuffers.ByteBuffer, obj?:CustomParam):CustomParam {
  return (obj || new CustomParam()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCustomParam(bb:flatbuffers.ByteBuffer, obj?:CustomParam):CustomParam {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CustomParam()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

key():string|null
key(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
key(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

value():string|null
value(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
value(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startCustomParam(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addKey(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, keyOffset, 0);
}

static addValue(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, valueOffset, 0);
}

static endCustomParam(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createCustomParam(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset, valueOffset:flatbuffers.Offset):flatbuffers.Offset {
  CustomParam.startCustomParam(builder);
  CustomParam.addKey(builder, keyOffset);
  CustomParam.addValue(builder, valueOffset);
  return CustomParam.endCustomParam(builder);
}

unpack(): CustomParamT {
  return new CustomParamT(
    this.key(),
    this.value()
  );
}


unpackTo(_o: CustomParamT): void {
  _o.key = this.key();
  _o.value = this.value();
}
}

export class CustomParamT implements flatbuffers.IGeneratedObject {
constructor(
  public key: string|Uint8Array|null = null,
  public value: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const key = (this.key !== null ? builder.createString(this.key!) : 0);
  const value = (this.value !== null ? builder.createString(this.value!) : 0);

  return CustomParam.createCustomParam(builder,
    key,
    value
  );
}
}
