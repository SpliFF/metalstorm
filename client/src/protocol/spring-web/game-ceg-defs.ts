// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameCegDef, GameCegDefT } from '../spring-web/game-ceg-def.js';


/**
 * Sent by the game server alongside GameUnitDefs / GameWeaponDefs.
 * Contains every CEG referenced by the game's weapon defs so the
 * client can spawn the right particle effects on Fired / Impact
 * events without further round-trips.
 */
export class GameCegDefs implements flatbuffers.IUnpackableObject<GameCegDefsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameCegDefs {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameCegDefs(bb:flatbuffers.ByteBuffer, obj?:GameCegDefs):GameCegDefs {
  return (obj || new GameCegDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameCegDefs(bb:flatbuffers.ByteBuffer, obj?:GameCegDefs):GameCegDefs {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameCegDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

defs(index: number, obj?:GameCegDef):GameCegDef|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GameCegDef()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

defsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startGameCegDefs(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, defsOffset, 0);
}

static createDefsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDefsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGameCegDefs(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameCegDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset):flatbuffers.Offset {
  GameCegDefs.startGameCegDefs(builder);
  GameCegDefs.addDefs(builder, defsOffset);
  return GameCegDefs.endGameCegDefs(builder);
}

unpack(): GameCegDefsT {
  return new GameCegDefsT(
    this.bb!.createObjList<GameCegDef, GameCegDefT>(this.defs.bind(this), this.defsLength())
  );
}


unpackTo(_o: GameCegDefsT): void {
  _o.defs = this.bb!.createObjList<GameCegDef, GameCegDefT>(this.defs.bind(this), this.defsLength());
}
}

export class GameCegDefsT implements flatbuffers.IGeneratedObject {
constructor(
  public defs: (GameCegDefT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const defs = GameCegDefs.createDefsVector(builder, builder.createObjectOffsetList(this.defs));

  return GameCegDefs.createGameCegDefs(builder,
    defs
  );
}
}
