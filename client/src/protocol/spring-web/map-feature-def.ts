// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Definition of a feature type — corresponds 1:1 with an entry in
 * `MapData.feature_types` (same indices). Tells the client how to render
 * and interact with feature instances of this type. Parsed by the server
 * from the map's `features/*.lua` and converted into web-ready assets:
 *
 *   - `model_url` is an HTTP URL to a glTF 2.0 binary (`.glb`)
 *     converted from the original `.s3o` via the modelimporter tool.
 *   - `texture_url` is an HTTP URL to a PNG converted from the
 *     original `.tga`/`.dds` via ImageMagick.
 *
 * Both URLs may be empty strings if the def has no model (e.g. an
 * invisible decal feature) or its assets failed to convert; in that
 * case the client falls back to a placeholder.
 */
export class MapFeatureDef implements flatbuffers.IUnpackableObject<MapFeatureDefT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MapFeatureDef {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMapFeatureDef(bb:flatbuffers.ByteBuffer, obj?:MapFeatureDef):MapFeatureDef {
  return (obj || new MapFeatureDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMapFeatureDef(bb:flatbuffers.ByteBuffer, obj?:MapFeatureDef):MapFeatureDef {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MapFeatureDef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

modelUrl():string|null
modelUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
modelUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

textureUrl():string|null
textureUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
textureUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

footprintX():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

footprintZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

height():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

radius():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

blocking():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

reclaimable():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

metal():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

energy():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

damage():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startMapFeatureDef(builder:flatbuffers.Builder) {
  builder.startObject(12);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, nameOffset, 0);
}

static addModelUrl(builder:flatbuffers.Builder, modelUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, modelUrlOffset, 0);
}

static addTextureUrl(builder:flatbuffers.Builder, textureUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, textureUrlOffset, 0);
}

static addFootprintX(builder:flatbuffers.Builder, footprintX:number) {
  builder.addFieldInt16(3, footprintX, 0);
}

static addFootprintZ(builder:flatbuffers.Builder, footprintZ:number) {
  builder.addFieldInt16(4, footprintZ, 0);
}

static addHeight(builder:flatbuffers.Builder, height:number) {
  builder.addFieldFloat32(5, height, 0.0);
}

static addRadius(builder:flatbuffers.Builder, radius:number) {
  builder.addFieldFloat32(6, radius, 0.0);
}

static addBlocking(builder:flatbuffers.Builder, blocking:boolean) {
  builder.addFieldInt8(7, +blocking, +false);
}

static addReclaimable(builder:flatbuffers.Builder, reclaimable:boolean) {
  builder.addFieldInt8(8, +reclaimable, +false);
}

static addMetal(builder:flatbuffers.Builder, metal:number) {
  builder.addFieldInt32(9, metal, 0);
}

static addEnergy(builder:flatbuffers.Builder, energy:number) {
  builder.addFieldInt32(10, energy, 0);
}

static addDamage(builder:flatbuffers.Builder, damage:number) {
  builder.addFieldInt32(11, damage, 0);
}

static endMapFeatureDef(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMapFeatureDef(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset, modelUrlOffset:flatbuffers.Offset, textureUrlOffset:flatbuffers.Offset, footprintX:number, footprintZ:number, height:number, radius:number, blocking:boolean, reclaimable:boolean, metal:number, energy:number, damage:number):flatbuffers.Offset {
  MapFeatureDef.startMapFeatureDef(builder);
  MapFeatureDef.addName(builder, nameOffset);
  MapFeatureDef.addModelUrl(builder, modelUrlOffset);
  MapFeatureDef.addTextureUrl(builder, textureUrlOffset);
  MapFeatureDef.addFootprintX(builder, footprintX);
  MapFeatureDef.addFootprintZ(builder, footprintZ);
  MapFeatureDef.addHeight(builder, height);
  MapFeatureDef.addRadius(builder, radius);
  MapFeatureDef.addBlocking(builder, blocking);
  MapFeatureDef.addReclaimable(builder, reclaimable);
  MapFeatureDef.addMetal(builder, metal);
  MapFeatureDef.addEnergy(builder, energy);
  MapFeatureDef.addDamage(builder, damage);
  return MapFeatureDef.endMapFeatureDef(builder);
}

unpack(): MapFeatureDefT {
  return new MapFeatureDefT(
    this.name(),
    this.modelUrl(),
    this.textureUrl(),
    this.footprintX(),
    this.footprintZ(),
    this.height(),
    this.radius(),
    this.blocking(),
    this.reclaimable(),
    this.metal(),
    this.energy(),
    this.damage()
  );
}


unpackTo(_o: MapFeatureDefT): void {
  _o.name = this.name();
  _o.modelUrl = this.modelUrl();
  _o.textureUrl = this.textureUrl();
  _o.footprintX = this.footprintX();
  _o.footprintZ = this.footprintZ();
  _o.height = this.height();
  _o.radius = this.radius();
  _o.blocking = this.blocking();
  _o.reclaimable = this.reclaimable();
  _o.metal = this.metal();
  _o.energy = this.energy();
  _o.damage = this.damage();
}
}

export class MapFeatureDefT implements flatbuffers.IGeneratedObject {
constructor(
  public name: string|Uint8Array|null = null,
  public modelUrl: string|Uint8Array|null = null,
  public textureUrl: string|Uint8Array|null = null,
  public footprintX: number = 0,
  public footprintZ: number = 0,
  public height: number = 0.0,
  public radius: number = 0.0,
  public blocking: boolean = false,
  public reclaimable: boolean = false,
  public metal: number = 0,
  public energy: number = 0,
  public damage: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const name = (this.name !== null ? builder.createString(this.name!) : 0);
  const modelUrl = (this.modelUrl !== null ? builder.createString(this.modelUrl!) : 0);
  const textureUrl = (this.textureUrl !== null ? builder.createString(this.textureUrl!) : 0);

  return MapFeatureDef.createMapFeatureDef(builder,
    name,
    modelUrl,
    textureUrl,
    this.footprintX,
    this.footprintZ,
    this.height,
    this.radius,
    this.blocking,
    this.reclaimable,
    this.metal,
    this.energy,
    this.damage
  );
}
}
