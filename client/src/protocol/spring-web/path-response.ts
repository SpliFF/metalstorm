// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * Server → Client: result of a `PathRequest`. The `request_id` matches
 * the one the client supplied. `waypoints` is the full path from
 * `start` to (a point within `goal_radius` of) `end`; empty if no
 * path was found within the requested constraints. `length` is the
 * total path length in elmos — useful for build-time estimates and
 * "is target reachable" checks (compare against straight-line
 * distance + builder range).
 *
 * Send-exactly-once semantics: every PathRequest produces exactly one
 * PathResponse, even on failure (with empty `waypoints`). After
 * PathResponse, the corresponding server-side path is released — the
 * client must request a fresh path for any future query.
 */
export class PathResponse implements flatbuffers.IUnpackableObject<PathResponseT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PathResponse {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPathResponse(bb:flatbuffers.ByteBuffer, obj?:PathResponse):PathResponse {
  return (obj || new PathResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPathResponse(bb:flatbuffers.ByteBuffer, obj?:PathResponse):PathResponse {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PathResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

waypoints(index: number, obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Vec3()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 12, this.bb!) : null;
}

waypointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

length():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

static startPathResponse(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRequestId(builder:flatbuffers.Builder, requestId:number) {
  builder.addFieldInt32(0, requestId, 0);
}

static addWaypoints(builder:flatbuffers.Builder, waypointsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, waypointsOffset, 0);
}

static startWaypointsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(12, numElems, 4);
}

static addLength(builder:flatbuffers.Builder, length:number) {
  builder.addFieldFloat32(2, length, 0.0);
}

static endPathResponse(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPathResponse(builder:flatbuffers.Builder, requestId:number, waypointsOffset:flatbuffers.Offset, length:number):flatbuffers.Offset {
  PathResponse.startPathResponse(builder);
  PathResponse.addRequestId(builder, requestId);
  PathResponse.addWaypoints(builder, waypointsOffset);
  PathResponse.addLength(builder, length);
  return PathResponse.endPathResponse(builder);
}

unpack(): PathResponseT {
  return new PathResponseT(
    this.requestId(),
    this.bb!.createObjList<Vec3, Vec3T>(this.waypoints.bind(this), this.waypointsLength()),
    this.length()
  );
}


unpackTo(_o: PathResponseT): void {
  _o.requestId = this.requestId();
  _o.waypoints = this.bb!.createObjList<Vec3, Vec3T>(this.waypoints.bind(this), this.waypointsLength());
  _o.length = this.length();
}
}

export class PathResponseT implements flatbuffers.IGeneratedObject {
constructor(
  public requestId: number = 0,
  public waypoints: (Vec3T)[] = [],
  public length: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const waypoints = builder.createStructOffsetList(this.waypoints, PathResponse.startWaypointsVector);

  return PathResponse.createPathResponse(builder,
    this.requestId,
    waypoints,
    this.length
  );
}
}
