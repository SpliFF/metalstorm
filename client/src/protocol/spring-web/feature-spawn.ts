// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One feature spawned on the server this tick. Covers wrecks created
 * by `CFeatureHandler::CreateWreckage` on unit death, debris dropped
 * by explosions, Lua `Spring.CreateFeature` calls, and any other
 * runtime-spawned `CFeature`. Map-placed features arrive via
 * `MapData.features` at game start, not here — only dynamic spawns.
 *
 * `team` is the owning team (controls reclaim rights); -1 means
 * gaia/neutral. `ally_team` lets the server filter visibility client-
 * side (we still broadcast unfiltered for simplicity since wrecks are
 * usually visible to everyone, but the field is here so per-team
 * filtering can be added without a schema bump).
 */
export class FeatureSpawn implements flatbuffers.IUnpackableObject<FeatureSpawnT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):FeatureSpawn {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsFeatureSpawn(bb:flatbuffers.ByteBuffer, obj?:FeatureSpawn):FeatureSpawn {
  return (obj || new FeatureSpawn()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsFeatureSpawn(bb:flatbuffers.ByteBuffer, obj?:FeatureSpawn):FeatureSpawn {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new FeatureSpawn()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

featureId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

defId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

x():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

y():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

z():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Spring heading: 16-bit fixed-point angle (0 = +Z, 16384 = +X).
 */
heading():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : 0;
}

/**
 * Build facing 0..3 (south/east/north/west), copied from the parent
 * unit's facing when the wreck inherits it.
 */
buildFacing():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

team():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : -1;
}

allyTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : -1;
}

static startFeatureSpawn(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addFeatureId(builder:flatbuffers.Builder, featureId:number) {
  builder.addFieldInt32(0, featureId, 0);
}

static addDefId(builder:flatbuffers.Builder, defId:number) {
  builder.addFieldInt32(1, defId, 0);
}

static addX(builder:flatbuffers.Builder, x:number) {
  builder.addFieldFloat32(2, x, 0.0);
}

static addY(builder:flatbuffers.Builder, y:number) {
  builder.addFieldFloat32(3, y, 0.0);
}

static addZ(builder:flatbuffers.Builder, z:number) {
  builder.addFieldFloat32(4, z, 0.0);
}

static addHeading(builder:flatbuffers.Builder, heading:number) {
  builder.addFieldInt16(5, heading, 0);
}

static addBuildFacing(builder:flatbuffers.Builder, buildFacing:number) {
  builder.addFieldInt8(6, buildFacing, 0);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(7, team, -1);
}

static addAllyTeam(builder:flatbuffers.Builder, allyTeam:number) {
  builder.addFieldInt8(8, allyTeam, -1);
}

static endFeatureSpawn(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createFeatureSpawn(builder:flatbuffers.Builder, featureId:number, defId:number, x:number, y:number, z:number, heading:number, buildFacing:number, team:number, allyTeam:number):flatbuffers.Offset {
  FeatureSpawn.startFeatureSpawn(builder);
  FeatureSpawn.addFeatureId(builder, featureId);
  FeatureSpawn.addDefId(builder, defId);
  FeatureSpawn.addX(builder, x);
  FeatureSpawn.addY(builder, y);
  FeatureSpawn.addZ(builder, z);
  FeatureSpawn.addHeading(builder, heading);
  FeatureSpawn.addBuildFacing(builder, buildFacing);
  FeatureSpawn.addTeam(builder, team);
  FeatureSpawn.addAllyTeam(builder, allyTeam);
  return FeatureSpawn.endFeatureSpawn(builder);
}

unpack(): FeatureSpawnT {
  return new FeatureSpawnT(
    this.featureId(),
    this.defId(),
    this.x(),
    this.y(),
    this.z(),
    this.heading(),
    this.buildFacing(),
    this.team(),
    this.allyTeam()
  );
}


unpackTo(_o: FeatureSpawnT): void {
  _o.featureId = this.featureId();
  _o.defId = this.defId();
  _o.x = this.x();
  _o.y = this.y();
  _o.z = this.z();
  _o.heading = this.heading();
  _o.buildFacing = this.buildFacing();
  _o.team = this.team();
  _o.allyTeam = this.allyTeam();
}
}

export class FeatureSpawnT implements flatbuffers.IGeneratedObject {
constructor(
  public featureId: number = 0,
  public defId: number = 0,
  public x: number = 0.0,
  public y: number = 0.0,
  public z: number = 0.0,
  public heading: number = 0,
  public buildFacing: number = 0,
  public team: number = -1,
  public allyTeam: number = -1
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return FeatureSpawn.createFeatureSpawn(builder,
    this.featureId,
    this.defId,
    this.x,
    this.y,
    this.z,
    this.heading,
    this.buildFacing,
    this.team,
    this.allyTeam
  );
}
}
