// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { RoomAIInfo, RoomAIInfoT } from '../spring-web/room-aiinfo.js';


/**
 * Reply to AIListRequest. Lists every AI plugin the server
 * discovered under the current game + engine roots. The client
 * uses this to populate the host's "Add AI" dropdown. The list
 * is stable for the lifetime of the lobby process; clients may
 * cache it and skip re-requesting unless they change game.
 */
export class AIListUpdate implements flatbuffers.IUnpackableObject<AIListUpdateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AIListUpdate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAIListUpdate(bb:flatbuffers.ByteBuffer, obj?:AIListUpdate):AIListUpdate {
  return (obj || new AIListUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAIListUpdate(bb:flatbuffers.ByteBuffer, obj?:AIListUpdate):AIListUpdate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new AIListUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

ais(index: number, obj?:RoomAIInfo):RoomAIInfo|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new RoomAIInfo()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

aisLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startAIListUpdate(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addAis(builder:flatbuffers.Builder, aisOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, aisOffset, 0);
}

static createAisVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startAisVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endAIListUpdate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createAIListUpdate(builder:flatbuffers.Builder, aisOffset:flatbuffers.Offset):flatbuffers.Offset {
  AIListUpdate.startAIListUpdate(builder);
  AIListUpdate.addAis(builder, aisOffset);
  return AIListUpdate.endAIListUpdate(builder);
}

unpack(): AIListUpdateT {
  return new AIListUpdateT(
    this.bb!.createObjList<RoomAIInfo, RoomAIInfoT>(this.ais.bind(this), this.aisLength())
  );
}


unpackTo(_o: AIListUpdateT): void {
  _o.ais = this.bb!.createObjList<RoomAIInfo, RoomAIInfoT>(this.ais.bind(this), this.aisLength());
}
}

export class AIListUpdateT implements flatbuffers.IGeneratedObject {
constructor(
  public ais: (RoomAIInfoT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const ais = AIListUpdate.createAisVector(builder, builder.createObjectOffsetList(this.ais));

  return AIListUpdate.createAIListUpdate(builder,
    ais
  );
}
}
