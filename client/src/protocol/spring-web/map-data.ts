// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MapFeature, MapFeatureT } from '../spring-web/map-feature.js';
import { MapStartPos, MapStartPosT } from '../spring-web/map-start-pos.js';


/**
 * Full map data sent by the game server on connect.
 * Binary data (heightmap, tileindex, typemap, metalmap) embedded directly.
 * Tile texture data served via HTTP separately (too large to embed).
 */
export class MapData implements flatbuffers.IUnpackableObject<MapDataT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MapData {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMapData(bb:flatbuffers.ByteBuffer, obj?:MapData):MapData {
  return (obj || new MapData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMapData(bb:flatbuffers.ByteBuffer, obj?:MapData):MapData {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MapData()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

mapx():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

mapy():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

squareSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

minHeight():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

maxHeight():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

tilesX():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

tilesZ():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

numTiles():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

tileSize():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

startPositions(index: number, obj?:MapStartPos):MapStartPos|null {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? (obj || new MapStartPos()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 8, this.bb!) : null;
}

startPositionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

featureTypes(index: number):string
featureTypes(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
featureTypes(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

featureTypesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

features(index: number, obj?:MapFeature):MapFeature|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? (obj || new MapFeature()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

featuresLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

heightmap(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint16(this.bb!.__vector(this.bb_pos + offset) + index * 2) : 0;
}

heightmapLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

heightmapArray():Uint16Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? new Uint16Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

tileindex(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.readInt32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

tileindexLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

tileindexArray():Int32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? new Int32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

typemap(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

typemapLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

typemapArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

metalmap(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

metalmapLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

metalmapArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

minimapUrl():string|null
minimapUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
minimapUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

tilesUrl():string|null
tilesUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
tilesUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

hasLuaGaia():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startMapData(builder:flatbuffers.Builder) {
  builder.startObject(19);
}

static addMapx(builder:flatbuffers.Builder, mapx:number) {
  builder.addFieldInt16(0, mapx, 0);
}

static addMapy(builder:flatbuffers.Builder, mapy:number) {
  builder.addFieldInt16(1, mapy, 0);
}

static addSquareSize(builder:flatbuffers.Builder, squareSize:number) {
  builder.addFieldInt8(2, squareSize, 0);
}

static addMinHeight(builder:flatbuffers.Builder, minHeight:number) {
  builder.addFieldFloat32(3, minHeight, 0.0);
}

static addMaxHeight(builder:flatbuffers.Builder, maxHeight:number) {
  builder.addFieldFloat32(4, maxHeight, 0.0);
}

static addTilesX(builder:flatbuffers.Builder, tilesX:number) {
  builder.addFieldInt16(5, tilesX, 0);
}

static addTilesZ(builder:flatbuffers.Builder, tilesZ:number) {
  builder.addFieldInt16(6, tilesZ, 0);
}

static addNumTiles(builder:flatbuffers.Builder, numTiles:number) {
  builder.addFieldInt32(7, numTiles, 0);
}

static addTileSize(builder:flatbuffers.Builder, tileSize:number) {
  builder.addFieldInt8(8, tileSize, 0);
}

static addStartPositions(builder:flatbuffers.Builder, startPositionsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(9, startPositionsOffset, 0);
}

static startStartPositionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 4);
}

static addFeatureTypes(builder:flatbuffers.Builder, featureTypesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, featureTypesOffset, 0);
}

static createFeatureTypesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFeatureTypesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addFeatures(builder:flatbuffers.Builder, featuresOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, featuresOffset, 0);
}

static createFeaturesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startFeaturesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addHeightmap(builder:flatbuffers.Builder, heightmapOffset:flatbuffers.Offset) {
  builder.addFieldOffset(12, heightmapOffset, 0);
}

static createHeightmapVector(builder:flatbuffers.Builder, data:number[]|Uint16Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createHeightmapVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createHeightmapVector(builder:flatbuffers.Builder, data:number[]|Uint16Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(2, data.length, 2);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt16(data[i]!);
  }
  return builder.endVector();
}

static startHeightmapVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(2, numElems, 2);
}

static addTileindex(builder:flatbuffers.Builder, tileindexOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, tileindexOffset, 0);
}

static createTileindexVector(builder:flatbuffers.Builder, data:number[]|Int32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createTileindexVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createTileindexVector(builder:flatbuffers.Builder, data:number[]|Int32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startTileindexVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTypemap(builder:flatbuffers.Builder, typemapOffset:flatbuffers.Offset) {
  builder.addFieldOffset(14, typemapOffset, 0);
}

static createTypemapVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startTypemapVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addMetalmap(builder:flatbuffers.Builder, metalmapOffset:flatbuffers.Offset) {
  builder.addFieldOffset(15, metalmapOffset, 0);
}

static createMetalmapVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startMetalmapVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addMinimapUrl(builder:flatbuffers.Builder, minimapUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(16, minimapUrlOffset, 0);
}

static addTilesUrl(builder:flatbuffers.Builder, tilesUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(17, tilesUrlOffset, 0);
}

static addHasLuaGaia(builder:flatbuffers.Builder, hasLuaGaia:boolean) {
  builder.addFieldInt8(18, +hasLuaGaia, +false);
}

static endMapData(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMapData(builder:flatbuffers.Builder, mapx:number, mapy:number, squareSize:number, minHeight:number, maxHeight:number, tilesX:number, tilesZ:number, numTiles:number, tileSize:number, startPositionsOffset:flatbuffers.Offset, featureTypesOffset:flatbuffers.Offset, featuresOffset:flatbuffers.Offset, heightmapOffset:flatbuffers.Offset, tileindexOffset:flatbuffers.Offset, typemapOffset:flatbuffers.Offset, metalmapOffset:flatbuffers.Offset, minimapUrlOffset:flatbuffers.Offset, tilesUrlOffset:flatbuffers.Offset, hasLuaGaia:boolean):flatbuffers.Offset {
  MapData.startMapData(builder);
  MapData.addMapx(builder, mapx);
  MapData.addMapy(builder, mapy);
  MapData.addSquareSize(builder, squareSize);
  MapData.addMinHeight(builder, minHeight);
  MapData.addMaxHeight(builder, maxHeight);
  MapData.addTilesX(builder, tilesX);
  MapData.addTilesZ(builder, tilesZ);
  MapData.addNumTiles(builder, numTiles);
  MapData.addTileSize(builder, tileSize);
  MapData.addStartPositions(builder, startPositionsOffset);
  MapData.addFeatureTypes(builder, featureTypesOffset);
  MapData.addFeatures(builder, featuresOffset);
  MapData.addHeightmap(builder, heightmapOffset);
  MapData.addTileindex(builder, tileindexOffset);
  MapData.addTypemap(builder, typemapOffset);
  MapData.addMetalmap(builder, metalmapOffset);
  MapData.addMinimapUrl(builder, minimapUrlOffset);
  MapData.addTilesUrl(builder, tilesUrlOffset);
  MapData.addHasLuaGaia(builder, hasLuaGaia);
  return MapData.endMapData(builder);
}

unpack(): MapDataT {
  return new MapDataT(
    this.mapx(),
    this.mapy(),
    this.squareSize(),
    this.minHeight(),
    this.maxHeight(),
    this.tilesX(),
    this.tilesZ(),
    this.numTiles(),
    this.tileSize(),
    this.bb!.createObjList<MapStartPos, MapStartPosT>(this.startPositions.bind(this), this.startPositionsLength()),
    this.bb!.createScalarList<string>(this.featureTypes.bind(this), this.featureTypesLength()),
    this.bb!.createObjList<MapFeature, MapFeatureT>(this.features.bind(this), this.featuresLength()),
    this.bb!.createScalarList<number>(this.heightmap.bind(this), this.heightmapLength()),
    this.bb!.createScalarList<number>(this.tileindex.bind(this), this.tileindexLength()),
    this.bb!.createScalarList<number>(this.typemap.bind(this), this.typemapLength()),
    this.bb!.createScalarList<number>(this.metalmap.bind(this), this.metalmapLength()),
    this.minimapUrl(),
    this.tilesUrl(),
    this.hasLuaGaia()
  );
}


unpackTo(_o: MapDataT): void {
  _o.mapx = this.mapx();
  _o.mapy = this.mapy();
  _o.squareSize = this.squareSize();
  _o.minHeight = this.minHeight();
  _o.maxHeight = this.maxHeight();
  _o.tilesX = this.tilesX();
  _o.tilesZ = this.tilesZ();
  _o.numTiles = this.numTiles();
  _o.tileSize = this.tileSize();
  _o.startPositions = this.bb!.createObjList<MapStartPos, MapStartPosT>(this.startPositions.bind(this), this.startPositionsLength());
  _o.featureTypes = this.bb!.createScalarList<string>(this.featureTypes.bind(this), this.featureTypesLength());
  _o.features = this.bb!.createObjList<MapFeature, MapFeatureT>(this.features.bind(this), this.featuresLength());
  _o.heightmap = this.bb!.createScalarList<number>(this.heightmap.bind(this), this.heightmapLength());
  _o.tileindex = this.bb!.createScalarList<number>(this.tileindex.bind(this), this.tileindexLength());
  _o.typemap = this.bb!.createScalarList<number>(this.typemap.bind(this), this.typemapLength());
  _o.metalmap = this.bb!.createScalarList<number>(this.metalmap.bind(this), this.metalmapLength());
  _o.minimapUrl = this.minimapUrl();
  _o.tilesUrl = this.tilesUrl();
  _o.hasLuaGaia = this.hasLuaGaia();
}
}

export class MapDataT implements flatbuffers.IGeneratedObject {
constructor(
  public mapx: number = 0,
  public mapy: number = 0,
  public squareSize: number = 0,
  public minHeight: number = 0.0,
  public maxHeight: number = 0.0,
  public tilesX: number = 0,
  public tilesZ: number = 0,
  public numTiles: number = 0,
  public tileSize: number = 0,
  public startPositions: (MapStartPosT)[] = [],
  public featureTypes: (string)[] = [],
  public features: (MapFeatureT)[] = [],
  public heightmap: (number)[] = [],
  public tileindex: (number)[] = [],
  public typemap: (number)[] = [],
  public metalmap: (number)[] = [],
  public minimapUrl: string|Uint8Array|null = null,
  public tilesUrl: string|Uint8Array|null = null,
  public hasLuaGaia: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const startPositions = builder.createStructOffsetList(this.startPositions, MapData.startStartPositionsVector);
  const featureTypes = MapData.createFeatureTypesVector(builder, builder.createObjectOffsetList(this.featureTypes));
  const features = MapData.createFeaturesVector(builder, builder.createObjectOffsetList(this.features));
  const heightmap = MapData.createHeightmapVector(builder, this.heightmap);
  const tileindex = MapData.createTileindexVector(builder, this.tileindex);
  const typemap = MapData.createTypemapVector(builder, this.typemap);
  const metalmap = MapData.createMetalmapVector(builder, this.metalmap);
  const minimapUrl = (this.minimapUrl !== null ? builder.createString(this.minimapUrl!) : 0);
  const tilesUrl = (this.tilesUrl !== null ? builder.createString(this.tilesUrl!) : 0);

  return MapData.createMapData(builder,
    this.mapx,
    this.mapy,
    this.squareSize,
    this.minHeight,
    this.maxHeight,
    this.tilesX,
    this.tilesZ,
    this.numTiles,
    this.tileSize,
    startPositions,
    featureTypes,
    features,
    heightmap,
    tileindex,
    typemap,
    metalmap,
    minimapUrl,
    tilesUrl,
    this.hasLuaGaia
  );
}
}
