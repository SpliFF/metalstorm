// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Server → client: execute `code` in the browser (dev/test relay,
 * PLAN-test-automation P7). `target` picks the executor:
 *   "worker"  — the render worker's global scope (same engine hooks as evalJs)
 *   "js"      — the main thread's global scope
 *   "widgets" — the in-worker LuaUI runtime via window.widgets.eval (Lua)
 *   "test"    — window.test harness expression on main
 * A client that is not a DEV build (and lacks ?allowClientEval=1) answers
 * success:false without evaluating anything.
 */
export class ClientEvalRequest implements flatbuffers.IUnpackableObject<ClientEvalRequestT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ClientEvalRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsClientEvalRequest(bb:flatbuffers.ByteBuffer, obj?:ClientEvalRequest):ClientEvalRequest {
  return (obj || new ClientEvalRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsClientEvalRequest(bb:flatbuffers.ByteBuffer, obj?:ClientEvalRequest):ClientEvalRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ClientEvalRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

target():string|null
target(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
target(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

code():string|null
code(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
code(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startClientEvalRequest(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRequestId(builder:flatbuffers.Builder, requestId:number) {
  builder.addFieldInt32(0, requestId, 0);
}

static addTarget(builder:flatbuffers.Builder, targetOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, targetOffset, 0);
}

static addCode(builder:flatbuffers.Builder, codeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, codeOffset, 0);
}

static endClientEvalRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createClientEvalRequest(builder:flatbuffers.Builder, requestId:number, targetOffset:flatbuffers.Offset, codeOffset:flatbuffers.Offset):flatbuffers.Offset {
  ClientEvalRequest.startClientEvalRequest(builder);
  ClientEvalRequest.addRequestId(builder, requestId);
  ClientEvalRequest.addTarget(builder, targetOffset);
  ClientEvalRequest.addCode(builder, codeOffset);
  return ClientEvalRequest.endClientEvalRequest(builder);
}

unpack(): ClientEvalRequestT {
  return new ClientEvalRequestT(
    this.requestId(),
    this.target(),
    this.code()
  );
}


unpackTo(_o: ClientEvalRequestT): void {
  _o.requestId = this.requestId();
  _o.target = this.target();
  _o.code = this.code();
}
}

export class ClientEvalRequestT implements flatbuffers.IGeneratedObject {
constructor(
  public requestId: number = 0,
  public target: string|Uint8Array|null = null,
  public code: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const target = (this.target !== null ? builder.createString(this.target!) : 0);
  const code = (this.code !== null ? builder.createString(this.code!) : 0);

  return ClientEvalRequest.createClientEvalRequest(builder,
    this.requestId,
    target,
    code
  );
}
}
