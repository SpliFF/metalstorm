// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SoundSourceKind } from '../spring-web/sound-source-kind.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * A single sound emission. The server pushes one of these whenever a
 * weapon fires, a projectile impacts, a unit dies, etc. The client
 * resolves `(source_kind, source_def_id, sound_id)` against the matching
 * def's `sounds` array, decodes the audio buffer (cached), and plays
 * through the 96-voice pool with HRTF panning at `position`.
 */
export class SoundEvent implements flatbuffers.IUnpackableObject<SoundEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SoundEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSoundEvent(bb:flatbuffers.ByteBuffer, obj?:SoundEvent):SoundEvent {
  return (obj || new SoundEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSoundEvent(bb:flatbuffers.ByteBuffer, obj?:SoundEvent):SoundEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SoundEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

soundId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

sourceDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

sourceKind():SoundSourceKind {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : SoundSourceKind.Unit;
}

position(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

volume():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

pitch():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

/**
 * Eviction priority when the 96-voice pool is full. Higher wins.
 */
priority():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 128;
}

/**
 * Owner team — used by the per-session LOS filter so ally sounds
 * remain audible through fog of war. 255 = no team / global sound.
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 255;
}

static startSoundEvent(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addSoundId(builder:flatbuffers.Builder, soundId:number) {
  builder.addFieldInt16(0, soundId, 0);
}

static addSourceDefId(builder:flatbuffers.Builder, sourceDefId:number) {
  builder.addFieldInt16(1, sourceDefId, 0);
}

static addSourceKind(builder:flatbuffers.Builder, sourceKind:SoundSourceKind) {
  builder.addFieldInt8(2, sourceKind, SoundSourceKind.Unit);
}

static addPosition(builder:flatbuffers.Builder, positionOffset:flatbuffers.Offset) {
  builder.addFieldStruct(3, positionOffset, 0);
}

static addVolume(builder:flatbuffers.Builder, volume:number) {
  builder.addFieldFloat32(4, volume, 1.0);
}

static addPitch(builder:flatbuffers.Builder, pitch:number) {
  builder.addFieldFloat32(5, pitch, 1.0);
}

static addPriority(builder:flatbuffers.Builder, priority:number) {
  builder.addFieldInt8(6, priority, 128);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(7, team, 255);
}

static endSoundEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): SoundEventT {
  return new SoundEventT(
    this.soundId(),
    this.sourceDefId(),
    this.sourceKind(),
    (this.position() !== null ? this.position()!.unpack() : null),
    this.volume(),
    this.pitch(),
    this.priority(),
    this.team()
  );
}


unpackTo(_o: SoundEventT): void {
  _o.soundId = this.soundId();
  _o.sourceDefId = this.sourceDefId();
  _o.sourceKind = this.sourceKind();
  _o.position = (this.position() !== null ? this.position()!.unpack() : null);
  _o.volume = this.volume();
  _o.pitch = this.pitch();
  _o.priority = this.priority();
  _o.team = this.team();
}
}

export class SoundEventT implements flatbuffers.IGeneratedObject {
constructor(
  public soundId: number = 0,
  public sourceDefId: number = 0,
  public sourceKind: SoundSourceKind = SoundSourceKind.Unit,
  public position: Vec3T|null = null,
  public volume: number = 1.0,
  public pitch: number = 1.0,
  public priority: number = 128,
  public team: number = 255
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  SoundEvent.startSoundEvent(builder);
  SoundEvent.addSoundId(builder, this.soundId);
  SoundEvent.addSourceDefId(builder, this.sourceDefId);
  SoundEvent.addSourceKind(builder, this.sourceKind);
  SoundEvent.addPosition(builder, (this.position !== null ? this.position!.pack(builder) : 0));
  SoundEvent.addVolume(builder, this.volume);
  SoundEvent.addPitch(builder, this.pitch);
  SoundEvent.addPriority(builder, this.priority);
  SoundEvent.addTeam(builder, this.team);

  return SoundEvent.endSoundEvent(builder);
}
}
