// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class RoomKick {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RoomKick {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRoomKick(bb:flatbuffers.ByteBuffer, obj?:RoomKick):RoomKick {
  return (obj || new RoomKick()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRoomKick(bb:flatbuffers.ByteBuffer, obj?:RoomKick):RoomKick {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RoomKick()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

playerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startRoomKick(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addPlayerId(builder:flatbuffers.Builder, playerId:number) {
  builder.addFieldInt32(0, playerId, 0);
}

static endRoomKick(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRoomKick(builder:flatbuffers.Builder, playerId:number):flatbuffers.Offset {
  RoomKick.startRoomKick(builder);
  RoomKick.addPlayerId(builder, playerId);
  return RoomKick.endRoomKick(builder);
}
}
