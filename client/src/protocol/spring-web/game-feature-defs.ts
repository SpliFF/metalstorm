// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameFeatureDef, GameFeatureDefT } from '../spring-web/game-feature-def.js';


/**
 * Game-wide feature def table. Sent alongside `GameUnitDefs` /
 * `GameWeaponDefs` / `GameCegDefs` from the same baked cache directory.
 */
export class GameFeatureDefs implements flatbuffers.IUnpackableObject<GameFeatureDefsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameFeatureDefs {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameFeatureDefs(bb:flatbuffers.ByteBuffer, obj?:GameFeatureDefs):GameFeatureDefs {
  return (obj || new GameFeatureDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameFeatureDefs(bb:flatbuffers.ByteBuffer, obj?:GameFeatureDefs):GameFeatureDefs {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameFeatureDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

defs(index: number, obj?:GameFeatureDef):GameFeatureDef|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GameFeatureDef()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

defsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Base URL for model/texture paths (matches GameUnitDefs.base_url).
 */
baseUrl():string|null
baseUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
baseUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startGameFeatureDefs(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, defsOffset, 0);
}

static createDefsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDefsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBaseUrl(builder:flatbuffers.Builder, baseUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, baseUrlOffset, 0);
}

static endGameFeatureDefs(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameFeatureDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset, baseUrlOffset:flatbuffers.Offset):flatbuffers.Offset {
  GameFeatureDefs.startGameFeatureDefs(builder);
  GameFeatureDefs.addDefs(builder, defsOffset);
  GameFeatureDefs.addBaseUrl(builder, baseUrlOffset);
  return GameFeatureDefs.endGameFeatureDefs(builder);
}

unpack(): GameFeatureDefsT {
  return new GameFeatureDefsT(
    this.bb!.createObjList<GameFeatureDef, GameFeatureDefT>(this.defs.bind(this), this.defsLength()),
    this.baseUrl()
  );
}


unpackTo(_o: GameFeatureDefsT): void {
  _o.defs = this.bb!.createObjList<GameFeatureDef, GameFeatureDefT>(this.defs.bind(this), this.defsLength());
  _o.baseUrl = this.baseUrl();
}
}

export class GameFeatureDefsT implements flatbuffers.IGeneratedObject {
constructor(
  public defs: (GameFeatureDefT)[] = [],
  public baseUrl: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const defs = GameFeatureDefs.createDefsVector(builder, builder.createObjectOffsetList(this.defs));
  const baseUrl = (this.baseUrl !== null ? builder.createString(this.baseUrl!) : 0);

  return GameFeatureDefs.createGameFeatureDefs(builder,
    defs,
    baseUrl
  );
}
}
