// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitOrder, UnitOrderT } from '../spring-web/unit-order.js';


export class UnitCommandQueue implements flatbuffers.IUnpackableObject<UnitCommandQueueT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitCommandQueue {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitCommandQueue(bb:flatbuffers.ByteBuffer, obj?:UnitCommandQueue):UnitCommandQueue {
  return (obj || new UnitCommandQueue()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitCommandQueue(bb:flatbuffers.ByteBuffer, obj?:UnitCommandQueue):UnitCommandQueue {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitCommandQueue()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

unitId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

orders(index: number, obj?:UnitOrder):UnitOrder|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new UnitOrder()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

ordersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUnitCommandQueue(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addUnitId(builder:flatbuffers.Builder, unitId:number) {
  builder.addFieldInt32(0, unitId, 0);
}

static addOrders(builder:flatbuffers.Builder, ordersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, ordersOffset, 0);
}

static createOrdersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startOrdersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitCommandQueue(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitCommandQueue(builder:flatbuffers.Builder, unitId:number, ordersOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitCommandQueue.startUnitCommandQueue(builder);
  UnitCommandQueue.addUnitId(builder, unitId);
  UnitCommandQueue.addOrders(builder, ordersOffset);
  return UnitCommandQueue.endUnitCommandQueue(builder);
}

unpack(): UnitCommandQueueT {
  return new UnitCommandQueueT(
    this.unitId(),
    this.bb!.createObjList<UnitOrder, UnitOrderT>(this.orders.bind(this), this.ordersLength())
  );
}


unpackTo(_o: UnitCommandQueueT): void {
  _o.unitId = this.unitId();
  _o.orders = this.bb!.createObjList<UnitOrder, UnitOrderT>(this.orders.bind(this), this.ordersLength());
}
}

export class UnitCommandQueueT implements flatbuffers.IGeneratedObject {
constructor(
  public unitId: number = 0,
  public orders: (UnitOrderT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const orders = UnitCommandQueue.createOrdersVector(builder, builder.createObjectOffsetList(this.orders));

  return UnitCommandQueue.createUnitCommandQueue(builder,
    this.unitId,
    orders
  );
}
}
