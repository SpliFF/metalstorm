// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameStarted, GameStartedT } from '../spring-web/game-started.js';
import { IpcPayload, unionToIpcPayload, unionListToIpcPayload } from '../spring-web/ipc-payload.js';


export class IpcMessage implements flatbuffers.IUnpackableObject<IpcMessageT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IpcMessage {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIpcMessage(bb:flatbuffers.ByteBuffer, obj?:IpcMessage):IpcMessage {
  return (obj || new IpcMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIpcMessage(bb:flatbuffers.ByteBuffer, obj?:IpcMessage):IpcMessage {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IpcMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

payloadType():IpcPayload {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : IpcPayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startIpcMessage(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:IpcPayload) {
  builder.addFieldInt8(0, payloadType, IpcPayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, payloadOffset, 0);
}

static endIpcMessage(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIpcMessage(builder:flatbuffers.Builder, payloadType:IpcPayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  IpcMessage.startIpcMessage(builder);
  IpcMessage.addPayloadType(builder, payloadType);
  IpcMessage.addPayload(builder, payloadOffset);
  return IpcMessage.endIpcMessage(builder);
}

unpack(): IpcMessageT {
  return new IpcMessageT(
    this.payloadType(),
    (() => {
      const temp = unionToIpcPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })()
  );
}


unpackTo(_o: IpcMessageT): void {
  _o.payloadType = this.payloadType();
  _o.payload = (() => {
      const temp = unionToIpcPayload(this.payloadType(), this.payload.bind(this));
      if(temp === null) { return null; }
      return temp.unpack()
  })();
}
}

export class IpcMessageT implements flatbuffers.IGeneratedObject {
constructor(
  public payloadType: IpcPayload = IpcPayload.NONE,
  public payload: GameStartedT|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const payload = builder.createObjectOffset(this.payload);

  return IpcMessage.createIpcMessage(builder,
    this.payloadType,
    payload
  );
}
}
