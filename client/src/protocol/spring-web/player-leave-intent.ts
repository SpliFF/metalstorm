// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Client → Server (game connection only): sent immediately before a
 * deliberate `disconnect()` so the server can distinguish *why* the client
 * is leaving. Optional — a client that disconnects without sending this
 * (crash, network drop, timeout) still produces a `PlayerRemoved` with the
 * server's own default reason (0). See `PlayerLeft.reason` /
 * `PlayerTeamEventItem.reason` for the shared reason enum.
 * PLAN-quickstart.md §3.3: the detach path (`gpDetach`) sends this with
 * reason=3 so PLAN-metalstorm-teams.md's `PlayerRemoved` consumer can tell
 * a parked/reconnecting player apart from one who actually quit.
 */
export class PlayerLeaveIntent implements flatbuffers.IUnpackableObject<PlayerLeaveIntentT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PlayerLeaveIntent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPlayerLeaveIntent(bb:flatbuffers.ByteBuffer, obj?:PlayerLeaveIntent):PlayerLeaveIntent {
  return (obj || new PlayerLeaveIntent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPlayerLeaveIntent(bb:flatbuffers.ByteBuffer, obj?:PlayerLeaveIntent):PlayerLeaveIntent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PlayerLeaveIntent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

reason():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startPlayerLeaveIntent(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addReason(builder:flatbuffers.Builder, reason:number) {
  builder.addFieldInt8(0, reason, 0);
}

static endPlayerLeaveIntent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPlayerLeaveIntent(builder:flatbuffers.Builder, reason:number):flatbuffers.Offset {
  PlayerLeaveIntent.startPlayerLeaveIntent(builder);
  PlayerLeaveIntent.addReason(builder, reason);
  return PlayerLeaveIntent.endPlayerLeaveIntent(builder);
}

unpack(): PlayerLeaveIntentT {
  return new PlayerLeaveIntentT(
    this.reason()
  );
}


unpackTo(_o: PlayerLeaveIntentT): void {
  _o.reason = this.reason();
}
}

export class PlayerLeaveIntentT implements flatbuffers.IGeneratedObject {
constructor(
  public reason: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return PlayerLeaveIntent.createPlayerLeaveIntent(builder,
    this.reason
  );
}
}
