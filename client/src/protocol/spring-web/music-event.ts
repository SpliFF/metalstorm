// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MusicState } from '../spring-web/music-state.js';


/**
 * Music-state transition event. Broadcast to all clients (not per-
 * viewport) in the same GameEventBatch as combat / sound events.
 * `fade_ms` is the crossfade duration the client should use when
 * switching to a track in the new state's playlist; defaults to 2 s
 * for in-game transitions, 500 ms for victory / defeat stings.
 */
export class MusicEvent implements flatbuffers.IUnpackableObject<MusicEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MusicEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMusicEvent(bb:flatbuffers.ByteBuffer, obj?:MusicEvent):MusicEvent {
  return (obj || new MusicEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMusicEvent(bb:flatbuffers.ByteBuffer, obj?:MusicEvent):MusicEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MusicEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

state():MusicState {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : MusicState.Peace;
}

fadeMs():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 2000;
}

static startMusicEvent(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addState(builder:flatbuffers.Builder, state:MusicState) {
  builder.addFieldInt8(0, state, MusicState.Peace);
}

static addFadeMs(builder:flatbuffers.Builder, fadeMs:number) {
  builder.addFieldInt16(1, fadeMs, 2000);
}

static endMusicEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMusicEvent(builder:flatbuffers.Builder, state:MusicState, fadeMs:number):flatbuffers.Offset {
  MusicEvent.startMusicEvent(builder);
  MusicEvent.addState(builder, state);
  MusicEvent.addFadeMs(builder, fadeMs);
  return MusicEvent.endMusicEvent(builder);
}

unpack(): MusicEventT {
  return new MusicEventT(
    this.state(),
    this.fadeMs()
  );
}


unpackTo(_o: MusicEventT): void {
  _o.state = this.state();
  _o.fadeMs = this.fadeMs();
}
}

export class MusicEventT implements flatbuffers.IGeneratedObject {
constructor(
  public state: MusicState = MusicState.Peace,
  public fadeMs: number = 2000
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return MusicEvent.createMusicEvent(builder,
    this.state,
    this.fadeMs
  );
}
}
