// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One row of the game server's authoritative player roster.
 *
 * Roster entries exist for every `CPlayer` in the server's `playerHandler`:
 * humans (commanding and spectating) and AI virtual players alike. Metalstorm
 * deliberately registers each AI slot as a real player so it pays authority
 * through the same gate as a human (PLAN-metalstorm-ai.md §1) — a departure
 * from stock Spring, where a SkirmishAI is not a player. `is_ai` is how a
 * client tells the two apart.
 */
export class PlayerEntry implements flatbuffers.IUnpackableObject<PlayerEntryT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PlayerEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPlayerEntry(bb:flatbuffers.ByteBuffer, obj?:PlayerEntry):PlayerEntry {
  return (obj || new PlayerEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPlayerEntry(bb:flatbuffers.ByteBuffer, obj?:PlayerEntry):PlayerEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PlayerEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Spring's `playerNum`. THE sim-side identity — same value as
 * `AuthResponse.player_num`, `UnitCommandEvent.player_id`, and the
 * `<playerID>` in Metalstorm's `authority_player_<id>` /
 * `score_<id>_earned` rulesParams.
 */
playerNum():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Sim team id. -1 for a spectator (no team).
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : -1;
}

/**
 * The team's ally team. -1 when the team is unknown / unassigned.
 */
allyTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : -1;
}

spectator():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * True for an AI virtual player (see the table comment).
 */
isAi():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * False once the player has disconnected. Entries are kept rather than
 * removed so a scoreboard can still name a player who dropped.
 */
active():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : true;
}

/**
 * DB account id for a human — the value `AuthResponse.player_id` carries
 * for that session. 0 for AI virtual players, which have no account.
 */
accountId():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startPlayerEntry(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addPlayerNum(builder:flatbuffers.Builder, playerNum:number) {
  builder.addFieldInt32(0, playerNum, 0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt16(2, team, -1);
}

static addAllyTeam(builder:flatbuffers.Builder, allyTeam:number) {
  builder.addFieldInt16(3, allyTeam, -1);
}

static addSpectator(builder:flatbuffers.Builder, spectator:boolean) {
  builder.addFieldInt8(4, +spectator, +false);
}

static addIsAi(builder:flatbuffers.Builder, isAi:boolean) {
  builder.addFieldInt8(5, +isAi, +false);
}

static addActive(builder:flatbuffers.Builder, active:boolean) {
  builder.addFieldInt8(6, +active, +true);
}

static addAccountId(builder:flatbuffers.Builder, accountId:number) {
  builder.addFieldInt32(7, accountId, 0);
}

static endPlayerEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPlayerEntry(builder:flatbuffers.Builder, playerNum:number, nameOffset:flatbuffers.Offset, team:number, allyTeam:number, spectator:boolean, isAi:boolean, active:boolean, accountId:number):flatbuffers.Offset {
  PlayerEntry.startPlayerEntry(builder);
  PlayerEntry.addPlayerNum(builder, playerNum);
  PlayerEntry.addName(builder, nameOffset);
  PlayerEntry.addTeam(builder, team);
  PlayerEntry.addAllyTeam(builder, allyTeam);
  PlayerEntry.addSpectator(builder, spectator);
  PlayerEntry.addIsAi(builder, isAi);
  PlayerEntry.addActive(builder, active);
  PlayerEntry.addAccountId(builder, accountId);
  return PlayerEntry.endPlayerEntry(builder);
}

unpack(): PlayerEntryT {
  return new PlayerEntryT(
    this.playerNum(),
    this.name(),
    this.team(),
    this.allyTeam(),
    this.spectator(),
    this.isAi(),
    this.active(),
    this.accountId()
  );
}


unpackTo(_o: PlayerEntryT): void {
  _o.playerNum = this.playerNum();
  _o.name = this.name();
  _o.team = this.team();
  _o.allyTeam = this.allyTeam();
  _o.spectator = this.spectator();
  _o.isAi = this.isAi();
  _o.active = this.active();
  _o.accountId = this.accountId();
}
}

export class PlayerEntryT implements flatbuffers.IGeneratedObject {
constructor(
  public playerNum: number = 0,
  public name: string|Uint8Array|null = null,
  public team: number = -1,
  public allyTeam: number = -1,
  public spectator: boolean = false,
  public isAi: boolean = false,
  public active: boolean = true,
  public accountId: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const name = (this.name !== null ? builder.createString(this.name!) : 0);

  return PlayerEntry.createPlayerEntry(builder,
    this.playerNum,
    name,
    this.team,
    this.allyTeam,
    this.spectator,
    this.isAi,
    this.active,
    this.accountId
  );
}
}
