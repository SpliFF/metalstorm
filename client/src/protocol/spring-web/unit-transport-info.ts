// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One transport relationship. Lists a transporter and the units it
 * is currently carrying. Cargo unit ids appear in the order
 * `CUnit::transportedUnits` holds them; a transporter with empty
 * cargo is omitted from the update entirely.
 */
export class UnitTransportInfo implements flatbuffers.IUnpackableObject<UnitTransportInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitTransportInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitTransportInfo(bb:flatbuffers.ByteBuffer, obj?:UnitTransportInfo):UnitTransportInfo {
  return (obj || new UnitTransportInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitTransportInfo(bb:flatbuffers.ByteBuffer, obj?:UnitTransportInfo):UnitTransportInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitTransportInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

transporterId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

cargo(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

cargoLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

cargoArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startUnitTransportInfo(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addTransporterId(builder:flatbuffers.Builder, transporterId:number) {
  builder.addFieldInt32(0, transporterId, 0);
}

static addCargo(builder:flatbuffers.Builder, cargoOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, cargoOffset, 0);
}

static createCargoVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createCargoVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createCargoVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startCargoVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitTransportInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitTransportInfo(builder:flatbuffers.Builder, transporterId:number, cargoOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitTransportInfo.startUnitTransportInfo(builder);
  UnitTransportInfo.addTransporterId(builder, transporterId);
  UnitTransportInfo.addCargo(builder, cargoOffset);
  return UnitTransportInfo.endUnitTransportInfo(builder);
}

unpack(): UnitTransportInfoT {
  return new UnitTransportInfoT(
    this.transporterId(),
    this.bb!.createScalarList<number>(this.cargo.bind(this), this.cargoLength())
  );
}


unpackTo(_o: UnitTransportInfoT): void {
  _o.transporterId = this.transporterId();
  _o.cargo = this.bb!.createScalarList<number>(this.cargo.bind(this), this.cargoLength());
}
}

export class UnitTransportInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public transporterId: number = 0,
  public cargo: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const cargo = UnitTransportInfo.createCargoVector(builder, this.cargo);

  return UnitTransportInfo.createUnitTransportInfo(builder,
    this.transporterId,
    cargo
  );
}
}
