// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * A single structured log entry. Used in LogIngest (inbound)
 * and LogBatch (outbound streaming to subscribers).
 */
export class LogEntryMsg implements flatbuffers.IUnpackableObject<LogEntryMsgT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LogEntryMsg {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLogEntryMsg(bb:flatbuffers.ByteBuffer, obj?:LogEntryMsg):LogEntryMsg {
  return (obj || new LogEntryMsg()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLogEntryMsg(bb:flatbuffers.ByteBuffer, obj?:LogEntryMsg):LogEntryMsg {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LogEntryMsg()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

id():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

timestamp():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

level():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

section():string|null
section(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
section(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

scope():string|null
scope(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
scope(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

process():string|null
process(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
process(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

message():string|null
message(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
message(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

frame():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startLogEntryMsg(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addId(builder:flatbuffers.Builder, id:bigint) {
  builder.addFieldInt64(0, id, BigInt('0'));
}

static addTimestamp(builder:flatbuffers.Builder, timestamp:bigint) {
  builder.addFieldInt64(1, timestamp, BigInt('0'));
}

static addLevel(builder:flatbuffers.Builder, level:number) {
  builder.addFieldInt8(2, level, 0);
}

static addSection(builder:flatbuffers.Builder, sectionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, sectionOffset, 0);
}

static addScope(builder:flatbuffers.Builder, scopeOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, scopeOffset, 0);
}

static addProcess(builder:flatbuffers.Builder, processOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, processOffset, 0);
}

static addMessage(builder:flatbuffers.Builder, messageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, messageOffset, 0);
}

static addFrame(builder:flatbuffers.Builder, frame:number) {
  builder.addFieldInt32(7, frame, 0);
}

static endLogEntryMsg(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createLogEntryMsg(builder:flatbuffers.Builder, id:bigint, timestamp:bigint, level:number, sectionOffset:flatbuffers.Offset, scopeOffset:flatbuffers.Offset, processOffset:flatbuffers.Offset, messageOffset:flatbuffers.Offset, frame:number):flatbuffers.Offset {
  LogEntryMsg.startLogEntryMsg(builder);
  LogEntryMsg.addId(builder, id);
  LogEntryMsg.addTimestamp(builder, timestamp);
  LogEntryMsg.addLevel(builder, level);
  LogEntryMsg.addSection(builder, sectionOffset);
  LogEntryMsg.addScope(builder, scopeOffset);
  LogEntryMsg.addProcess(builder, processOffset);
  LogEntryMsg.addMessage(builder, messageOffset);
  LogEntryMsg.addFrame(builder, frame);
  return LogEntryMsg.endLogEntryMsg(builder);
}

unpack(): LogEntryMsgT {
  return new LogEntryMsgT(
    this.id(),
    this.timestamp(),
    this.level(),
    this.section(),
    this.scope(),
    this.process(),
    this.message(),
    this.frame()
  );
}


unpackTo(_o: LogEntryMsgT): void {
  _o.id = this.id();
  _o.timestamp = this.timestamp();
  _o.level = this.level();
  _o.section = this.section();
  _o.scope = this.scope();
  _o.process = this.process();
  _o.message = this.message();
  _o.frame = this.frame();
}
}

export class LogEntryMsgT implements flatbuffers.IGeneratedObject {
constructor(
  public id: bigint = BigInt('0'),
  public timestamp: bigint = BigInt('0'),
  public level: number = 0,
  public section: string|Uint8Array|null = null,
  public scope: string|Uint8Array|null = null,
  public process: string|Uint8Array|null = null,
  public message: string|Uint8Array|null = null,
  public frame: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const section = (this.section !== null ? builder.createString(this.section!) : 0);
  const scope = (this.scope !== null ? builder.createString(this.scope!) : 0);
  const process = (this.process !== null ? builder.createString(this.process!) : 0);
  const message = (this.message !== null ? builder.createString(this.message!) : 0);

  return LogEntryMsg.createLogEntryMsg(builder,
    this.id,
    this.timestamp,
    this.level,
    section,
    scope,
    process,
    message,
    this.frame
  );
}
}
