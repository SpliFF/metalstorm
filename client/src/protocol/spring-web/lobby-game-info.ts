// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One game plugin discovered under content/games. Shown in the
 * lobby's create-room dropdown. Named `LobbyGameInfo` to avoid
 * colliding with the existing `GameInfo` table which represents
 * live-sim state (map/game speed/frame/paused).
 */
export class LobbyGameInfo implements flatbuffers.IUnpackableObject<LobbyGameInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LobbyGameInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLobbyGameInfo(bb:flatbuffers.ByteBuffer, obj?:LobbyGameInfo):LobbyGameInfo {
  return (obj || new LobbyGameInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLobbyGameInfo(bb:flatbuffers.ByteBuffer, obj?:LobbyGameInfo):LobbyGameInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LobbyGameInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

id():string|null
id(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
id(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

displayName():string|null
displayName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
displayName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

description():string|null
description(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
description(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

version():string|null
version(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
version(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startLobbyGameInfo(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addId(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, idOffset, 0);
}

static addDisplayName(builder:flatbuffers.Builder, displayNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, displayNameOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, descriptionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, descriptionOffset, 0);
}

static addVersion(builder:flatbuffers.Builder, versionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, versionOffset, 0);
}

static endLobbyGameInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createLobbyGameInfo(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset, displayNameOffset:flatbuffers.Offset, descriptionOffset:flatbuffers.Offset, versionOffset:flatbuffers.Offset):flatbuffers.Offset {
  LobbyGameInfo.startLobbyGameInfo(builder);
  LobbyGameInfo.addId(builder, idOffset);
  LobbyGameInfo.addDisplayName(builder, displayNameOffset);
  LobbyGameInfo.addDescription(builder, descriptionOffset);
  LobbyGameInfo.addVersion(builder, versionOffset);
  return LobbyGameInfo.endLobbyGameInfo(builder);
}

unpack(): LobbyGameInfoT {
  return new LobbyGameInfoT(
    this.id(),
    this.displayName(),
    this.description(),
    this.version()
  );
}


unpackTo(_o: LobbyGameInfoT): void {
  _o.id = this.id();
  _o.displayName = this.displayName();
  _o.description = this.description();
  _o.version = this.version();
}
}

export class LobbyGameInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public id: string|Uint8Array|null = null,
  public displayName: string|Uint8Array|null = null,
  public description: string|Uint8Array|null = null,
  public version: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const id = (this.id !== null ? builder.createString(this.id!) : 0);
  const displayName = (this.displayName !== null ? builder.createString(this.displayName!) : 0);
  const description = (this.description !== null ? builder.createString(this.description!) : 0);
  const version = (this.version !== null ? builder.createString(this.version!) : 0);

  return LobbyGameInfo.createLobbyGameInfo(builder,
    id,
    displayName,
    description,
    version
  );
}
}
