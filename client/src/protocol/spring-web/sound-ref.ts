// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SoundCategory } from '../spring-web/sound-category.js';


/**
 * A sound asset reference attached to a def. The client uses
 * `source_def_id + sound_id` from a SoundEvent to look up the matching
 * SoundRef and resolve `path` to an HTTP URL against the content server.
 */
export class SoundRef implements flatbuffers.IUnpackableObject<SoundRefT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SoundRef {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSoundRef(bb:flatbuffers.ByteBuffer, obj?:SoundRef):SoundRef {
  return (obj || new SoundRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSoundRef(bb:flatbuffers.ByteBuffer, obj?:SoundRef):SoundRef {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SoundRef()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Index into the def's `sounds` array. Matches SoundEvent.sound_id.
 */
id():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * Relative path under the content root (e.g. `sounds/weapon/laser1.webm`).
 * Server's `NormalizeSoundPath` rewrites any source extension to
 * `.webm` before serialisation, so the wire-format path is always
 * the canonical Opus-in-WebM file.
 */
path():string|null
path(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
path(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

category():SoundCategory {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : SoundCategory.Fire;
}

/**
 * Per-asset default gain (0–1). Final mix volume is volume * SoundEvent.volume.
 */
volume():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

/**
 * Per-asset default pitch / playback rate. Final rate is pitch * SoundEvent.pitch.
 */
pitch():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 1.0;
}

/**
 * Unresolved logical name of this sound (e.g. `"weapon/laser1"`,
 * `"bot_select"`) — the raw GuiSoundSetData.name value from the
 * unit / weapon def. The client uses this to look up the matching
 * entry in `gamedata/sounds.lua`'s `SoundItems` table; the
 * resolved item supplies `gain` / `pitch` / `priority` /
 * `maxconcurrent` / `maxdist` / `rolloff` / `in3d` defaults that
 * the server doesn't know about. Empty string means "no logical
 * name, just use `path`".
 */
name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startSoundRef(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addId(builder:flatbuffers.Builder, id:number) {
  builder.addFieldInt16(0, id, 0);
}

static addPath(builder:flatbuffers.Builder, pathOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, pathOffset, 0);
}

static addCategory(builder:flatbuffers.Builder, category:SoundCategory) {
  builder.addFieldInt8(2, category, SoundCategory.Fire);
}

static addVolume(builder:flatbuffers.Builder, volume:number) {
  builder.addFieldFloat32(3, volume, 1.0);
}

static addPitch(builder:flatbuffers.Builder, pitch:number) {
  builder.addFieldFloat32(4, pitch, 1.0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, nameOffset, 0);
}

static endSoundRef(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSoundRef(builder:flatbuffers.Builder, id:number, pathOffset:flatbuffers.Offset, category:SoundCategory, volume:number, pitch:number, nameOffset:flatbuffers.Offset):flatbuffers.Offset {
  SoundRef.startSoundRef(builder);
  SoundRef.addId(builder, id);
  SoundRef.addPath(builder, pathOffset);
  SoundRef.addCategory(builder, category);
  SoundRef.addVolume(builder, volume);
  SoundRef.addPitch(builder, pitch);
  SoundRef.addName(builder, nameOffset);
  return SoundRef.endSoundRef(builder);
}

unpack(): SoundRefT {
  return new SoundRefT(
    this.id(),
    this.path(),
    this.category(),
    this.volume(),
    this.pitch(),
    this.name()
  );
}


unpackTo(_o: SoundRefT): void {
  _o.id = this.id();
  _o.path = this.path();
  _o.category = this.category();
  _o.volume = this.volume();
  _o.pitch = this.pitch();
  _o.name = this.name();
}
}

export class SoundRefT implements flatbuffers.IGeneratedObject {
constructor(
  public id: number = 0,
  public path: string|Uint8Array|null = null,
  public category: SoundCategory = SoundCategory.Fire,
  public volume: number = 1.0,
  public pitch: number = 1.0,
  public name: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const path = (this.path !== null ? builder.createString(this.path!) : 0);
  const name = (this.name !== null ? builder.createString(this.name!) : 0);

  return SoundRef.createSoundRef(builder,
    this.id,
    path,
    this.category,
    this.volume,
    this.pitch,
    name
  );
}
}
