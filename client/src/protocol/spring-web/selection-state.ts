// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Client → Server: the local player's current selection. Sent debounced
 * (~50ms) on selection change. Server uses it to scope
 * UnitCmdDescsUpdate to only the units the player has selected, and as
 * the implicit target list for grouped commands when squad_ids is
 * empty. Sending an empty list is valid (no selection). The server
 * treats absent SelectionState as "selection unchanged" — resends are
 * idempotent.
 */
export class SelectionState implements flatbuffers.IUnpackableObject<SelectionStateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SelectionState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSelectionState(bb:flatbuffers.ByteBuffer, obj?:SelectionState):SelectionState {
  return (obj || new SelectionState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSelectionState(bb:flatbuffers.ByteBuffer, obj?:SelectionState):SelectionState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SelectionState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

sequence():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

unitIds(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

unitIdsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

unitIdsArray():Uint32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Uint32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

static startSelectionState(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addSequence(builder:flatbuffers.Builder, sequence:number) {
  builder.addFieldInt32(0, sequence, 0);
}

static addUnitIds(builder:flatbuffers.Builder, unitIdsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, unitIdsOffset, 0);
}

static createUnitIdsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createUnitIdsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createUnitIdsVector(builder:flatbuffers.Builder, data:number[]|Uint32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startUnitIdsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endSelectionState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSelectionState(builder:flatbuffers.Builder, sequence:number, unitIdsOffset:flatbuffers.Offset):flatbuffers.Offset {
  SelectionState.startSelectionState(builder);
  SelectionState.addSequence(builder, sequence);
  SelectionState.addUnitIds(builder, unitIdsOffset);
  return SelectionState.endSelectionState(builder);
}

unpack(): SelectionStateT {
  return new SelectionStateT(
    this.sequence(),
    this.bb!.createScalarList<number>(this.unitIds.bind(this), this.unitIdsLength())
  );
}


unpackTo(_o: SelectionStateT): void {
  _o.sequence = this.sequence();
  _o.unitIds = this.bb!.createScalarList<number>(this.unitIds.bind(this), this.unitIdsLength());
}
}

export class SelectionStateT implements flatbuffers.IGeneratedObject {
constructor(
  public sequence: number = 0,
  public unitIds: (number)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const unitIds = SelectionState.createUnitIdsVector(builder, this.unitIds);

  return SelectionState.createSelectionState(builder,
    this.sequence,
    unitIds
  );
}
}
