// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One unit's self-destruct countdown state.
 */
export class UnitSelfDInfo implements flatbuffers.IUnpackableObject<UnitSelfDInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitSelfDInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitSelfDInfo(bb:flatbuffers.ByteBuffer, obj?:UnitSelfDInfo):UnitSelfDInfo {
  return (obj || new UnitSelfDInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitSelfDInfo(bb:flatbuffers.ByteBuffer, obj?:UnitSelfDInfo):UnitSelfDInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitSelfDInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

unitId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Game-seconds remaining on the self-destruct countdown. Matches
 * Spring's `CUnit::selfDCountdown` directly (decremented once per
 * game-second, not per frame). 0 = not self-destructing.
 */
secondsRemaining():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

static startUnitSelfDInfo(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addUnitId(builder:flatbuffers.Builder, unitId:number) {
  builder.addFieldInt32(0, unitId, 0);
}

static addSecondsRemaining(builder:flatbuffers.Builder, secondsRemaining:number) {
  builder.addFieldInt16(1, secondsRemaining, 0);
}

static endUnitSelfDInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitSelfDInfo(builder:flatbuffers.Builder, unitId:number, secondsRemaining:number):flatbuffers.Offset {
  UnitSelfDInfo.startUnitSelfDInfo(builder);
  UnitSelfDInfo.addUnitId(builder, unitId);
  UnitSelfDInfo.addSecondsRemaining(builder, secondsRemaining);
  return UnitSelfDInfo.endUnitSelfDInfo(builder);
}

unpack(): UnitSelfDInfoT {
  return new UnitSelfDInfoT(
    this.unitId(),
    this.secondsRemaining()
  );
}


unpackTo(_o: UnitSelfDInfoT): void {
  _o.unitId = this.unitId();
  _o.secondsRemaining = this.secondsRemaining();
}
}

export class UnitSelfDInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public unitId: number = 0,
  public secondsRemaining: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return UnitSelfDInfo.createUnitSelfDInfo(builder,
    this.unitId,
    this.secondsRemaining
  );
}
}
