// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { RulesParamEntry, RulesParamEntryT } from '../spring-web/rules-param-entry.js';
import { RulesParamScope } from '../spring-web/rules-param-scope.js';


/**
 * Server → Client: a per-sim-tick batch of rules-param changes for ONE
 * scope (+ id for Team scope). Consumed by the LuaUI worker's
 * `handleRulesParamUpdate`, feeding `Spring.GetGameRulesParam(s)` /
 * `Spring.GetTeamRulesParam(s)` in widgets/gadgets.
 *
 * `id` is the teamID for Team scope (ignored for Game). `replace = true`
 * asks the client to clear the target map before applying — used only for
 * the join/rejoin snapshot so a late joiner converges exactly to current
 * state; per-tick deltas use `replace = false`.
 *
 * **Visibility** (replicates `LuaSyncedRead::GetTeamRulesParam(s)`): Team
 * batches are filtered server-side per connection against each param's
 * `los` bitmask — same-ally sessions see PRIVATE-and-below, allied-team
 * sessions see ALLIED-and-below, everyone sees PUBLIC; spectators (team < 0)
 * see all. Game batches are broadcast to every client.
 */
export class RulesParamUpdate implements flatbuffers.IUnpackableObject<RulesParamUpdateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RulesParamUpdate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRulesParamUpdate(bb:flatbuffers.ByteBuffer, obj?:RulesParamUpdate):RulesParamUpdate {
  return (obj || new RulesParamUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRulesParamUpdate(bb:flatbuffers.ByteBuffer, obj?:RulesParamUpdate):RulesParamUpdate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RulesParamUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

scope():RulesParamScope {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : RulesParamScope.Game;
}

id():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

replace():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

params(index: number, obj?:RulesParamEntry):RulesParamEntry|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new RulesParamEntry()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

paramsRev():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startRulesParamUpdate(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addScope(builder:flatbuffers.Builder, scope:RulesParamScope) {
  builder.addFieldInt8(0, scope, RulesParamScope.Game);
}

static addId(builder:flatbuffers.Builder, id:number) {
  builder.addFieldInt32(1, id, 0);
}

static addReplace(builder:flatbuffers.Builder, replace:boolean) {
  builder.addFieldInt8(2, +replace, +false);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addParamsRev(builder:flatbuffers.Builder, paramsRev:number) {
  builder.addFieldInt32(4, paramsRev, 0);
}

static endRulesParamUpdate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRulesParamUpdate(builder:flatbuffers.Builder, scope:RulesParamScope, id:number, replace:boolean, paramsOffset:flatbuffers.Offset, paramsRev:number):flatbuffers.Offset {
  RulesParamUpdate.startRulesParamUpdate(builder);
  RulesParamUpdate.addScope(builder, scope);
  RulesParamUpdate.addId(builder, id);
  RulesParamUpdate.addReplace(builder, replace);
  RulesParamUpdate.addParams(builder, paramsOffset);
  RulesParamUpdate.addParamsRev(builder, paramsRev);
  return RulesParamUpdate.endRulesParamUpdate(builder);
}

unpack(): RulesParamUpdateT {
  return new RulesParamUpdateT(
    this.scope(),
    this.id(),
    this.replace(),
    this.bb!.createObjList<RulesParamEntry, RulesParamEntryT>(this.params.bind(this), this.paramsLength()),
    this.paramsRev()
  );
}


unpackTo(_o: RulesParamUpdateT): void {
  _o.scope = this.scope();
  _o.id = this.id();
  _o.replace = this.replace();
  _o.params = this.bb!.createObjList<RulesParamEntry, RulesParamEntryT>(this.params.bind(this), this.paramsLength());
  _o.paramsRev = this.paramsRev();
}
}

export class RulesParamUpdateT implements flatbuffers.IGeneratedObject {
constructor(
  public scope: RulesParamScope = RulesParamScope.Game,
  public id: number = 0,
  public replace: boolean = false,
  public params: (RulesParamEntryT)[] = [],
  public paramsRev: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const params = RulesParamUpdate.createParamsVector(builder, builder.createObjectOffsetList(this.params));

  return RulesParamUpdate.createRulesParamUpdate(builder,
    this.scope,
    this.id,
    this.replace,
    params,
    this.paramsRev
  );
}
}
