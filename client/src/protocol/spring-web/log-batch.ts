// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { LogEntryMsg, LogEntryMsgT } from '../spring-web/log-entry-msg.js';


/**
 * Log server → subscriber: streamed batch of log entries.
 */
export class LogBatch implements flatbuffers.IUnpackableObject<LogBatchT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LogBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLogBatch(bb:flatbuffers.ByteBuffer, obj?:LogBatch):LogBatch {
  return (obj || new LogBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLogBatch(bb:flatbuffers.ByteBuffer, obj?:LogBatch):LogBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LogBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

roomId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

entries(index: number, obj?:LogEntryMsg):LogEntryMsg|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new LogEntryMsg()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

entriesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

latestId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startLogBatch(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addRoomId(builder:flatbuffers.Builder, roomId:number) {
  builder.addFieldInt32(0, roomId, 0);
}

static addEntries(builder:flatbuffers.Builder, entriesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, entriesOffset, 0);
}

static createEntriesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEntriesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLatestId(builder:flatbuffers.Builder, latestId:bigint) {
  builder.addFieldInt64(2, latestId, BigInt('0'));
}

static endLogBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createLogBatch(builder:flatbuffers.Builder, roomId:number, entriesOffset:flatbuffers.Offset, latestId:bigint):flatbuffers.Offset {
  LogBatch.startLogBatch(builder);
  LogBatch.addRoomId(builder, roomId);
  LogBatch.addEntries(builder, entriesOffset);
  LogBatch.addLatestId(builder, latestId);
  return LogBatch.endLogBatch(builder);
}

unpack(): LogBatchT {
  return new LogBatchT(
    this.roomId(),
    this.bb!.createObjList<LogEntryMsg, LogEntryMsgT>(this.entries.bind(this), this.entriesLength()),
    this.latestId()
  );
}


unpackTo(_o: LogBatchT): void {
  _o.roomId = this.roomId();
  _o.entries = this.bb!.createObjList<LogEntryMsg, LogEntryMsgT>(this.entries.bind(this), this.entriesLength());
  _o.latestId = this.latestId();
}
}

export class LogBatchT implements flatbuffers.IGeneratedObject {
constructor(
  public roomId: number = 0,
  public entries: (LogEntryMsgT)[] = [],
  public latestId: bigint = BigInt('0')
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const entries = LogBatch.createEntriesVector(builder, builder.createObjectOffsetList(this.entries));

  return LogBatch.createLogBatch(builder,
    this.roomId,
    entries,
    this.latestId
  );
}
}
