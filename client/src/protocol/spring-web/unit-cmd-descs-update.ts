// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitCmdDescs, UnitCmdDescsT } from '../spring-web/unit-cmd-descs.js';


/**
 * Per-session broadcast: every own-team unit's available command list,
 * filtered to build commands. Sent at low cadence (~1 Hz). Client treats
 * each snapshot as a complete replacement of its cmd-desc cache.
 */
export class UnitCmdDescsUpdate implements flatbuffers.IUnpackableObject<UnitCmdDescsUpdateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitCmdDescsUpdate {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitCmdDescsUpdate(bb:flatbuffers.ByteBuffer, obj?:UnitCmdDescsUpdate):UnitCmdDescsUpdate {
  return (obj || new UnitCmdDescsUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitCmdDescsUpdate(bb:flatbuffers.ByteBuffer, obj?:UnitCmdDescsUpdate):UnitCmdDescsUpdate {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitCmdDescsUpdate()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

units(index: number, obj?:UnitCmdDescs):UnitCmdDescs|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new UnitCmdDescs()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

unitsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUnitCmdDescsUpdate(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addUnits(builder:flatbuffers.Builder, unitsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, unitsOffset, 0);
}

static createUnitsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startUnitsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitCmdDescsUpdate(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitCmdDescsUpdate(builder:flatbuffers.Builder, unitsOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitCmdDescsUpdate.startUnitCmdDescsUpdate(builder);
  UnitCmdDescsUpdate.addUnits(builder, unitsOffset);
  return UnitCmdDescsUpdate.endUnitCmdDescsUpdate(builder);
}

unpack(): UnitCmdDescsUpdateT {
  return new UnitCmdDescsUpdateT(
    this.bb!.createObjList<UnitCmdDescs, UnitCmdDescsT>(this.units.bind(this), this.unitsLength())
  );
}


unpackTo(_o: UnitCmdDescsUpdateT): void {
  _o.units = this.bb!.createObjList<UnitCmdDescs, UnitCmdDescsT>(this.units.bind(this), this.unitsLength());
}
}

export class UnitCmdDescsUpdateT implements flatbuffers.IGeneratedObject {
constructor(
  public units: (UnitCmdDescsT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const units = UnitCmdDescsUpdate.createUnitsVector(builder, builder.createObjectOffsetList(this.units));

  return UnitCmdDescsUpdate.createUnitCmdDescsUpdate(builder,
    units
  );
}
}
