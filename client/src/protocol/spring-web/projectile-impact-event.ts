// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ProjectileImpactKind } from '../spring-web/projectile-impact-kind.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * Projectile death event. Sent exactly once per projectile, when it
 * collides, expires, or is intercepted. Triggers explosion VFX +
 * removes the local projectile entry.
 */
export class ProjectileImpactEvent implements flatbuffers.IUnpackableObject<ProjectileImpactEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ProjectileImpactEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProjectileImpactEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileImpactEvent):ProjectileImpactEvent {
  return (obj || new ProjectileImpactEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProjectileImpactEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileImpactEvent):ProjectileImpactEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ProjectileImpactEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

pos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

impactKind():ProjectileImpactKind {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ProjectileImpactKind.Terrain;
}

targetId():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Owner-team id — used by the per-session LOS filter so a client
 * always sees impacts from its own/allied projectiles, even if the
 * impact happens in fog of war.
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startProjectileImpactEvent(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addProjId(builder:flatbuffers.Builder, projId:number) {
  builder.addFieldInt32(0, projId, 0);
}

static addPos(builder:flatbuffers.Builder, posOffset:flatbuffers.Offset) {
  builder.addFieldStruct(1, posOffset, 0);
}

static addImpactKind(builder:flatbuffers.Builder, impactKind:ProjectileImpactKind) {
  builder.addFieldInt8(2, impactKind, ProjectileImpactKind.Terrain);
}

static addTargetId(builder:flatbuffers.Builder, targetId:number) {
  builder.addFieldInt32(3, targetId, 0);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(4, team, 0);
}

static endProjectileImpactEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): ProjectileImpactEventT {
  return new ProjectileImpactEventT(
    this.projId(),
    (this.pos() !== null ? this.pos()!.unpack() : null),
    this.impactKind(),
    this.targetId(),
    this.team()
  );
}


unpackTo(_o: ProjectileImpactEventT): void {
  _o.projId = this.projId();
  _o.pos = (this.pos() !== null ? this.pos()!.unpack() : null);
  _o.impactKind = this.impactKind();
  _o.targetId = this.targetId();
  _o.team = this.team();
}
}

export class ProjectileImpactEventT implements flatbuffers.IGeneratedObject {
constructor(
  public projId: number = 0,
  public pos: Vec3T|null = null,
  public impactKind: ProjectileImpactKind = ProjectileImpactKind.Terrain,
  public targetId: number = 0,
  public team: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  ProjectileImpactEvent.startProjectileImpactEvent(builder);
  ProjectileImpactEvent.addProjId(builder, this.projId);
  ProjectileImpactEvent.addPos(builder, (this.pos !== null ? this.pos!.pack(builder) : 0));
  ProjectileImpactEvent.addImpactKind(builder, this.impactKind);
  ProjectileImpactEvent.addTargetId(builder, this.targetId);
  ProjectileImpactEvent.addTeam(builder, this.team);

  return ProjectileImpactEvent.endProjectileImpactEvent(builder);
}
}
