// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { StandingOrderInfo, StandingOrderInfoT } from '../spring-web/standing-order-info.js';


/**
 * Server → Client: snapshot of all standing orders the client is
 * allowed to see. Filtering rules:
 *   - Always include orders owned by the client's team.
 *   - Include allied-team orders (allies see each other's plans).
 *   - Enemy orders are never sent.
 * Sent on any standing-order state change (create / update / remove /
 * assignment-count change), NOT per-tick. The assigned_squad_count
 * field on each StandingOrderInfo updates only when a squad is added
 * or released — squad churn under a stable count does not push a
 * snapshot.
 */
export class StandingOrderState implements flatbuffers.IUnpackableObject<StandingOrderStateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):StandingOrderState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsStandingOrderState(bb:flatbuffers.ByteBuffer, obj?:StandingOrderState):StandingOrderState {
  return (obj || new StandingOrderState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsStandingOrderState(bb:flatbuffers.ByteBuffer, obj?:StandingOrderState):StandingOrderState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new StandingOrderState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

orders(index: number, obj?:StandingOrderInfo):StandingOrderInfo|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new StandingOrderInfo()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

ordersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startStandingOrderState(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addOrders(builder:flatbuffers.Builder, ordersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, ordersOffset, 0);
}

static createOrdersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startOrdersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endStandingOrderState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createStandingOrderState(builder:flatbuffers.Builder, ordersOffset:flatbuffers.Offset):flatbuffers.Offset {
  StandingOrderState.startStandingOrderState(builder);
  StandingOrderState.addOrders(builder, ordersOffset);
  return StandingOrderState.endStandingOrderState(builder);
}

unpack(): StandingOrderStateT {
  return new StandingOrderStateT(
    this.bb!.createObjList<StandingOrderInfo, StandingOrderInfoT>(this.orders.bind(this), this.ordersLength())
  );
}


unpackTo(_o: StandingOrderStateT): void {
  _o.orders = this.bb!.createObjList<StandingOrderInfo, StandingOrderInfoT>(this.orders.bind(this), this.ordersLength());
}
}

export class StandingOrderStateT implements flatbuffers.IGeneratedObject {
constructor(
  public orders: (StandingOrderInfoT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const orders = StandingOrderState.createOrdersVector(builder, builder.createObjectOffsetList(this.orders));

  return StandingOrderState.createStandingOrderState(builder,
    orders
  );
}
}
