// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Water rendering properties. Spring's water system is also used for
 * lava/acid/void-fill — the colour and damage distinguish them.
 * Water plane is at world Y = 0; terrain below that is flooded.
 */
export class MapWater implements flatbuffers.IUnpackableObject<MapWaterT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MapWater {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMapWater(bb:flatbuffers.ByteBuffer, obj?:MapWater):MapWater {
  return (obj || new MapWater()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMapWater(bb:flatbuffers.ByteBuffer, obj?:MapWater):MapWater {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MapWater()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

baseColor(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

baseColorLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

baseColorArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

surfaceColor(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

surfaceColorLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

surfaceColorArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

minColor(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

minColorLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

minColorArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

surfaceAlpha():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

damage():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

voidWater():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startMapWater(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addBaseColor(builder:flatbuffers.Builder, baseColorOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, baseColorOffset, 0);
}

static createBaseColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createBaseColorVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createBaseColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startBaseColorVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSurfaceColor(builder:flatbuffers.Builder, surfaceColorOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, surfaceColorOffset, 0);
}

static createSurfaceColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createSurfaceColorVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createSurfaceColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startSurfaceColorVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addMinColor(builder:flatbuffers.Builder, minColorOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, minColorOffset, 0);
}

static createMinColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createMinColorVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createMinColorVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startMinColorVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addSurfaceAlpha(builder:flatbuffers.Builder, surfaceAlpha:number) {
  builder.addFieldFloat32(3, surfaceAlpha, 0.0);
}

static addDamage(builder:flatbuffers.Builder, damage:number) {
  builder.addFieldFloat32(4, damage, 0.0);
}

static addVoidWater(builder:flatbuffers.Builder, voidWater:boolean) {
  builder.addFieldInt8(5, +voidWater, +false);
}

static endMapWater(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMapWater(builder:flatbuffers.Builder, baseColorOffset:flatbuffers.Offset, surfaceColorOffset:flatbuffers.Offset, minColorOffset:flatbuffers.Offset, surfaceAlpha:number, damage:number, voidWater:boolean):flatbuffers.Offset {
  MapWater.startMapWater(builder);
  MapWater.addBaseColor(builder, baseColorOffset);
  MapWater.addSurfaceColor(builder, surfaceColorOffset);
  MapWater.addMinColor(builder, minColorOffset);
  MapWater.addSurfaceAlpha(builder, surfaceAlpha);
  MapWater.addDamage(builder, damage);
  MapWater.addVoidWater(builder, voidWater);
  return MapWater.endMapWater(builder);
}

unpack(): MapWaterT {
  return new MapWaterT(
    this.bb!.createScalarList<number>(this.baseColor.bind(this), this.baseColorLength()),
    this.bb!.createScalarList<number>(this.surfaceColor.bind(this), this.surfaceColorLength()),
    this.bb!.createScalarList<number>(this.minColor.bind(this), this.minColorLength()),
    this.surfaceAlpha(),
    this.damage(),
    this.voidWater()
  );
}


unpackTo(_o: MapWaterT): void {
  _o.baseColor = this.bb!.createScalarList<number>(this.baseColor.bind(this), this.baseColorLength());
  _o.surfaceColor = this.bb!.createScalarList<number>(this.surfaceColor.bind(this), this.surfaceColorLength());
  _o.minColor = this.bb!.createScalarList<number>(this.minColor.bind(this), this.minColorLength());
  _o.surfaceAlpha = this.surfaceAlpha();
  _o.damage = this.damage();
  _o.voidWater = this.voidWater();
}
}

export class MapWaterT implements flatbuffers.IGeneratedObject {
constructor(
  public baseColor: (number)[] = [],
  public surfaceColor: (number)[] = [],
  public minColor: (number)[] = [],
  public surfaceAlpha: number = 0.0,
  public damage: number = 0.0,
  public voidWater: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const baseColor = MapWater.createBaseColorVector(builder, this.baseColor);
  const surfaceColor = MapWater.createSurfaceColorVector(builder, this.surfaceColor);
  const minColor = MapWater.createMinColorVector(builder, this.minColor);

  return MapWater.createMapWater(builder,
    baseColor,
    surfaceColor,
    minColor,
    this.surfaceAlpha,
    this.damage,
    this.voidWater
  );
}
}
