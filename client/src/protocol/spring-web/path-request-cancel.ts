// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Client → Server: abort a pending request. Sent on `widget:Shutdown`
 * or when a widget no longer cares about the result (e.g. the player
 * cancelled the build queue that triggered the path preview). Server
 * drops the request if it's still pending, ignores it otherwise.
 * Abandoned requests age out after 5 seconds server-side regardless.
 */
export class PathRequestCancel implements flatbuffers.IUnpackableObject<PathRequestCancelT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):PathRequestCancel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsPathRequestCancel(bb:flatbuffers.ByteBuffer, obj?:PathRequestCancel):PathRequestCancel {
  return (obj || new PathRequestCancel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsPathRequestCancel(bb:flatbuffers.ByteBuffer, obj?:PathRequestCancel):PathRequestCancel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new PathRequestCancel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startPathRequestCancel(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addRequestId(builder:flatbuffers.Builder, requestId:number) {
  builder.addFieldInt32(0, requestId, 0);
}

static endPathRequestCancel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createPathRequestCancel(builder:flatbuffers.Builder, requestId:number):flatbuffers.Offset {
  PathRequestCancel.startPathRequestCancel(builder);
  PathRequestCancel.addRequestId(builder, requestId);
  return PathRequestCancel.endPathRequestCancel(builder);
}

unpack(): PathRequestCancelT {
  return new PathRequestCancelT(
    this.requestId()
  );
}


unpackTo(_o: PathRequestCancelT): void {
  _o.requestId = this.requestId();
}
}

export class PathRequestCancelT implements flatbuffers.IGeneratedObject {
constructor(
  public requestId: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return PathRequestCancel.createPathRequestCancel(builder,
    this.requestId
  );
}
}
