// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { DirectiveInfo, DirectiveInfoT } from '../spring-web/directive-info.js';


export class DirectiveState implements flatbuffers.IUnpackableObject<DirectiveStateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DirectiveState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDirectiveState(bb:flatbuffers.ByteBuffer, obj?:DirectiveState):DirectiveState {
  return (obj || new DirectiveState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDirectiveState(bb:flatbuffers.ByteBuffer, obj?:DirectiveState):DirectiveState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DirectiveState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

directives(index: number, obj?:DirectiveInfo):DirectiveInfo|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new DirectiveInfo()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

directivesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startDirectiveState(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addDirectives(builder:flatbuffers.Builder, directivesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, directivesOffset, 0);
}

static createDirectivesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDirectivesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endDirectiveState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createDirectiveState(builder:flatbuffers.Builder, directivesOffset:flatbuffers.Offset):flatbuffers.Offset {
  DirectiveState.startDirectiveState(builder);
  DirectiveState.addDirectives(builder, directivesOffset);
  return DirectiveState.endDirectiveState(builder);
}

unpack(): DirectiveStateT {
  return new DirectiveStateT(
    this.bb!.createObjList<DirectiveInfo, DirectiveInfoT>(this.directives.bind(this), this.directivesLength())
  );
}


unpackTo(_o: DirectiveStateT): void {
  _o.directives = this.bb!.createObjList<DirectiveInfo, DirectiveInfoT>(this.directives.bind(this), this.directivesLength());
}
}

export class DirectiveStateT implements flatbuffers.IGeneratedObject {
constructor(
  public directives: (DirectiveInfoT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const directives = DirectiveState.createDirectivesVector(builder, builder.createObjectOffsetList(this.directives));

  return DirectiveState.createDirectiveState(builder,
    directives
  );
}
}
