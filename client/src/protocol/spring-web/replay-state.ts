// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Replay server → every attached spectator, on change and on a slow
 * heartbeat. This is what a seek bar is drawn from; a live game never
 * sends it, so a client that never receives one shows no playback UI.
 */
export class ReplayState implements flatbuffers.IUnpackableObject<ReplayStateT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ReplayState {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsReplayState(bb:flatbuffers.ByteBuffer, obj?:ReplayState):ReplayState {
  return (obj || new ReplayState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsReplayState(bb:flatbuffers.ByteBuffer, obj?:ReplayState):ReplayState {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ReplayState()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

startFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

endFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

currentFrame():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

paused():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

speed():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

seeking():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

seekTarget():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

/**
 * Player number of the spectator holding the controls, or -1 if
 * nobody is attached. Compare against your own AuthResponse
 * `player_num` to know whether the buttons are yours.
 */
controllerPlayerNum():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

/**
 * Seek-bar ticks: frames the recording carries a checkpoint at.
 * Empty until PLAN-persistence's sim serializer lands, which is also
 * why a backward seek is refused rather than served (see
 * ReplayControlDeck.h).
 */
checkpointFrames(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

checkpointFramesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

checkpointFramesArray():Int32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? new Int32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

truncated():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

gameId():string|null
gameId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
gameId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

mapId():string|null
mapId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
mapId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * This client's own POV: -1 = global view, else the team whose fog
 * it is watching. Per-client, unlike everything above it.
 */
povTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startReplayState(builder:flatbuffers.Builder) {
  builder.startObject(13);
}

static addStartFrame(builder:flatbuffers.Builder, startFrame:number) {
  builder.addFieldInt32(0, startFrame, 0);
}

static addEndFrame(builder:flatbuffers.Builder, endFrame:number) {
  builder.addFieldInt32(1, endFrame, 0);
}

static addCurrentFrame(builder:flatbuffers.Builder, currentFrame:number) {
  builder.addFieldInt32(2, currentFrame, 0);
}

static addPaused(builder:flatbuffers.Builder, paused:boolean) {
  builder.addFieldInt8(3, +paused, +false);
}

static addSpeed(builder:flatbuffers.Builder, speed:number) {
  builder.addFieldFloat32(4, speed, 0.0);
}

static addSeeking(builder:flatbuffers.Builder, seeking:boolean) {
  builder.addFieldInt8(5, +seeking, +false);
}

static addSeekTarget(builder:flatbuffers.Builder, seekTarget:number) {
  builder.addFieldInt32(6, seekTarget, 0);
}

static addControllerPlayerNum(builder:flatbuffers.Builder, controllerPlayerNum:number) {
  builder.addFieldInt32(7, controllerPlayerNum, 0);
}

static addCheckpointFrames(builder:flatbuffers.Builder, checkpointFramesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, checkpointFramesOffset, 0);
}

static createCheckpointFramesVector(builder:flatbuffers.Builder, data:number[]|Int32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createCheckpointFramesVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createCheckpointFramesVector(builder:flatbuffers.Builder, data:number[]|Int32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt32(data[i]!);
  }
  return builder.endVector();
}

static startCheckpointFramesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTruncated(builder:flatbuffers.Builder, truncated:boolean) {
  builder.addFieldInt8(9, +truncated, +false);
}

static addGameId(builder:flatbuffers.Builder, gameIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(10, gameIdOffset, 0);
}

static addMapId(builder:flatbuffers.Builder, mapIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(11, mapIdOffset, 0);
}

static addPovTeam(builder:flatbuffers.Builder, povTeam:number) {
  builder.addFieldInt32(12, povTeam, 0);
}

static endReplayState(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createReplayState(builder:flatbuffers.Builder, startFrame:number, endFrame:number, currentFrame:number, paused:boolean, speed:number, seeking:boolean, seekTarget:number, controllerPlayerNum:number, checkpointFramesOffset:flatbuffers.Offset, truncated:boolean, gameIdOffset:flatbuffers.Offset, mapIdOffset:flatbuffers.Offset, povTeam:number):flatbuffers.Offset {
  ReplayState.startReplayState(builder);
  ReplayState.addStartFrame(builder, startFrame);
  ReplayState.addEndFrame(builder, endFrame);
  ReplayState.addCurrentFrame(builder, currentFrame);
  ReplayState.addPaused(builder, paused);
  ReplayState.addSpeed(builder, speed);
  ReplayState.addSeeking(builder, seeking);
  ReplayState.addSeekTarget(builder, seekTarget);
  ReplayState.addControllerPlayerNum(builder, controllerPlayerNum);
  ReplayState.addCheckpointFrames(builder, checkpointFramesOffset);
  ReplayState.addTruncated(builder, truncated);
  ReplayState.addGameId(builder, gameIdOffset);
  ReplayState.addMapId(builder, mapIdOffset);
  ReplayState.addPovTeam(builder, povTeam);
  return ReplayState.endReplayState(builder);
}

unpack(): ReplayStateT {
  return new ReplayStateT(
    this.startFrame(),
    this.endFrame(),
    this.currentFrame(),
    this.paused(),
    this.speed(),
    this.seeking(),
    this.seekTarget(),
    this.controllerPlayerNum(),
    this.bb!.createScalarList<number>(this.checkpointFrames.bind(this), this.checkpointFramesLength()),
    this.truncated(),
    this.gameId(),
    this.mapId(),
    this.povTeam()
  );
}


unpackTo(_o: ReplayStateT): void {
  _o.startFrame = this.startFrame();
  _o.endFrame = this.endFrame();
  _o.currentFrame = this.currentFrame();
  _o.paused = this.paused();
  _o.speed = this.speed();
  _o.seeking = this.seeking();
  _o.seekTarget = this.seekTarget();
  _o.controllerPlayerNum = this.controllerPlayerNum();
  _o.checkpointFrames = this.bb!.createScalarList<number>(this.checkpointFrames.bind(this), this.checkpointFramesLength());
  _o.truncated = this.truncated();
  _o.gameId = this.gameId();
  _o.mapId = this.mapId();
  _o.povTeam = this.povTeam();
}
}

export class ReplayStateT implements flatbuffers.IGeneratedObject {
constructor(
  public startFrame: number = 0,
  public endFrame: number = 0,
  public currentFrame: number = 0,
  public paused: boolean = false,
  public speed: number = 0.0,
  public seeking: boolean = false,
  public seekTarget: number = 0,
  public controllerPlayerNum: number = 0,
  public checkpointFrames: (number)[] = [],
  public truncated: boolean = false,
  public gameId: string|Uint8Array|null = null,
  public mapId: string|Uint8Array|null = null,
  public povTeam: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const checkpointFrames = ReplayState.createCheckpointFramesVector(builder, this.checkpointFrames);
  const gameId = (this.gameId !== null ? builder.createString(this.gameId!) : 0);
  const mapId = (this.mapId !== null ? builder.createString(this.mapId!) : 0);

  return ReplayState.createReplayState(builder,
    this.startFrame,
    this.endFrame,
    this.currentFrame,
    this.paused,
    this.speed,
    this.seeking,
    this.seekTarget,
    this.controllerPlayerNum,
    checkpointFrames,
    this.truncated,
    gameId,
    mapId,
    this.povTeam
  );
}
}
