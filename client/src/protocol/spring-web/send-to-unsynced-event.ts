// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SendToUnsyncedArg, SendToUnsyncedArgT } from '../spring-web/send-to-unsynced-arg.js';


/**
 * Server → Client: forwarded `Spring.SendToUnsynced(...)` call from a
 * synced LuaRules gadget. The first arg is conventionally the topic
 * string (by ZK convention and matching upstream
 * `CUnsyncedLuaHandle::RecvFromSynced` semantics); the worker peels
 * args[0] and looks up the handler registered via
 * `gadgetHandler:AddSyncAction(topic, fn)`. `client_id == 0` is
 * broadcast to every client (today the only case — the synced call
 * has no per-player addressing).
 */
export class SendToUnsyncedEvent implements flatbuffers.IUnpackableObject<SendToUnsyncedEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SendToUnsyncedEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSendToUnsyncedEvent(bb:flatbuffers.ByteBuffer, obj?:SendToUnsyncedEvent):SendToUnsyncedEvent {
  return (obj || new SendToUnsyncedEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSendToUnsyncedEvent(bb:flatbuffers.ByteBuffer, obj?:SendToUnsyncedEvent):SendToUnsyncedEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SendToUnsyncedEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

clientId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

args(index: number, obj?:SendToUnsyncedArg):SendToUnsyncedArg|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new SendToUnsyncedArg()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

argsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startSendToUnsyncedEvent(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addClientId(builder:flatbuffers.Builder, clientId:number) {
  builder.addFieldInt32(0, clientId, 0);
}

static addArgs(builder:flatbuffers.Builder, argsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, argsOffset, 0);
}

static createArgsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startArgsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endSendToUnsyncedEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSendToUnsyncedEvent(builder:flatbuffers.Builder, clientId:number, argsOffset:flatbuffers.Offset):flatbuffers.Offset {
  SendToUnsyncedEvent.startSendToUnsyncedEvent(builder);
  SendToUnsyncedEvent.addClientId(builder, clientId);
  SendToUnsyncedEvent.addArgs(builder, argsOffset);
  return SendToUnsyncedEvent.endSendToUnsyncedEvent(builder);
}

unpack(): SendToUnsyncedEventT {
  return new SendToUnsyncedEventT(
    this.clientId(),
    this.bb!.createObjList<SendToUnsyncedArg, SendToUnsyncedArgT>(this.args.bind(this), this.argsLength())
  );
}


unpackTo(_o: SendToUnsyncedEventT): void {
  _o.clientId = this.clientId();
  _o.args = this.bb!.createObjList<SendToUnsyncedArg, SendToUnsyncedArgT>(this.args.bind(this), this.argsLength());
}
}

export class SendToUnsyncedEventT implements flatbuffers.IGeneratedObject {
constructor(
  public clientId: number = 0,
  public args: (SendToUnsyncedArgT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const args = SendToUnsyncedEvent.createArgsVector(builder, builder.createObjectOffsetList(this.args));

  return SendToUnsyncedEvent.createSendToUnsyncedEvent(builder,
    this.clientId,
    args
  );
}
}
