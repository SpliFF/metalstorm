// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Convert from spectator to active player. Spectators can send this
 * to request joining the game as a rostered player (assigned to a team).
 * If the room is full (non-spectator player count >= maxPlayers), the
 * request is rejected. If accepted, the spectator transitions to a
 * player with team assignment (auto-assigned or specified by team field),
 * and the room broadcasts the state change via RoomStateUpdate.
 */
export class RoomEnlist implements flatbuffers.IUnpackableObject<RoomEnlistT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RoomEnlist {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRoomEnlist(bb:flatbuffers.ByteBuffer, obj?:RoomEnlist):RoomEnlist {
  return (obj || new RoomEnlist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRoomEnlist(bb:flatbuffers.ByteBuffer, obj?:RoomEnlist):RoomEnlist {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RoomEnlist()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

team():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 255;
}

static startRoomEnlist(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(0, team, 255);
}

static endRoomEnlist(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRoomEnlist(builder:flatbuffers.Builder, team:number):flatbuffers.Offset {
  RoomEnlist.startRoomEnlist(builder);
  RoomEnlist.addTeam(builder, team);
  return RoomEnlist.endRoomEnlist(builder);
}

unpack(): RoomEnlistT {
  return new RoomEnlistT(
    this.team()
  );
}


unpackTo(_o: RoomEnlistT): void {
  _o.team = this.team();
}
}

export class RoomEnlistT implements flatbuffers.IGeneratedObject {
constructor(
  public team: number = 255
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return RoomEnlist.createRoomEnlist(builder,
    this.team
  );
}
}
