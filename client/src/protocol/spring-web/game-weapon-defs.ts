// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameWeaponDef, GameWeaponDefT } from '../spring-web/game-weapon-def.js';


/**
 * Sent by the game server after successful auth, alongside
 * GameUnitDefs. Contains every weapon type the game defines so
 * the client can render projectiles with correct visuals.
 */
export class GameWeaponDefs implements flatbuffers.IUnpackableObject<GameWeaponDefsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameWeaponDefs {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameWeaponDefs(bb:flatbuffers.ByteBuffer, obj?:GameWeaponDefs):GameWeaponDefs {
  return (obj || new GameWeaponDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameWeaponDefs(bb:flatbuffers.ByteBuffer, obj?:GameWeaponDefs):GameWeaponDefs {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameWeaponDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

defs(index: number, obj?:GameWeaponDef):GameWeaponDef|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GameWeaponDef()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

defsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startGameWeaponDefs(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, defsOffset, 0);
}

static createDefsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDefsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endGameWeaponDefs(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameWeaponDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset):flatbuffers.Offset {
  GameWeaponDefs.startGameWeaponDefs(builder);
  GameWeaponDefs.addDefs(builder, defsOffset);
  return GameWeaponDefs.endGameWeaponDefs(builder);
}

unpack(): GameWeaponDefsT {
  return new GameWeaponDefsT(
    this.bb!.createObjList<GameWeaponDef, GameWeaponDefT>(this.defs.bind(this), this.defsLength())
  );
}


unpackTo(_o: GameWeaponDefsT): void {
  _o.defs = this.bb!.createObjList<GameWeaponDef, GameWeaponDefT>(this.defs.bind(this), this.defsLength());
}
}

export class GameWeaponDefsT implements flatbuffers.IGeneratedObject {
constructor(
  public defs: (GameWeaponDefT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const defs = GameWeaponDefs.createDefsVector(builder, builder.createObjectOffsetList(this.defs));

  return GameWeaponDefs.createGameWeaponDefs(builder,
    defs
  );
}
}
