// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GameUnitDef, GameUnitDefT } from '../spring-web/game-unit-def.js';


/**
 * Sent by the game server after successful auth, before entity state
 * streaming begins. Contains every unit type the game defines so the
 * client can preload models.
 */
export class GameUnitDefs implements flatbuffers.IUnpackableObject<GameUnitDefsT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GameUnitDefs {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGameUnitDefs(bb:flatbuffers.ByteBuffer, obj?:GameUnitDefs):GameUnitDefs {
  return (obj || new GameUnitDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGameUnitDefs(bb:flatbuffers.ByteBuffer, obj?:GameUnitDefs):GameUnitDefs {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GameUnitDefs()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

defs(index: number, obj?:GameUnitDef):GameUnitDef|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new GameUnitDef()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

defsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Base URL for model/texture paths (e.g. "/api/content/assets/").
 * Client prepends this to each def's model_url/texture_url.
 */
baseUrl():string|null
baseUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
baseUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startGameUnitDefs(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, defsOffset, 0);
}

static createDefsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startDefsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addBaseUrl(builder:flatbuffers.Builder, baseUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, baseUrlOffset, 0);
}

static endGameUnitDefs(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createGameUnitDefs(builder:flatbuffers.Builder, defsOffset:flatbuffers.Offset, baseUrlOffset:flatbuffers.Offset):flatbuffers.Offset {
  GameUnitDefs.startGameUnitDefs(builder);
  GameUnitDefs.addDefs(builder, defsOffset);
  GameUnitDefs.addBaseUrl(builder, baseUrlOffset);
  return GameUnitDefs.endGameUnitDefs(builder);
}

unpack(): GameUnitDefsT {
  return new GameUnitDefsT(
    this.bb!.createObjList<GameUnitDef, GameUnitDefT>(this.defs.bind(this), this.defsLength()),
    this.baseUrl()
  );
}


unpackTo(_o: GameUnitDefsT): void {
  _o.defs = this.bb!.createObjList<GameUnitDef, GameUnitDefT>(this.defs.bind(this), this.defsLength());
  _o.baseUrl = this.baseUrl();
}
}

export class GameUnitDefsT implements flatbuffers.IGeneratedObject {
constructor(
  public defs: (GameUnitDefT)[] = [],
  public baseUrl: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const defs = GameUnitDefs.createDefsVector(builder, builder.createObjectOffsetList(this.defs));
  const baseUrl = (this.baseUrl !== null ? builder.createString(this.baseUrl!) : 0);

  return GameUnitDefs.createGameUnitDefs(builder,
    defs,
    baseUrl
  );
}
}
