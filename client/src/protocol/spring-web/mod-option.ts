// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One game modoption (always a string key→value, exactly as the lobby
 * passed it to the game server via `--modoption key=value` and as the
 * synced side stores it in `CGameSetup::GetModOptions`). Values stay
 * strings on the client too — Recoil's `Spring.GetModOptions()` pushes
 * `lua_pushsstring(value)` and callers `tonumber()` numeric options
 * themselves (LuaSyncedRead.cpp `PushAllOptions`).
 */
export class ModOption implements flatbuffers.IUnpackableObject<ModOptionT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ModOption {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsModOption(bb:flatbuffers.ByteBuffer, obj?:ModOption):ModOption {
  return (obj || new ModOption()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsModOption(bb:flatbuffers.ByteBuffer, obj?:ModOption):ModOption {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ModOption()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

key():string|null
key(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
key(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

value():string|null
value(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
value(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startModOption(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addKey(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, keyOffset, 0);
}

static addValue(builder:flatbuffers.Builder, valueOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, valueOffset, 0);
}

static endModOption(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createModOption(builder:flatbuffers.Builder, keyOffset:flatbuffers.Offset, valueOffset:flatbuffers.Offset):flatbuffers.Offset {
  ModOption.startModOption(builder);
  ModOption.addKey(builder, keyOffset);
  ModOption.addValue(builder, valueOffset);
  return ModOption.endModOption(builder);
}

unpack(): ModOptionT {
  return new ModOptionT(
    this.key(),
    this.value()
  );
}


unpackTo(_o: ModOptionT): void {
  _o.key = this.key();
  _o.value = this.value();
}
}

export class ModOptionT implements flatbuffers.IGeneratedObject {
constructor(
  public key: string|Uint8Array|null = null,
  public value: string|Uint8Array|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const key = (this.key !== null ? builder.createString(this.key!) : 0);
  const value = (this.value !== null ? builder.createString(this.value!) : 0);

  return ModOption.createModOption(builder,
    key,
    value
  );
}
}
