// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * Server → Client: a relayed `Spring.SendLuaUIMsg` from another (or the
 * same) player. The server has already applied the audience filter (the
 * `mode` from the originating `LuaUIMsg`, evaluated against this receiver's
 * team/spectator state per Recoil `CLuaHandle::HandleLuaMsg`), so the client
 * dispatches unconditionally to `widget:RecvLuaMsg(data, player_id)`.
 * `player_id` is the sender's player number (server-resolved).
 */
export class LuaUIMsgRelay implements flatbuffers.IUnpackableObject<LuaUIMsgRelayT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):LuaUIMsgRelay {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsLuaUIMsgRelay(bb:flatbuffers.ByteBuffer, obj?:LuaUIMsgRelay):LuaUIMsgRelay {
  return (obj || new LuaUIMsgRelay()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsLuaUIMsgRelay(bb:flatbuffers.ByteBuffer, obj?:LuaUIMsgRelay):LuaUIMsgRelay {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new LuaUIMsgRelay()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

data(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb!.__vector(this.bb_pos + offset) + index) : 0;
}

dataLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

dataArray():Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? new Uint8Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

playerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startLuaUIMsgRelay(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addData(builder:flatbuffers.Builder, dataOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, dataOffset, 0);
}

static createDataVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset {
  builder.startVector(1, data.length, 1);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addInt8(data[i]!);
  }
  return builder.endVector();
}

static startDataVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(1, numElems, 1);
}

static addPlayerId(builder:flatbuffers.Builder, playerId:number) {
  builder.addFieldInt32(1, playerId, 0);
}

static endLuaUIMsgRelay(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createLuaUIMsgRelay(builder:flatbuffers.Builder, dataOffset:flatbuffers.Offset, playerId:number):flatbuffers.Offset {
  LuaUIMsgRelay.startLuaUIMsgRelay(builder);
  LuaUIMsgRelay.addData(builder, dataOffset);
  LuaUIMsgRelay.addPlayerId(builder, playerId);
  return LuaUIMsgRelay.endLuaUIMsgRelay(builder);
}

unpack(): LuaUIMsgRelayT {
  return new LuaUIMsgRelayT(
    this.bb!.createScalarList<number>(this.data.bind(this), this.dataLength()),
    this.playerId()
  );
}


unpackTo(_o: LuaUIMsgRelayT): void {
  _o.data = this.bb!.createScalarList<number>(this.data.bind(this), this.dataLength());
  _o.playerId = this.playerId();
}
}

export class LuaUIMsgRelayT implements flatbuffers.IGeneratedObject {
constructor(
  public data: (number)[] = [],
  public playerId: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const data = LuaUIMsgRelay.createDataVector(builder, this.data);

  return LuaUIMsgRelay.createLuaUIMsgRelay(builder,
    data,
    this.playerId
  );
}
}
