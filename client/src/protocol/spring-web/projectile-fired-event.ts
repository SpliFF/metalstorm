// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * Projectile creation event. Sent once per projectile, when the server
 * instantiates it. The client uses this to spawn a local visual that
 * runs the same ballistic / hit-scan motion the simulation runs — no
 * per-frame position stream is needed for short-lived projectiles.
 */
export class ProjectileFiredEvent implements flatbuffers.IUnpackableObject<ProjectileFiredEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ProjectileFiredEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProjectileFiredEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileFiredEvent):ProjectileFiredEvent {
  return (obj || new ProjectileFiredEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProjectileFiredEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileFiredEvent):ProjectileFiredEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ProjectileFiredEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

weaponDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

ownerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

team():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

pos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

vel(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

targetPos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

targetId():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

ttl():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : 0;
}

gravity():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

/**
 * Hit-scan flag — when true the client renders a beam from pos→target_pos
 * for one tick (or the weapon's beam duration) and discards.
 */
hitscan():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startProjectileFiredEvent(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addProjId(builder:flatbuffers.Builder, projId:number) {
  builder.addFieldInt32(0, projId, 0);
}

static addWeaponDefId(builder:flatbuffers.Builder, weaponDefId:number) {
  builder.addFieldInt16(1, weaponDefId, 0);
}

static addOwnerId(builder:flatbuffers.Builder, ownerId:number) {
  builder.addFieldInt32(2, ownerId, 0);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(3, team, 0);
}

static addPos(builder:flatbuffers.Builder, posOffset:flatbuffers.Offset) {
  builder.addFieldStruct(4, posOffset, 0);
}

static addVel(builder:flatbuffers.Builder, velOffset:flatbuffers.Offset) {
  builder.addFieldStruct(5, velOffset, 0);
}

static addTargetPos(builder:flatbuffers.Builder, targetPosOffset:flatbuffers.Offset) {
  builder.addFieldStruct(6, targetPosOffset, 0);
}

static addTargetId(builder:flatbuffers.Builder, targetId:number) {
  builder.addFieldInt32(7, targetId, 0);
}

static addTtl(builder:flatbuffers.Builder, ttl:number) {
  builder.addFieldInt16(8, ttl, 0);
}

static addGravity(builder:flatbuffers.Builder, gravity:number) {
  builder.addFieldFloat32(9, gravity, 0.0);
}

static addHitscan(builder:flatbuffers.Builder, hitscan:boolean) {
  builder.addFieldInt8(10, +hitscan, +false);
}

static endProjectileFiredEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): ProjectileFiredEventT {
  return new ProjectileFiredEventT(
    this.projId(),
    this.weaponDefId(),
    this.ownerId(),
    this.team(),
    (this.pos() !== null ? this.pos()!.unpack() : null),
    (this.vel() !== null ? this.vel()!.unpack() : null),
    (this.targetPos() !== null ? this.targetPos()!.unpack() : null),
    this.targetId(),
    this.ttl(),
    this.gravity(),
    this.hitscan()
  );
}


unpackTo(_o: ProjectileFiredEventT): void {
  _o.projId = this.projId();
  _o.weaponDefId = this.weaponDefId();
  _o.ownerId = this.ownerId();
  _o.team = this.team();
  _o.pos = (this.pos() !== null ? this.pos()!.unpack() : null);
  _o.vel = (this.vel() !== null ? this.vel()!.unpack() : null);
  _o.targetPos = (this.targetPos() !== null ? this.targetPos()!.unpack() : null);
  _o.targetId = this.targetId();
  _o.ttl = this.ttl();
  _o.gravity = this.gravity();
  _o.hitscan = this.hitscan();
}
}

export class ProjectileFiredEventT implements flatbuffers.IGeneratedObject {
constructor(
  public projId: number = 0,
  public weaponDefId: number = 0,
  public ownerId: number = 0,
  public team: number = 0,
  public pos: Vec3T|null = null,
  public vel: Vec3T|null = null,
  public targetPos: Vec3T|null = null,
  public targetId: number = 0,
  public ttl: number = 0,
  public gravity: number = 0.0,
  public hitscan: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  ProjectileFiredEvent.startProjectileFiredEvent(builder);
  ProjectileFiredEvent.addProjId(builder, this.projId);
  ProjectileFiredEvent.addWeaponDefId(builder, this.weaponDefId);
  ProjectileFiredEvent.addOwnerId(builder, this.ownerId);
  ProjectileFiredEvent.addTeam(builder, this.team);
  ProjectileFiredEvent.addPos(builder, (this.pos !== null ? this.pos!.pack(builder) : 0));
  ProjectileFiredEvent.addVel(builder, (this.vel !== null ? this.vel!.pack(builder) : 0));
  ProjectileFiredEvent.addTargetPos(builder, (this.targetPos !== null ? this.targetPos!.pack(builder) : 0));
  ProjectileFiredEvent.addTargetId(builder, this.targetId);
  ProjectileFiredEvent.addTtl(builder, this.ttl);
  ProjectileFiredEvent.addGravity(builder, this.gravity);
  ProjectileFiredEvent.addHitscan(builder, this.hitscan);

  return ProjectileFiredEvent.endProjectileFiredEvent(builder);
}
}
