// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { RoomState } from '../spring-web/room-state.js';


export class RoomListEntry {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RoomListEntry {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRoomListEntry(bb:flatbuffers.ByteBuffer, obj?:RoomListEntry):RoomListEntry {
  return (obj || new RoomListEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRoomListEntry(bb:flatbuffers.ByteBuffer, obj?:RoomListEntry):RoomListEntry {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RoomListEntry()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

roomId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

mapName():string|null
mapName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
mapName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

gameName():string|null
gameName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
gameName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

state():RoomState {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : RoomState.Configuring;
}

playerCount():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

maxPlayers():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

hasPassword():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

hostName():string|null
hostName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
hostName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startRoomListEntry(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addRoomId(builder:flatbuffers.Builder, roomId:number) {
  builder.addFieldInt32(0, roomId, 0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addMapName(builder:flatbuffers.Builder, mapNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, mapNameOffset, 0);
}

static addGameName(builder:flatbuffers.Builder, gameNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, gameNameOffset, 0);
}

static addState(builder:flatbuffers.Builder, state:RoomState) {
  builder.addFieldInt8(4, state, RoomState.Configuring);
}

static addPlayerCount(builder:flatbuffers.Builder, playerCount:number) {
  builder.addFieldInt8(5, playerCount, 0);
}

static addMaxPlayers(builder:flatbuffers.Builder, maxPlayers:number) {
  builder.addFieldInt8(6, maxPlayers, 0);
}

static addHasPassword(builder:flatbuffers.Builder, hasPassword:boolean) {
  builder.addFieldInt8(7, +hasPassword, +false);
}

static addHostName(builder:flatbuffers.Builder, hostNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, hostNameOffset, 0);
}

static endRoomListEntry(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRoomListEntry(builder:flatbuffers.Builder, roomId:number, nameOffset:flatbuffers.Offset, mapNameOffset:flatbuffers.Offset, gameNameOffset:flatbuffers.Offset, state:RoomState, playerCount:number, maxPlayers:number, hasPassword:boolean, hostNameOffset:flatbuffers.Offset):flatbuffers.Offset {
  RoomListEntry.startRoomListEntry(builder);
  RoomListEntry.addRoomId(builder, roomId);
  RoomListEntry.addName(builder, nameOffset);
  RoomListEntry.addMapName(builder, mapNameOffset);
  RoomListEntry.addGameName(builder, gameNameOffset);
  RoomListEntry.addState(builder, state);
  RoomListEntry.addPlayerCount(builder, playerCount);
  RoomListEntry.addMaxPlayers(builder, maxPlayers);
  RoomListEntry.addHasPassword(builder, hasPassword);
  RoomListEntry.addHostName(builder, hostNameOffset);
  return RoomListEntry.endRoomListEntry(builder);
}
}
