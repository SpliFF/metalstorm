// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



export class RoomReady implements flatbuffers.IUnpackableObject<RoomReadyT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RoomReady {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRoomReady(bb:flatbuffers.ByteBuffer, obj?:RoomReady):RoomReady {
  return (obj || new RoomReady()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRoomReady(bb:flatbuffers.ByteBuffer, obj?:RoomReady):RoomReady {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RoomReady()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

ready():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startRoomReady(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addReady(builder:flatbuffers.Builder, ready:boolean) {
  builder.addFieldInt8(0, +ready, +false);
}

static endRoomReady(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRoomReady(builder:flatbuffers.Builder, ready:boolean):flatbuffers.Offset {
  RoomReady.startRoomReady(builder);
  RoomReady.addReady(builder, ready);
  return RoomReady.endRoomReady(builder);
}

unpack(): RoomReadyT {
  return new RoomReadyT(
    this.ready()
  );
}


unpackTo(_o: RoomReadyT): void {
  _o.ready = this.ready();
}
}

export class RoomReadyT implements flatbuffers.IGeneratedObject {
constructor(
  public ready: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return RoomReady.createRoomReady(builder,
    this.ready
  );
}
}
