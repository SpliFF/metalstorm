// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitCommandEvent, UnitCommandEventT } from '../spring-web/unit-command-event.js';


/**
 * Per-tick batch of command events. Filtered server-side to the union
 * of every viewer's allied teams — widgets only see commands on units
 * they're allowed to observe (own team + alliance).
 */
export class UnitCommandBatch implements flatbuffers.IUnpackableObject<UnitCommandBatchT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitCommandBatch {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitCommandBatch(bb:flatbuffers.ByteBuffer, obj?:UnitCommandBatch):UnitCommandBatch {
  return (obj || new UnitCommandBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitCommandBatch(bb:flatbuffers.ByteBuffer, obj?:UnitCommandBatch):UnitCommandBatch {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitCommandBatch()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

events(index: number, obj?:UnitCommandEvent):UnitCommandEvent|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new UnitCommandEvent()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

eventsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startUnitCommandBatch(builder:flatbuffers.Builder) {
  builder.startObject(1);
}

static addEvents(builder:flatbuffers.Builder, eventsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, eventsOffset, 0);
}

static createEventsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startEventsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endUnitCommandBatch(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitCommandBatch(builder:flatbuffers.Builder, eventsOffset:flatbuffers.Offset):flatbuffers.Offset {
  UnitCommandBatch.startUnitCommandBatch(builder);
  UnitCommandBatch.addEvents(builder, eventsOffset);
  return UnitCommandBatch.endUnitCommandBatch(builder);
}

unpack(): UnitCommandBatchT {
  return new UnitCommandBatchT(
    this.bb!.createObjList<UnitCommandEvent, UnitCommandEventT>(this.events.bind(this), this.eventsLength())
  );
}


unpackTo(_o: UnitCommandBatchT): void {
  _o.events = this.bb!.createObjList<UnitCommandEvent, UnitCommandEventT>(this.events.bind(this), this.eventsLength());
}
}

export class UnitCommandBatchT implements flatbuffers.IGeneratedObject {
constructor(
  public events: (UnitCommandEventT)[] = []
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const events = UnitCommandBatch.createEventsVector(builder, builder.createObjectOffsetList(this.events));

  return UnitCommandBatch.createUnitCommandBatch(builder,
    events
  );
}
}
