// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { MapStartPos, MapStartPosT } from '../spring-web/map-start-pos.js';


/**
 * Sent by the lobby for map selection UI.
 */
export class MapInfo implements flatbuffers.IUnpackableObject<MapInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):MapInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsMapInfo(bb:flatbuffers.ByteBuffer, obj?:MapInfo):MapInfo {
  return (obj || new MapInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsMapInfo(bb:flatbuffers.ByteBuffer, obj?:MapInfo):MapInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new MapInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

id():string|null
id(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
id(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

shortName():string|null
shortName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
shortName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

description():string|null
description(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
description(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

author():string|null
author(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
author(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

version():string|null
version(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
version(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

mapx():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

mapy():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

widthElmos():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

heightElmos():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

minHeight():number {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

maxHeight():number {
  const offset = this.bb!.__offset(this.bb_pos, 26);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

maxPlayers():number {
  const offset = this.bb!.__offset(this.bb_pos, 28);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

startPositions(index: number, obj?:MapStartPos):MapStartPos|null {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? (obj || new MapStartPos()).__init(this.bb!.__vector(this.bb_pos + offset) + index * 8, this.bb!) : null;
}

startPositionsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 30);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

gravity():number {
  const offset = this.bb!.__offset(this.bb_pos, 32);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

tidalStrength():number {
  const offset = this.bb!.__offset(this.bb_pos, 34);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

maxMetal():number {
  const offset = this.bb!.__offset(this.bb_pos, 36);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

extractorRadius():number {
  const offset = this.bb!.__offset(this.bb_pos, 38);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : 0.0;
}

minimapUrl():string|null
minimapUrl(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
minimapUrl(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 40);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

hasLuaGaia():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 42);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startMapInfo(builder:flatbuffers.Builder) {
  builder.startObject(20);
}

static addId(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, idOffset, 0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addShortName(builder:flatbuffers.Builder, shortNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, shortNameOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, descriptionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, descriptionOffset, 0);
}

static addAuthor(builder:flatbuffers.Builder, authorOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, authorOffset, 0);
}

static addVersion(builder:flatbuffers.Builder, versionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, versionOffset, 0);
}

static addMapx(builder:flatbuffers.Builder, mapx:number) {
  builder.addFieldInt16(6, mapx, 0);
}

static addMapy(builder:flatbuffers.Builder, mapy:number) {
  builder.addFieldInt16(7, mapy, 0);
}

static addWidthElmos(builder:flatbuffers.Builder, widthElmos:number) {
  builder.addFieldInt32(8, widthElmos, 0);
}

static addHeightElmos(builder:flatbuffers.Builder, heightElmos:number) {
  builder.addFieldInt32(9, heightElmos, 0);
}

static addMinHeight(builder:flatbuffers.Builder, minHeight:number) {
  builder.addFieldFloat32(10, minHeight, 0.0);
}

static addMaxHeight(builder:flatbuffers.Builder, maxHeight:number) {
  builder.addFieldFloat32(11, maxHeight, 0.0);
}

static addMaxPlayers(builder:flatbuffers.Builder, maxPlayers:number) {
  builder.addFieldInt8(12, maxPlayers, 0);
}

static addStartPositions(builder:flatbuffers.Builder, startPositionsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(13, startPositionsOffset, 0);
}

static startStartPositionsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(8, numElems, 4);
}

static addGravity(builder:flatbuffers.Builder, gravity:number) {
  builder.addFieldFloat32(14, gravity, 0.0);
}

static addTidalStrength(builder:flatbuffers.Builder, tidalStrength:number) {
  builder.addFieldFloat32(15, tidalStrength, 0.0);
}

static addMaxMetal(builder:flatbuffers.Builder, maxMetal:number) {
  builder.addFieldFloat32(16, maxMetal, 0.0);
}

static addExtractorRadius(builder:flatbuffers.Builder, extractorRadius:number) {
  builder.addFieldFloat32(17, extractorRadius, 0.0);
}

static addMinimapUrl(builder:flatbuffers.Builder, minimapUrlOffset:flatbuffers.Offset) {
  builder.addFieldOffset(18, minimapUrlOffset, 0);
}

static addHasLuaGaia(builder:flatbuffers.Builder, hasLuaGaia:boolean) {
  builder.addFieldInt8(19, +hasLuaGaia, +false);
}

static endMapInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createMapInfo(builder:flatbuffers.Builder, idOffset:flatbuffers.Offset, nameOffset:flatbuffers.Offset, shortNameOffset:flatbuffers.Offset, descriptionOffset:flatbuffers.Offset, authorOffset:flatbuffers.Offset, versionOffset:flatbuffers.Offset, mapx:number, mapy:number, widthElmos:number, heightElmos:number, minHeight:number, maxHeight:number, maxPlayers:number, startPositionsOffset:flatbuffers.Offset, gravity:number, tidalStrength:number, maxMetal:number, extractorRadius:number, minimapUrlOffset:flatbuffers.Offset, hasLuaGaia:boolean):flatbuffers.Offset {
  MapInfo.startMapInfo(builder);
  MapInfo.addId(builder, idOffset);
  MapInfo.addName(builder, nameOffset);
  MapInfo.addShortName(builder, shortNameOffset);
  MapInfo.addDescription(builder, descriptionOffset);
  MapInfo.addAuthor(builder, authorOffset);
  MapInfo.addVersion(builder, versionOffset);
  MapInfo.addMapx(builder, mapx);
  MapInfo.addMapy(builder, mapy);
  MapInfo.addWidthElmos(builder, widthElmos);
  MapInfo.addHeightElmos(builder, heightElmos);
  MapInfo.addMinHeight(builder, minHeight);
  MapInfo.addMaxHeight(builder, maxHeight);
  MapInfo.addMaxPlayers(builder, maxPlayers);
  MapInfo.addStartPositions(builder, startPositionsOffset);
  MapInfo.addGravity(builder, gravity);
  MapInfo.addTidalStrength(builder, tidalStrength);
  MapInfo.addMaxMetal(builder, maxMetal);
  MapInfo.addExtractorRadius(builder, extractorRadius);
  MapInfo.addMinimapUrl(builder, minimapUrlOffset);
  MapInfo.addHasLuaGaia(builder, hasLuaGaia);
  return MapInfo.endMapInfo(builder);
}

unpack(): MapInfoT {
  return new MapInfoT(
    this.id(),
    this.name(),
    this.shortName(),
    this.description(),
    this.author(),
    this.version(),
    this.mapx(),
    this.mapy(),
    this.widthElmos(),
    this.heightElmos(),
    this.minHeight(),
    this.maxHeight(),
    this.maxPlayers(),
    this.bb!.createObjList<MapStartPos, MapStartPosT>(this.startPositions.bind(this), this.startPositionsLength()),
    this.gravity(),
    this.tidalStrength(),
    this.maxMetal(),
    this.extractorRadius(),
    this.minimapUrl(),
    this.hasLuaGaia()
  );
}


unpackTo(_o: MapInfoT): void {
  _o.id = this.id();
  _o.name = this.name();
  _o.shortName = this.shortName();
  _o.description = this.description();
  _o.author = this.author();
  _o.version = this.version();
  _o.mapx = this.mapx();
  _o.mapy = this.mapy();
  _o.widthElmos = this.widthElmos();
  _o.heightElmos = this.heightElmos();
  _o.minHeight = this.minHeight();
  _o.maxHeight = this.maxHeight();
  _o.maxPlayers = this.maxPlayers();
  _o.startPositions = this.bb!.createObjList<MapStartPos, MapStartPosT>(this.startPositions.bind(this), this.startPositionsLength());
  _o.gravity = this.gravity();
  _o.tidalStrength = this.tidalStrength();
  _o.maxMetal = this.maxMetal();
  _o.extractorRadius = this.extractorRadius();
  _o.minimapUrl = this.minimapUrl();
  _o.hasLuaGaia = this.hasLuaGaia();
}
}

export class MapInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public id: string|Uint8Array|null = null,
  public name: string|Uint8Array|null = null,
  public shortName: string|Uint8Array|null = null,
  public description: string|Uint8Array|null = null,
  public author: string|Uint8Array|null = null,
  public version: string|Uint8Array|null = null,
  public mapx: number = 0,
  public mapy: number = 0,
  public widthElmos: number = 0,
  public heightElmos: number = 0,
  public minHeight: number = 0.0,
  public maxHeight: number = 0.0,
  public maxPlayers: number = 0,
  public startPositions: (MapStartPosT)[] = [],
  public gravity: number = 0.0,
  public tidalStrength: number = 0.0,
  public maxMetal: number = 0.0,
  public extractorRadius: number = 0.0,
  public minimapUrl: string|Uint8Array|null = null,
  public hasLuaGaia: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const id = (this.id !== null ? builder.createString(this.id!) : 0);
  const name = (this.name !== null ? builder.createString(this.name!) : 0);
  const shortName = (this.shortName !== null ? builder.createString(this.shortName!) : 0);
  const description = (this.description !== null ? builder.createString(this.description!) : 0);
  const author = (this.author !== null ? builder.createString(this.author!) : 0);
  const version = (this.version !== null ? builder.createString(this.version!) : 0);
  const startPositions = builder.createStructOffsetList(this.startPositions, MapInfo.startStartPositionsVector);
  const minimapUrl = (this.minimapUrl !== null ? builder.createString(this.minimapUrl!) : 0);

  return MapInfo.createMapInfo(builder,
    id,
    name,
    shortName,
    description,
    author,
    version,
    this.mapx,
    this.mapy,
    this.widthElmos,
    this.heightElmos,
    this.minHeight,
    this.maxHeight,
    this.maxPlayers,
    startPositions,
    this.gravity,
    this.tidalStrength,
    this.maxMetal,
    this.extractorRadius,
    minimapUrl,
    this.hasLuaGaia
  );
}
}
