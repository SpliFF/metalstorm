// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One ally team's start box in elmos (xmin, zmin, xmax, zmax) — matches
 * `Spring.GetAllyTeamStartBox`. NB the current headless flow never calls
 * `Spring.SetAllyTeamStartBox`, so every ally team carries the default
 * full-map box (0,0 → mapWidth,mapHeight); the field is streamed faithfully
 * so a game that *does* set boxes works without a second wire change.
 */
export class AllyStartBox implements flatbuffers.IUnpackableObject<AllyStartBoxT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):AllyStartBox {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

allyTeam():number {
  return this.bb!.readInt16(this.bb_pos);
}

xmin():number {
  return this.bb!.readFloat32(this.bb_pos + 4);
}

zmin():number {
  return this.bb!.readFloat32(this.bb_pos + 8);
}

xmax():number {
  return this.bb!.readFloat32(this.bb_pos + 12);
}

zmax():number {
  return this.bb!.readFloat32(this.bb_pos + 16);
}

static sizeOf():number {
  return 20;
}

static createAllyStartBox(builder:flatbuffers.Builder, ally_team: number, xmin: number, zmin: number, xmax: number, zmax: number):flatbuffers.Offset {
  builder.prep(4, 20);
  builder.writeFloat32(zmax);
  builder.writeFloat32(xmax);
  builder.writeFloat32(zmin);
  builder.writeFloat32(xmin);
  builder.pad(2);
  builder.writeInt16(ally_team);
  return builder.offset();
}


unpack(): AllyStartBoxT {
  return new AllyStartBoxT(
    this.allyTeam(),
    this.xmin(),
    this.zmin(),
    this.xmax(),
    this.zmax()
  );
}


unpackTo(_o: AllyStartBoxT): void {
  _o.allyTeam = this.allyTeam();
  _o.xmin = this.xmin();
  _o.zmin = this.zmin();
  _o.xmax = this.xmax();
  _o.zmax = this.zmax();
}
}

export class AllyStartBoxT implements flatbuffers.IGeneratedObject {
constructor(
  public allyTeam: number = 0,
  public xmin: number = 0.0,
  public zmin: number = 0.0,
  public xmax: number = 0.0,
  public zmax: number = 0.0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return AllyStartBox.createAllyStartBox(builder,
    this.allyTeam,
    this.xmin,
    this.zmin,
    this.xmax,
    this.zmax
  );
}
}
