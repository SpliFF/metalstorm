// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ProjectileTrajectoryReason } from '../spring-web/projectile-trajectory-reason.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * Trajectory change event. Sent only when the projectile bounces,
 * is steered onto a new target, or has its motion overridden. The
 * client rewrites its local projectile's pos+vel at the given frame.
 */
export class ProjectileTrajectoryEvent implements flatbuffers.IUnpackableObject<ProjectileTrajectoryEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ProjectileTrajectoryEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProjectileTrajectoryEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileTrajectoryEvent):ProjectileTrajectoryEvent {
  return (obj || new ProjectileTrajectoryEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProjectileTrajectoryEvent(bb:flatbuffers.ByteBuffer, obj?:ProjectileTrajectoryEvent):ProjectileTrajectoryEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ProjectileTrajectoryEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

pos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

vel(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

reason():ProjectileTrajectoryReason {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ProjectileTrajectoryReason.Bounce;
}

/**
 * Owner-team id — see ProjectileImpactEvent.team. Lets the per-session
 * filter keep ally trajectory updates flowing through fog of war.
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startProjectileTrajectoryEvent(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addProjId(builder:flatbuffers.Builder, projId:number) {
  builder.addFieldInt32(0, projId, 0);
}

static addPos(builder:flatbuffers.Builder, posOffset:flatbuffers.Offset) {
  builder.addFieldStruct(1, posOffset, 0);
}

static addVel(builder:flatbuffers.Builder, velOffset:flatbuffers.Offset) {
  builder.addFieldStruct(2, velOffset, 0);
}

static addReason(builder:flatbuffers.Builder, reason:ProjectileTrajectoryReason) {
  builder.addFieldInt8(3, reason, ProjectileTrajectoryReason.Bounce);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(4, team, 0);
}

static endProjectileTrajectoryEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): ProjectileTrajectoryEventT {
  return new ProjectileTrajectoryEventT(
    this.projId(),
    (this.pos() !== null ? this.pos()!.unpack() : null),
    (this.vel() !== null ? this.vel()!.unpack() : null),
    this.reason(),
    this.team()
  );
}


unpackTo(_o: ProjectileTrajectoryEventT): void {
  _o.projId = this.projId();
  _o.pos = (this.pos() !== null ? this.pos()!.unpack() : null);
  _o.vel = (this.vel() !== null ? this.vel()!.unpack() : null);
  _o.reason = this.reason();
  _o.team = this.team();
}
}

export class ProjectileTrajectoryEventT implements flatbuffers.IGeneratedObject {
constructor(
  public projId: number = 0,
  public pos: Vec3T|null = null,
  public vel: Vec3T|null = null,
  public reason: ProjectileTrajectoryReason = ProjectileTrajectoryReason.Bounce,
  public team: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  ProjectileTrajectoryEvent.startProjectileTrajectoryEvent(builder);
  ProjectileTrajectoryEvent.addProjId(builder, this.projId);
  ProjectileTrajectoryEvent.addPos(builder, (this.pos !== null ? this.pos!.pack(builder) : 0));
  ProjectileTrajectoryEvent.addVel(builder, (this.vel !== null ? this.vel!.pack(builder) : 0));
  ProjectileTrajectoryEvent.addReason(builder, this.reason);
  ProjectileTrajectoryEvent.addTeam(builder, this.team);

  return ProjectileTrajectoryEvent.endProjectileTrajectoryEvent(builder);
}
}
