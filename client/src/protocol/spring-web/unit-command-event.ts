// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitCommandKind } from '../spring-web/unit-command-kind.js';


/**
 * Server → Client: one per `UnitCommand` / `UnitCmdDone` event. Mirrors
 * the arguments LuaUI widgets receive on the matching callins. The
 * `tag` matches the value the queue snapshot reports for the same
 * command — widgets can correlate an Issued event with its later Done.
 */
export class UnitCommandEvent implements flatbuffers.IUnpackableObject<UnitCommandEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitCommandEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitCommandEvent(bb:flatbuffers.ByteBuffer, obj?:UnitCommandEvent):UnitCommandEvent {
  return (obj || new UnitCommandEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitCommandEvent(bb:flatbuffers.ByteBuffer, obj?:UnitCommandEvent):UnitCommandEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitCommandEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

kind():UnitCommandKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : UnitCommandKind.Issued;
}

unitId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

unitDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

unitTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

cmdId():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

params(index: number):number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat32(this.bb!.__vector(this.bb_pos + offset) + index * 4) : 0;
}

paramsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

paramsArray():Float32Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? new Float32Array(this.bb!.bytes().buffer, this.bb!.bytes().byteOffset + this.bb!.__vector(this.bb_pos + offset), this.bb!.__vector_len(this.bb_pos + offset)) : null;
}

options():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

tag():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Spring's `playerNum`. `-1` (= 255 unsigned) for system / Lua / AI
 * issued commands; player-issued events carry the originating player.
 */
playerId():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt16(this.bb_pos + offset) : -1;
}

/**
 * True when the command was issued by sim Lua (LuaRules, gadget:),
 * false when issued by player input or the engine's own scheduling.
 */
fromSynced():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * True when the command was issued by any Lua context (synced or
 * unsynced), false for engine / network-decoded sources.
 */
fromLua():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 24);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startUnitCommandEvent(builder:flatbuffers.Builder) {
  builder.startObject(11);
}

static addKind(builder:flatbuffers.Builder, kind:UnitCommandKind) {
  builder.addFieldInt8(0, kind, UnitCommandKind.Issued);
}

static addUnitId(builder:flatbuffers.Builder, unitId:number) {
  builder.addFieldInt32(1, unitId, 0);
}

static addUnitDefId(builder:flatbuffers.Builder, unitDefId:number) {
  builder.addFieldInt16(2, unitDefId, 0);
}

static addUnitTeam(builder:flatbuffers.Builder, unitTeam:number) {
  builder.addFieldInt8(3, unitTeam, 0);
}

static addCmdId(builder:flatbuffers.Builder, cmdId:number) {
  builder.addFieldInt32(4, cmdId, 0);
}

static addParams(builder:flatbuffers.Builder, paramsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(5, paramsOffset, 0);
}

static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array):flatbuffers.Offset;
/**
 * @deprecated This Uint8Array overload will be removed in the future.
 */
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Uint8Array):flatbuffers.Offset;
static createParamsVector(builder:flatbuffers.Builder, data:number[]|Float32Array|Uint8Array):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addFloat32(data[i]!);
  }
  return builder.endVector();
}

static startParamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addOptions(builder:flatbuffers.Builder, options:number) {
  builder.addFieldInt8(6, options, 0);
}

static addTag(builder:flatbuffers.Builder, tag:number) {
  builder.addFieldInt32(7, tag, 0);
}

static addPlayerId(builder:flatbuffers.Builder, playerId:number) {
  builder.addFieldInt16(8, playerId, -1);
}

static addFromSynced(builder:flatbuffers.Builder, fromSynced:boolean) {
  builder.addFieldInt8(9, +fromSynced, +false);
}

static addFromLua(builder:flatbuffers.Builder, fromLua:boolean) {
  builder.addFieldInt8(10, +fromLua, +false);
}

static endUnitCommandEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitCommandEvent(builder:flatbuffers.Builder, kind:UnitCommandKind, unitId:number, unitDefId:number, unitTeam:number, cmdId:number, paramsOffset:flatbuffers.Offset, options:number, tag:number, playerId:number, fromSynced:boolean, fromLua:boolean):flatbuffers.Offset {
  UnitCommandEvent.startUnitCommandEvent(builder);
  UnitCommandEvent.addKind(builder, kind);
  UnitCommandEvent.addUnitId(builder, unitId);
  UnitCommandEvent.addUnitDefId(builder, unitDefId);
  UnitCommandEvent.addUnitTeam(builder, unitTeam);
  UnitCommandEvent.addCmdId(builder, cmdId);
  UnitCommandEvent.addParams(builder, paramsOffset);
  UnitCommandEvent.addOptions(builder, options);
  UnitCommandEvent.addTag(builder, tag);
  UnitCommandEvent.addPlayerId(builder, playerId);
  UnitCommandEvent.addFromSynced(builder, fromSynced);
  UnitCommandEvent.addFromLua(builder, fromLua);
  return UnitCommandEvent.endUnitCommandEvent(builder);
}

unpack(): UnitCommandEventT {
  return new UnitCommandEventT(
    this.kind(),
    this.unitId(),
    this.unitDefId(),
    this.unitTeam(),
    this.cmdId(),
    this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength()),
    this.options(),
    this.tag(),
    this.playerId(),
    this.fromSynced(),
    this.fromLua()
  );
}


unpackTo(_o: UnitCommandEventT): void {
  _o.kind = this.kind();
  _o.unitId = this.unitId();
  _o.unitDefId = this.unitDefId();
  _o.unitTeam = this.unitTeam();
  _o.cmdId = this.cmdId();
  _o.params = this.bb!.createScalarList<number>(this.params.bind(this), this.paramsLength());
  _o.options = this.options();
  _o.tag = this.tag();
  _o.playerId = this.playerId();
  _o.fromSynced = this.fromSynced();
  _o.fromLua = this.fromLua();
}
}

export class UnitCommandEventT implements flatbuffers.IGeneratedObject {
constructor(
  public kind: UnitCommandKind = UnitCommandKind.Issued,
  public unitId: number = 0,
  public unitDefId: number = 0,
  public unitTeam: number = 0,
  public cmdId: number = 0,
  public params: (number)[] = [],
  public options: number = 0,
  public tag: number = 0,
  public playerId: number = -1,
  public fromSynced: boolean = false,
  public fromLua: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const params = UnitCommandEvent.createParamsVector(builder, this.params);

  return UnitCommandEvent.createUnitCommandEvent(builder,
    this.kind,
    this.unitId,
    this.unitDefId,
    this.unitTeam,
    this.cmdId,
    params,
    this.options,
    this.tag,
    this.playerId,
    this.fromSynced,
    this.fromLua
  );
}
}
