// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';



/**
 * One available AI plugin, enumerated from content/engine/ai + the
 * current game's data/games/<game>/ai directory. Sent as part
 * of AIListUpdate when a client subscribes to a room's AI list.
 */
export class RoomAIInfo implements flatbuffers.IUnpackableObject<RoomAIInfoT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):RoomAIInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsRoomAIInfo(bb:flatbuffers.ByteBuffer, obj?:RoomAIInfo):RoomAIInfo {
  return (obj || new RoomAIInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsRoomAIInfo(bb:flatbuffers.ByteBuffer, obj?:RoomAIInfo):RoomAIInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new RoomAIInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

aiId():string|null
aiId(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
aiId(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

displayName():string|null
displayName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
displayName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

description():string|null
description(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
description(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

isEngineProvided():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startRoomAIInfo(builder:flatbuffers.Builder) {
  builder.startObject(4);
}

static addAiId(builder:flatbuffers.Builder, aiIdOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, aiIdOffset, 0);
}

static addDisplayName(builder:flatbuffers.Builder, displayNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, displayNameOffset, 0);
}

static addDescription(builder:flatbuffers.Builder, descriptionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, descriptionOffset, 0);
}

static addIsEngineProvided(builder:flatbuffers.Builder, isEngineProvided:boolean) {
  builder.addFieldInt8(3, +isEngineProvided, +false);
}

static endRoomAIInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createRoomAIInfo(builder:flatbuffers.Builder, aiIdOffset:flatbuffers.Offset, displayNameOffset:flatbuffers.Offset, descriptionOffset:flatbuffers.Offset, isEngineProvided:boolean):flatbuffers.Offset {
  RoomAIInfo.startRoomAIInfo(builder);
  RoomAIInfo.addAiId(builder, aiIdOffset);
  RoomAIInfo.addDisplayName(builder, displayNameOffset);
  RoomAIInfo.addDescription(builder, descriptionOffset);
  RoomAIInfo.addIsEngineProvided(builder, isEngineProvided);
  return RoomAIInfo.endRoomAIInfo(builder);
}

unpack(): RoomAIInfoT {
  return new RoomAIInfoT(
    this.aiId(),
    this.displayName(),
    this.description(),
    this.isEngineProvided()
  );
}


unpackTo(_o: RoomAIInfoT): void {
  _o.aiId = this.aiId();
  _o.displayName = this.displayName();
  _o.description = this.description();
  _o.isEngineProvided = this.isEngineProvided();
}
}

export class RoomAIInfoT implements flatbuffers.IGeneratedObject {
constructor(
  public aiId: string|Uint8Array|null = null,
  public displayName: string|Uint8Array|null = null,
  public description: string|Uint8Array|null = null,
  public isEngineProvided: boolean = false
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  const aiId = (this.aiId !== null ? builder.createString(this.aiId!) : 0);
  const displayName = (this.displayName !== null ? builder.createString(this.displayName!) : 0);
  const description = (this.description !== null ? builder.createString(this.description!) : 0);

  return RoomAIInfo.createRoomAIInfo(builder,
    aiId,
    displayName,
    description,
    this.isEngineProvided
  );
}
}
