// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { UnitLifecycleKind } from '../spring-web/unit-lifecycle-kind.js';


export class UnitLifecycleEvent implements flatbuffers.IUnpackableObject<UnitLifecycleEventT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):UnitLifecycleEvent {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsUnitLifecycleEvent(bb:flatbuffers.ByteBuffer, obj?:UnitLifecycleEvent):UnitLifecycleEvent {
  return (obj || new UnitLifecycleEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsUnitLifecycleEvent(bb:flatbuffers.ByteBuffer, obj?:UnitLifecycleEvent):UnitLifecycleEvent {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new UnitLifecycleEvent()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

kind():UnitLifecycleKind {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : UnitLifecycleKind.FromFactory;
}

unitId():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

unitDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

unitTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

/**
 * FromFactory: the factory unit id. 0 for Taken/Given.
 */
factoryId():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * FromFactory: the factory def id. 0 for Taken/Given.
 */
factoryDefId():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint16(this.bb_pos + offset) : 0;
}

/**
 * FromFactory: true if the unit was built via a player-issued
 * build command, false if produced from a queued template.
 */
userOrders():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Taken/Given: pre-transfer team. -1 for FromFactory.
 */
oldTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : -1;
}

/**
 * Taken/Given: post-transfer team. -1 for FromFactory.
 */
newTeam():number {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readInt8(this.bb_pos + offset) : -1;
}

/**
 * Created: the initiating builder unit id. 0 = no builder
 * (Spring.CreateUnit without a builder argument, or unit produced
 * through some other code path). 0 for FromFactory/Taken/Given.
 */
builderId():number {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

static startUnitLifecycleEvent(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addKind(builder:flatbuffers.Builder, kind:UnitLifecycleKind) {
  builder.addFieldInt8(0, kind, UnitLifecycleKind.FromFactory);
}

static addUnitId(builder:flatbuffers.Builder, unitId:number) {
  builder.addFieldInt32(1, unitId, 0);
}

static addUnitDefId(builder:flatbuffers.Builder, unitDefId:number) {
  builder.addFieldInt16(2, unitDefId, 0);
}

static addUnitTeam(builder:flatbuffers.Builder, unitTeam:number) {
  builder.addFieldInt8(3, unitTeam, 0);
}

static addFactoryId(builder:flatbuffers.Builder, factoryId:number) {
  builder.addFieldInt32(4, factoryId, 0);
}

static addFactoryDefId(builder:flatbuffers.Builder, factoryDefId:number) {
  builder.addFieldInt16(5, factoryDefId, 0);
}

static addUserOrders(builder:flatbuffers.Builder, userOrders:boolean) {
  builder.addFieldInt8(6, +userOrders, +false);
}

static addOldTeam(builder:flatbuffers.Builder, oldTeam:number) {
  builder.addFieldInt8(7, oldTeam, -1);
}

static addNewTeam(builder:flatbuffers.Builder, newTeam:number) {
  builder.addFieldInt8(8, newTeam, -1);
}

static addBuilderId(builder:flatbuffers.Builder, builderId:number) {
  builder.addFieldInt32(9, builderId, 0);
}

static endUnitLifecycleEvent(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createUnitLifecycleEvent(builder:flatbuffers.Builder, kind:UnitLifecycleKind, unitId:number, unitDefId:number, unitTeam:number, factoryId:number, factoryDefId:number, userOrders:boolean, oldTeam:number, newTeam:number, builderId:number):flatbuffers.Offset {
  UnitLifecycleEvent.startUnitLifecycleEvent(builder);
  UnitLifecycleEvent.addKind(builder, kind);
  UnitLifecycleEvent.addUnitId(builder, unitId);
  UnitLifecycleEvent.addUnitDefId(builder, unitDefId);
  UnitLifecycleEvent.addUnitTeam(builder, unitTeam);
  UnitLifecycleEvent.addFactoryId(builder, factoryId);
  UnitLifecycleEvent.addFactoryDefId(builder, factoryDefId);
  UnitLifecycleEvent.addUserOrders(builder, userOrders);
  UnitLifecycleEvent.addOldTeam(builder, oldTeam);
  UnitLifecycleEvent.addNewTeam(builder, newTeam);
  UnitLifecycleEvent.addBuilderId(builder, builderId);
  return UnitLifecycleEvent.endUnitLifecycleEvent(builder);
}

unpack(): UnitLifecycleEventT {
  return new UnitLifecycleEventT(
    this.kind(),
    this.unitId(),
    this.unitDefId(),
    this.unitTeam(),
    this.factoryId(),
    this.factoryDefId(),
    this.userOrders(),
    this.oldTeam(),
    this.newTeam(),
    this.builderId()
  );
}


unpackTo(_o: UnitLifecycleEventT): void {
  _o.kind = this.kind();
  _o.unitId = this.unitId();
  _o.unitDefId = this.unitDefId();
  _o.unitTeam = this.unitTeam();
  _o.factoryId = this.factoryId();
  _o.factoryDefId = this.factoryDefId();
  _o.userOrders = this.userOrders();
  _o.oldTeam = this.oldTeam();
  _o.newTeam = this.newTeam();
  _o.builderId = this.builderId();
}
}

export class UnitLifecycleEventT implements flatbuffers.IGeneratedObject {
constructor(
  public kind: UnitLifecycleKind = UnitLifecycleKind.FromFactory,
  public unitId: number = 0,
  public unitDefId: number = 0,
  public unitTeam: number = 0,
  public factoryId: number = 0,
  public factoryDefId: number = 0,
  public userOrders: boolean = false,
  public oldTeam: number = -1,
  public newTeam: number = -1,
  public builderId: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  return UnitLifecycleEvent.createUnitLifecycleEvent(builder,
    this.kind,
    this.unitId,
    this.unitDefId,
    this.unitTeam,
    this.factoryId,
    this.factoryDefId,
    this.userOrders,
    this.oldTeam,
    this.newTeam,
    this.builderId
  );
}
}
