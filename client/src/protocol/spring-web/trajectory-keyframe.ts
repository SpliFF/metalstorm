// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TrajectoryKeyframeKind } from '../spring-web/trajectory-keyframe-kind.js';
import { Vec3, Vec3T } from '../spring-web/vec3.js';


/**
 * PLAN-latency L3 — a knot on a Tier-S projectile's flight path.
 *
 * Replaces ProjectileTrajectoryEvent's "rewrite pos/vel and keep
 * integrating" contract with "here is a point the path passes through, at
 * this frame". The client fits a Catmull-Rom spline through the knots
 * parametrised by `frame` and evaluates it at the presentation cursor P,
 * so the rendered path is a function of P alone — no local integrator, no
 * extrapolate-and-snap, and therefore no correction jumps.
 *
 * The `frame` field is the whole point: a trajectory event's position was
 * only meaningful relative to when the packet happened to arrive, which is
 * why the old path had to snap. A keyframe is timestamped on the sim
 * timeline and lands on the L1 presentation timeline unambiguously.
 * Ordering rule the client must apply: if two knots for the same proj_id
 * carry the same `frame`, the LATER one in the vector wins. That happens
 * when a sampled knot (Heartbeat/StageChange) and an event knot (Bounce,
 * Terminal) fall on one frame — the event knot is the post-event state and
 * is pushed second. Vector order is emission order, and a batch is drained
 * once per tick, so the pair can never straddle two batches.
 */
export class TrajectoryKeyframe implements flatbuffers.IUnpackableObject<TrajectoryKeyframeT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TrajectoryKeyframe {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTrajectoryKeyframe(bb:flatbuffers.ByteBuffer, obj?:TrajectoryKeyframe):TrajectoryKeyframe {
  return (obj || new TrajectoryKeyframe()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTrajectoryKeyframe(bb:flatbuffers.ByteBuffer, obj?:TrajectoryKeyframe):TrajectoryKeyframe {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TrajectoryKeyframe()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

projId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * Sim frame this knot is stamped at. Spline parameter, not a hint.
 */
frame():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

pos(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

vel(obj?:Vec3):Vec3|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new Vec3()).__init(this.bb_pos + offset, this.bb!) : null;
}

kind():TrajectoryKeyframeKind {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : TrajectoryKeyframeKind.Launch;
}

/**
 * Owner-team id — see ProjectileImpactEvent.team. Lets the per-session
 * filter keep ally keyframes flowing through fog of war.
 */
team():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : 0;
}

static startTrajectoryKeyframe(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addProjId(builder:flatbuffers.Builder, projId:number) {
  builder.addFieldInt32(0, projId, 0);
}

static addFrame(builder:flatbuffers.Builder, frame:number) {
  builder.addFieldInt32(1, frame, 0);
}

static addPos(builder:flatbuffers.Builder, posOffset:flatbuffers.Offset) {
  builder.addFieldStruct(2, posOffset, 0);
}

static addVel(builder:flatbuffers.Builder, velOffset:flatbuffers.Offset) {
  builder.addFieldStruct(3, velOffset, 0);
}

static addKind(builder:flatbuffers.Builder, kind:TrajectoryKeyframeKind) {
  builder.addFieldInt8(4, kind, TrajectoryKeyframeKind.Launch);
}

static addTeam(builder:flatbuffers.Builder, team:number) {
  builder.addFieldInt8(5, team, 0);
}

static endTrajectoryKeyframe(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}


unpack(): TrajectoryKeyframeT {
  return new TrajectoryKeyframeT(
    this.projId(),
    this.frame(),
    (this.pos() !== null ? this.pos()!.unpack() : null),
    (this.vel() !== null ? this.vel()!.unpack() : null),
    this.kind(),
    this.team()
  );
}


unpackTo(_o: TrajectoryKeyframeT): void {
  _o.projId = this.projId();
  _o.frame = this.frame();
  _o.pos = (this.pos() !== null ? this.pos()!.unpack() : null);
  _o.vel = (this.vel() !== null ? this.vel()!.unpack() : null);
  _o.kind = this.kind();
  _o.team = this.team();
}
}

export class TrajectoryKeyframeT implements flatbuffers.IGeneratedObject {
constructor(
  public projId: number = 0,
  public frame: number = 0,
  public pos: Vec3T|null = null,
  public vel: Vec3T|null = null,
  public kind: TrajectoryKeyframeKind = TrajectoryKeyframeKind.Launch,
  public team: number = 0
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  TrajectoryKeyframe.startTrajectoryKeyframe(builder);
  TrajectoryKeyframe.addProjId(builder, this.projId);
  TrajectoryKeyframe.addFrame(builder, this.frame);
  TrajectoryKeyframe.addPos(builder, (this.pos !== null ? this.pos!.pack(builder) : 0));
  TrajectoryKeyframe.addVel(builder, (this.vel !== null ? this.vel!.pack(builder) : 0));
  TrajectoryKeyframe.addKind(builder, this.kind);
  TrajectoryKeyframe.addTeam(builder, this.team);

  return TrajectoryKeyframe.endTrajectoryKeyframe(builder);
}
}
